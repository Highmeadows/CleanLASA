% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA149_labels.R
\name{apply_lasa149_labels}
\alias{apply_lasa149_labels}
\title{Apply LASA149 (General Self Regulation) SPSS labels}
\usage{
apply_lasa149_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA149 \code{.sav} file,
for example via \code{\link[haven:read_spss]{haven::read_sav()}}.}

\item{wave}{Character scalar identifying LASA wave \code{"G"} or \code{"H"};
matching is case-insensitive.}

\item{name_corrections}{Optional named character vector. Names are
canonical suffixes without a wave prefix (for example \code{qgsr01}) and
values are actual column names in \code{data}.}

\item{to_factor}{Logical. Convert labelled categorical variables to factors
using their documented value labels.}

\item{to_numeric}{Logical. Accepted for the shared interface; LASA149 has
no numeric variables eligible for conversion.}

\item{standardize_names}{Logical. Rename matched columns to lowercase
suffixes without the wave prefix, standardize the respondent-number
column to \code{respnr}, and add \code{LASA_wave}.}

\item{split_wavecode}{Logical. Remove the wave prefix from matched column
names and add \code{LASA_wave}. Always enabled by \code{standardize_names = TRUE}.}
}
\value{
\code{data} with documented metadata applied, optional conversion and
renaming, a \code{LASA_wave} attribute, and a matching audit in the
\code{label_report} attribute.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to the fourteen General Self Regulation (GSR) items
documented for LASA149. The item set covers strategy use, metastrategy
knowledge, action planning, and coping planning.
}
\details{
LASA149 is documented for waves G and H. Both waves contain the same
fourteen categorical items; only the wave prefix differs. All items use a
six-point agreement scale and the \code{-1} no-answer code. The GSR total and
subscale scores shown in the same codebook belong to filecode LASA349 and
are deliberately not included here.

Column matching tries, in order: an explicit \code{name_corrections} override,
an exact case-sensitive match, and a case-insensitive exact match.
\code{to_factor = TRUE} converts matched categorical items to factors while
retaining observed unlabelled codes as numeric-text levels. LASA149 has no
numeric variables eligible for \code{to_numeric} conversion. Original values
and labels are preserved in \code{original_values} and \code{original_labels}.
}
\examples{
dat <- data.frame(
  RespNr = 1:3,
  GQGSR01 = c(-1, 1, 6),
  gqgsr14 = c(1, 3, 6)
)
dat <- apply_lasa149_labels(dat, wave = "G")
attr(dat$GQGSR01, "labels")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
