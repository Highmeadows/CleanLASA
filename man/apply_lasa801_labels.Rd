% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA801_labels.R
\name{apply_lasa801_labels}
\alias{apply_lasa801_labels}
\title{Apply LASA801 (Coffee and Tea Consumption) SPSS Labels}
\usage{
apply_lasa801_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA801 \code{.sav} file.}

\item{wave}{Character scalar. Only \code{"J"} is documented as available.}

\item{name_corrections}{Optional named character vector mapping canonical
suffixes without the J prefix to actual column names in \code{data}.}

\item{to_factor}{Logical. Convert labelled categorical variables to factors.}

\item{to_numeric}{Logical. Restore eligible cup counts and free-text cup
volumes to plain numeric, replacing negative codes with \code{NA}.}

\item{standardize_names}{Logical. Standardize matched names and \code{respnr},
remove the wave prefix, and add \code{LASA_wave}.}

\item{split_wavecode}{Logical. Remove the wave prefix and add \code{LASA_wave};
implied by \code{standardize_names = TRUE}.}
}
\value{
\code{data} with LASA801 metadata, requested conversions and renaming,
preserved original coding, a \code{LASA_wave} attribute, and a \code{label_report}.
}
\description{
Attaches the variable and value labels documented for the 26 coffee- and
tea-consumption variables in LASA801. The available file is wave J. It
includes parallel questions for participants aged 40--65 years and for
consumption during the past month, plus usual coffee- and tea-cup volume.
Although the PDF header mentions wave K, its footnote states that K was not
yet available; consequently this function accepts only wave J.
}
\details{
Daily total cup counts and free-text "other" cup volumes are numeric and
eligible for \code{to_numeric}; negative missing codes become \code{NA}. Weekly
frequencies, beverage-specific cup categories (which include code 11 for
not applicable), and cup-size categories can be converted with
\code{to_factor}. Original coding is retained in reference attributes.

Matching tries \code{name_corrections}, an exact case-sensitive name, then a
case-insensitive exact name. Standardized naming removes the J prefix,
standardizes \code{respnr}, and adds \code{LASA_wave}. \code{split_wavecode} removes the
prefix and adds the wave column without otherwise standardizing \code{respnr}.
}
\examples{
dat <- data.frame(RespNr = 1:2, jqcof1 = c(0, 7), jqcof2 = c(-1, 4))
apply_lasa801_labels(dat, wave = "J", to_numeric = TRUE)
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
