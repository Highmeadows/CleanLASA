% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA141_labels.R
\name{apply_lasa141_labels}
\alias{apply_lasa141_labels}
\title{Apply LASA141 (Need for affiliation) SPSS labels}
\usage{
apply_lasa141_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LAS2B141 or LASAG141
\code{.sav} file, for example via \code{\link[haven:read_spss]{haven::read_sav()}}. Names are \code{bqan1},
\code{bqan4}, \code{bqan6}, \code{bqan8}, \code{bqan9}, and \code{bqan10} in 2B, with the
corresponding \code{g} prefix in wave G.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"2B"} or \code{"G"}.}

\item{name_corrections}{Optional named character vector mapping canonical
suffixes without the wave prefix (\code{qan1}, \code{qan4}, \code{qan6}, \code{qan8}, \code{qan9},
or \code{qan10}) to actual column names in \code{data}.}

\item{to_factor}{Logical. If \code{TRUE}, the categorical affiliation items are
converted to factors using the documented response labels.}

\item{to_numeric}{Logical. Retained for the shared parameter contract.
LASA141 contains no numeric count or continuous variables, so it does not
change the documented items.}

\item{standardize_names}{Logical. If \code{TRUE}, matched columns are renamed to
canonical lowercase suffixes without the wave prefix, \code{respnr} is
standardized, and \code{split_wavecode} is treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{TRUE}, matched columns have the wave
prefix removed and a \code{LASA_wave} column containing the selected wave is
inserted immediately after the respondent-number column.}
}
\value{
\code{data}, with LASA141 variable/value labels, optional factor
conversion and standardized naming, preserved original SPSS coding, the
\code{LASA_wave} provenance attribute, and a \code{label_report} matching audit.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to the six need-for-affiliation questionnaire items
documented in LASA141 for waves 2B and G.
}
\details{
The inventory consists of items 1, 4, 6, 8, 9, and 10. Each uses no answer
(-1), no (1), more-or-less (2), and yes (3). The constructed six-item score
shown in the same source PDF belongs to the separate LASA341 file and is
intentionally not labelled here. The PDF also cross-references LASA078,
which remains a separate filecode.

All LASA141 items are categorical. \code{to_factor = TRUE} converts them to
factors and preserves observed unlabelled codes as numeric-text levels;
\code{to_numeric} is retained for the shared parameter contract but does not
change them. Column matching tries an explicit \code{name_corrections} override,
an exact match, then a case-insensitive exact match. Standardized naming
optionally removes the \code{b} or \code{g} prefix, standardizes \code{respnr}, and adds
\code{LASA_wave}, while preserving original SPSS labels and values.
}
\examples{
dat <- data.frame(RespNr = 1:2, bqan1 = c(1, 3), bqan10 = c(-1, 2))
dat <- apply_lasa141_labels(dat, wave = "2B", to_factor = TRUE)
attr(dat$bqan1, "labels")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
