% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA021_labels.R
\name{apply_lasa021_labels}
\alias{apply_lasa021_labels}
\title{Apply LASA021 (Memory complaints and MMSE) SPSS labels}
\usage{
apply_lasa021_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA021 \code{.sav} file,
for example via \code{\link[haven:read_spss]{haven::read_sav()}}. Standard-wave names include
\code{bmemory1} and \code{bmmse01} through \code{bmmse23}; wave MB can additionally
contain \code{bmmsevrs} and \code{bimmse01} through \code{bimmse23}, excluding
\code{bimmse12}.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"B"}, \code{"C"}, \code{"D"}, \code{"E"}, \code{"2B"}, \code{"F"},
\code{"G"}, \code{"H"}, \code{"3B"}, \code{"MB"}, \code{"I"}, \code{"J"}, or \code{"K"}.}

\item{name_corrections}{Optional named character vector for explicit column
name overrides. Names are canonical LASA021 suffixes without the wave
prefix (for example \code{memory1}, \code{mmse12}, or \code{immse23}), and values are
actual column names in \code{data}, for example
\code{c(memory1 = "memory_problem")}.}

\item{to_factor}{Logical. If \code{FALSE} (default), categorical variables
remain numeric with SPSS-style value-label attributes. If \code{TRUE}, they
are converted to factors using their documented labels. An observed
value without a label remains a numeric-text factor level.}

\item{to_numeric}{Logical. If \code{FALSE} (default), bounded right-answer
counts retain their SPSS-style value labels. If \code{TRUE}, those counts are
restored to ordinary numeric, negative missing-reason codes become \code{NA},
and non-negative counts are retained. Binary items remain categorical.
This takes precedence over \code{to_factor} for count items.}

\item{standardize_names}{Logical. If \code{FALSE} (default), source column names
are retained except for renaming requested through \code{split_wavecode}. If
\code{TRUE}, matched LASA021 columns are renamed to their canonical lowercase
suffixes with the wave code removed; \code{"respnr"} is standardized; and
\code{split_wavecode} is always treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{FALSE} (default) and
\code{standardize_names = FALSE}, no wave-code splitting occurs. If \code{TRUE},
matched columns are renamed with their wave prefix removed and a new
\code{"LASA_wave"} column filled with \code{wave} is inserted immediately after
the respondent-number column. It is always treated as \code{TRUE} when
\code{standardize_names = TRUE}.}
}
\value{
\code{data}, with variable/value-label attributes attached to every
matched LASA021 column, optional factor conversion and canonical
renaming, and \code{original_labels}/\code{original_values} attributes preserving
the original SPSS coding. A \code{"LASA_wave"} column is added when requested.
The generic name-matching audit is attached as \code{attr(data, "label_report")} and can be retrieved with \code{\link[=lasa_label_report]{lasa_label_report()}}.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to the memory-complaint and Mini-Mental State
Examination variables documented in LASA021. The function covers all 13
documented waves. Standard waves contain two memory questions and 23 MMSE
items; wave MB additionally contains an MMSE-version variable and 22 items
from the illiterate MMSE, whose item 12 is not present in the codebook.
}
\details{
The coding changes after waves B/C. From D onward, memory question 1 and
the primary MMSE items add a -2 routing code referring to the wave-specific
MMSE score in LASA221. MMSE orientation items 02 through 10 instead refer
to item 01. Wave MB uses \code{bmmsevrs} routing and has distinct item wording,
including subtraction of 3 and days in reverse order. These differences
are selected from \code{wave}; no labels are inferred across wave schemas.

By default, matched variables remain numeric with their SPSS labels. With
\code{to_factor = TRUE}, they become factors using the documented label text;
observed unlabelled codes remain numeric-text levels. With
\code{to_numeric = TRUE}, the bounded right-answer counts are restored to plain
numeric, negative missing-reason codes become \code{NA}, and non-negative counts
are retained. Binary wrong/correct and no/yes items remain categorical.
Numeric conversion takes precedence over factor conversion for count items.

The source PDF also documents LASA221 (the maximum spelling/subtraction
MMSE score) and LASA421 (separate subtraction and spelling scores). Those
filecodes are intentionally outside LASA021 and their variables are not
changed by this function.

Column matching tries, in order: (1) an explicit override in
\code{name_corrections}, (2) an exact case-sensitive name match, and (3) a
case-insensitive exact match. Variables documented for the selected wave
but absent from \code{data} are left untouched and recorded as \code{"not found"} in
the generic matching audit.

\code{name_corrections}, \code{to_factor}, \code{to_numeric}, \code{standardize_names}, and
\code{split_wavecode} are part of the parameter contract shared by every
\verb{apply_*_labels()} function in this package. Regardless of conversion,
every matched column keeps its original SPSS value coding in
\code{attr(x, "original_labels")} and \code{attr(x, "original_values")}. The
\code{"respnr"} column is matched in any capitalization and, when
\code{standardize_names = TRUE}, renamed to \code{"respnr"}.
}
\examples{
dat <- data.frame(
  RespNr = 1:3,
  gmemory1 = c(-2, 1, 2),
  gmmse01 = c(-2, 1, 2),
  gmmse11 = c(-2, 1, 3)
)
dat <- apply_lasa021_labels(dat, wave = "G")
attr(dat$gmemory1, "labels")
attr(dat$gmmse11, "label")

mb <- data.frame(bmmsevrs = c(-2, 1, 2), bimmse01 = c(-2, 1, 2))
mb <- apply_lasa021_labels(mb, wave = "MB", to_factor = TRUE)
levels(mb$bimmse01)
}
\seealso{
\code{\link[=apply_lasa020_labels]{apply_lasa020_labels()}}, \code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
