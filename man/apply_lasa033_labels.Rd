% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA033_labels.R
\name{apply_lasa033_labels}
\alias{apply_lasa033_labels}
\title{Apply LASA033 (Receiving care) SPSS labels}
\usage{
apply_lasa033_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA033 \code{.sav} file,
for example via \code{\link[haven:read_spss]{haven::read_sav()}}. Depending on \code{wave}, names include
\code{bhealth1}, \code{hrhour201}, \code{iphelp06A}, and \code{krhelp02B}.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"B"}, \code{"C"}, \code{"D"}, \code{"E"}, \code{"2B"}, \code{"F"},
\code{"G"}, \code{"H"}, \code{"3B"}, \code{"MB"}, \code{"I"}, \code{"J"}, or \code{"K"}.}

\item{name_corrections}{Optional named character vector for explicit column
name overrides. Names are canonical LASA033 suffixes without the wave
prefix (for example \code{rhelpyn}, \code{phlp01y}, or \code{rhelp02b}), and values are
actual column names in \code{data}, for example
\code{c(rhelpyn = "receives_personal_care")}.}

\item{to_factor}{Logical. If \code{FALSE} (default), categorical variables
remain numeric with SPSS-style value-label attributes. If \code{TRUE}, they
are converted to factors using their documented labels. An observed
value without a label remains a numeric-text factor level. Numeric
variables are also factored unless \code{to_numeric = TRUE}.}

\item{to_numeric}{Logical. If \code{FALSE} (default), documented numeric
variables retain their SPSS-style missing/routing labels. If \code{TRUE},
years, counts, and hours become ordinary numeric, negative codes become
\code{NA}, and non-negative values are retained. Other variables remain
categorical. This takes precedence over \code{to_factor} for numeric fields.}

\item{standardize_names}{Logical. If \code{FALSE} (default), source column names
are retained except for renaming requested through \code{split_wavecode}. If
\code{TRUE}, matched LASA033 columns are renamed to their canonical lowercase
suffixes with the wave code removed; \code{"respnr"} is standardized; and
\code{split_wavecode} is always treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{FALSE} (default) and
\code{standardize_names = FALSE}, no wave-code splitting occurs. If \code{TRUE},
matched columns are renamed with their wave prefix removed and a new
\code{"LASA_wave"} column filled with \code{wave} is inserted immediately after
the respondent-number column. It is always treated as \code{TRUE} when
\code{standardize_names = TRUE}.}
}
\value{
\code{data}, with variable/value-label attributes attached to every
matched LASA033 column, optional factor/numeric conversion and canonical
renaming, and \code{original_labels}/\code{original_values} attributes preserving
the original SPSS coding. A \code{"LASA_wave"} column is added when requested.
The generic name-matching audit is attached as \code{attr(data, "label_report")} and can be retrieved with \code{\link[=lasa_label_report]{lasa_label_report()}}.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to the receiving-care variables documented in
LASA033. All 13 main-interview waves are supported. The exact inventory is
selected by \code{wave}; it ranges from 80 variables in wave E to 955 variables
in wave H.
}
\details{
The codebook changes substantially over time. Early waves distinguish help
with personal and domestic care and, except in wave E, who provided care
during illness. Later waves add total weekly hours, nursing, guidance and
administrative help, unmet needs, and control over caregiving. Waves I/J/K
split neighbours from friends; J/K additionally split children from
children-in-law. Wave H contains 660 person-level variables described as
ranges in the PDF. Those ranges are expanded here in the same documented
order: sex and weekly hours for up to five people in each provider group.

Variables that represent years, counts, or hours are marked as numeric.
With \code{to_numeric = TRUE}, their negative missing/routing codes become \code{NA}
and non-negative values are retained. All other variables remain
categorical. Numeric conversion takes precedence over factor conversion.
Wave-H person-level hour variables and helper-count variables have no value
labels in the source; their variable labels or value-label sets are left
correspondingly empty while they remain available for numeric conversion.

The source PDF also documents LASAH233 constructed scale-score variables.
They belong to filecode 233 and are intentionally not changed by this
LASA033 function.

Column matching tries, in order: (1) an explicit override in
\code{name_corrections}, (2) an exact case-sensitive name match, and (3) a
case-insensitive exact match. Variables documented for the selected wave
but absent from \code{data} are left untouched and recorded as \code{"not found"} in
the generic matching audit.

\code{name_corrections}, \code{to_factor}, \code{to_numeric}, \code{standardize_names}, and
\code{split_wavecode} are part of the parameter contract shared by every
\verb{apply_*_labels()} function in this package. Regardless of conversion,
every matched column keeps its original SPSS coding in
\code{attr(x, "original_labels")} and \code{attr(x, "original_values")}. The
\code{"respnr"} column is matched in any capitalization and, when
\code{standardize_names = TRUE}, renamed to \code{"respnr"}.
}
\examples{
dat <- data.frame(
  RespNr = 1:3,
  irhelpyn = c(-5, 1, 2),
  irhour01 = c(-4, 4, 12),
  iphelp06A = c(-2, 0, 1)
)
dat <- apply_lasa033_labels(dat, wave = "I")
attr(dat$irhelpyn, "labels")
}
\seealso{
\code{\link[=apply_lasa032_labels]{apply_lasa032_labels()}}, \code{\link[=apply_lasa034_labels]{apply_lasa034_labels()}},
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
