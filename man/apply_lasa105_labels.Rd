% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA105_labels.R
\name{apply_lasa105_labels}
\alias{apply_lasa105_labels}
\title{Apply LASA105 (Religious coping) SPSS labels}
\usage{
apply_lasa105_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA105 \code{.sav} file.
Names are \code{bqrco03}, \code{bqrco05}, and \code{bqrco07} in 3B, or \code{brco03},
\code{brco05}, and \code{brco07} in MB.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"3B"} or \code{"MB"}.}

\item{name_corrections}{Optional named character vector mapping the
wave-specific canonical suffixes without the \code{b} prefix (\code{qrco03},
\code{qrco05}, \code{qrco07}, \code{rco03}, \code{rco05}, or \code{rco07}) to actual column names
in \code{data}.}

\item{to_factor}{Logical. If \code{TRUE}, matched variables are converted to
factors using the documented labels; observed unlabelled codes remain
numeric-text levels.}

\item{to_numeric}{Logical. Accepted for interface consistency; LASA105 has
no variables eligible for numeric conversion.}

\item{standardize_names}{Logical. If \code{TRUE}, matched columns are renamed to
canonical lowercase suffixes, \code{respnr} is standardized, and
\code{split_wavecode} is treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{TRUE}, the leading \code{b} is removed from
matched names and \code{LASA_wave} is inserted immediately after the
respondent-number column.}
}
\value{
\code{data}, with LASA105 labels, optional factor conversion and name
standardization, preserved original SPSS coding, the \code{LASA_wave}
provenance attribute, and a \code{label_report} matching audit.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to the three religious-coping items documented in
LASA105 for waves 3B and MB.
}
\details{
Both waves assess turning to God in a crisis, confessing sins and asking
forgiveness, and feeling abandoned by God. Wave 3B uses suffixes \code{qrco03},
\code{qrco05}, and \code{qrco07}; MB uses \code{rco03}, \code{rco05}, and \code{rco07}. The response
map is identical in both waves and ranges from never to very often, with
separate negative codes for a short interview and an item not asked.

All LASA105 variables are categorical. Therefore \code{to_numeric} is accepted
for the shared interface but does not convert them. With
\code{to_factor = TRUE}, matched fields become factors and observed unlabelled
codes remain numeric-text levels.

Column matching tries an explicit \code{name_corrections} override, an exact
case-sensitive name, then a case-insensitive exact name. Every matched
column preserves its original coding in \code{original_labels} and
\code{original_values}. Requested standardization removes the leading \code{b},
standardizes \code{respnr}, and inserts \code{LASA_wave}.
}
\examples{
dat <- data.frame(bqrco03 = c(-2, 1, 4), bqrco07 = c(1, 2, 3))
dat <- apply_lasa105_labels(dat, wave = "3B")
attr(dat$bqrco03, "labels")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
