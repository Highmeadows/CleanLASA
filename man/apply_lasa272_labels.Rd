% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA272_labels.R
\name{apply_lasa272_labels}
\alias{apply_lasa272_labels}
\title{Apply LASA272 (Negative Life Events) SPSS labels}
\usage{
apply_lasa272_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA272 \code{.sav} file,
containing wave-prefixed variables such as \code{cfadied}, \code{dfadied}, or
\code{kfadied}.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"C"}, \code{"D"}, \code{"E"}, \code{"F"}, \code{"G"}, \code{"H"},
\code{"I"}, \code{"J"}, or \code{"K"}.}

\item{name_corrections}{Optional named character vector for explicit name
overrides. Names are canonical LASA272 suffixes without the wave prefix
(for example \code{fadied} or \code{illpart}), and values are the actual column
names in \code{data}, for example \code{c(fadied = "C_FATHER_DIED")}.}

\item{to_factor}{Logical. If \code{FALSE} (default), variables remain numeric
with SPSS-style value-label attributes. If \code{TRUE}, value-labelled
variables are converted to factors. Observed codes absent from the
codebook are retained as numeric-text levels rather than converted to
\code{NA}.}

\item{to_numeric}{Logical. If \code{FALSE} (default), variables retain their
value-label attributes. If \code{TRUE}, variables whose value labels consist
exclusively of negative missing-reason codes are converted to plain
numeric and negative values become \code{NA}. LASA272's documented variables
all include substantive response codes, so this option normally leaves
them labelled (or converts them to factors when \code{to_factor = TRUE}).}

\item{standardize_names}{Logical. If \code{FALSE} (default), original column
names are retained (except for any renaming triggered by
\code{split_wavecode}, see below). If \code{TRUE}, matched columns are renamed to
their canonical lowercase LASA272 names with the wave code removed,
such as \code{fadied} or \code{killpart}, \code{"respnr"} (in any capitalization) is
renamed to \code{"respnr"}, and \code{split_wavecode} is always treated as \code{TRUE}
as well, regardless of what was passed for \code{split_wavecode}.}

\item{split_wavecode}{Logical. If \code{FALSE} (default) and
\code{standardize_names = FALSE}, no wave-code splitting occurs. If \code{TRUE},
matched columns are renamed with the wave-letter prefix removed (e.g.
\code{cfadied} becomes \code{fadied}), and a new \code{"LASA_wave"} column, filled with
\code{wave}, is inserted right after \code{"respnr"}. Always treated as \code{TRUE}
when \code{standardize_names = TRUE}.}
}
\value{
\code{data}, with variable and value labels applied to matched LASA272
columns, optional factor conversion and name standardization, plus
reference \code{original_labels}/\code{original_values} attributes preserving the
original SPSS value coding regardless of that reshaping. A new
\code{"LASA_wave"} column is added after \code{"respnr"} when \code{standardize_names = TRUE} or \code{split_wavecode = TRUE}. A \code{"label_report"} attribute is
retrievable with \code{\link[=lasa_label_report]{lasa_label_report()}}.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to data imported from a LASA272 negative-life-events
\code{.sav} file. The function uses the wave-specific names, question wording,
and routing/missing-value codes documented for waves C through K.
}
\details{
Column matching tries, in order: (1) an explicit override in
\code{name_corrections}, (2) an exact case-sensitive match, and (3) a
case-insensitive exact match. Variables that cannot be matched are left
unchanged and recorded in the \code{"label_report"} attribute.

Regardless of \code{to_factor}/\code{to_numeric}, every matched column also keeps
its original SPSS value coding available as reference attributes --
\code{attr(x, "original_labels")} and \code{attr(x, "original_values")} -- and the
\code{"respnr"} column (in any capitalization) is matched and, when
\code{standardize_names = TRUE}, renamed to \code{"respnr"}.
}
\examples{
\dontrun{
dat_c <- haven::read_sav("LASAC272.SAV", user_na = TRUE)
dat_c <- apply_lasa272_labels(dat_c, wave = "C")

dat_k <- read_lasa_sav(
  "LASAK272.SAV",
  to_factor = TRUE,
  standardize_names = TRUE
)

lasa_label_report(dat_k, problems_only = TRUE)
}
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}},
\code{\link[=apply_lasa046_labels]{apply_lasa046_labels()}}
}
