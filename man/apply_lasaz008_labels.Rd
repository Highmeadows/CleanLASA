% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASAz008_labels.R
\name{apply_lasaz008_labels}
\alias{apply_lasaz008_labels}
\title{Apply LASAz008 (Age) SPSS labels}
\usage{
apply_lasaz008_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from \code{LASAz008.sav}.}

\item{wave}{Character scalar \code{"Z"}, matched case-insensitively.}

\item{name_corrections}{Optional named character vector mapping canonical
LASAz008 names to actual column names in \code{data}.}

\item{to_factor}{Logical. Convert value-labelled variables to factors while
retaining observed undocumented values as numeric-text levels.}

\item{to_numeric}{Logical. Restore eligible age/count variables to plain
numeric and replace negative values with \code{NA}.}

\item{standardize_names}{Logical. Lowercase matched names, standardize
\code{respnr}, and add \code{LASA_wave}.}

\item{split_wavecode}{Logical. Add \code{LASA_wave}; implied by
\code{standardize_names = TRUE}.}
}
\value{
\code{data} with LASAz008 metadata, requested conversions and naming,
an \code{LASA_wave} attribute, and a \code{label_report} matching audit.
}
\description{
Attaches the variable and value labels documented for the longitudinal
LASAz008 age file. The file combines LSN wave A and LASA waves B through K,
including the 2B and 3B cohorts, and stores interview dates plus age in
years and days for the main/telephone and medical interviews. LASMB008 is
documented in the same PDF but belongs to a separate data file and is not
labelled by this function.
}
\details{
LASAz008 is a wave-spanning \code{Z} file, so \code{wave} must be \code{"Z"}. Variables
whose only value label is the documented \code{-2} no-interview code are
eligible for \code{to_numeric}; negative observed values then become \code{NA}.
Date variables retain their date-valued no-interview label where the
codebook documents 03.10.1582 and are not treated as plain numeric.

Matching tries an explicit \code{name_corrections} entry, an exact match, and a
case-insensitive exact match, in that order. \code{standardize_names} lowercases
matched names and standardizes \code{respnr}; either \code{standardize_names} or
\code{split_wavecode} adds \code{LASA_wave = "Z"}. Original values and labels are
preserved in \code{original_values} and \code{original_labels} attributes.
}
\examples{
dat <- data.frame(RespNr = 1:2, bage = c(65, -2), bdays = c(24000, -2))
apply_lasaz008_labels(dat, wave = "Z", to_numeric = TRUE)
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
