% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA017_labels.R
\name{apply_lasa017_labels}
\alias{apply_lasa017_labels}
\title{Apply LASA017 (Income) SPSS labels}
\usage{
apply_lasa017_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA017 \code{.sav} file,
for example via \code{\link[haven:read_spss]{haven::read_sav()}}. Expected wave-prefixed names include
\code{brespinc}, \code{brinc01}, \code{fincc5s}, \code{iincc8s}, and \code{kincc10s}.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"B"}, \code{"C"}, \code{"D"}, \code{"E"}, \code{"2B"}, \code{"F"},
\code{"G"}, \code{"H"}, \code{"3B"}, \code{"MB"}, \code{"I"}, \code{"J"}, or \code{"K"}.}

\item{name_corrections}{Optional named character vector for explicit column
name overrides. Names are canonical LASA017 suffixes without the wave
prefix (for example \code{respinc}, \code{inccat}, or \code{incc8s}), and values are
actual column names in \code{data}, for example
\code{c(inccat = "B_INCOME_CATEGORY")}.}

\item{to_factor}{Logical. If \code{FALSE} (default), categorical variables
remain numeric with SPSS-style value-label attributes. If \code{TRUE}, they
are converted to factors using their documented value labels. Observed
values without a codebook label remain numeric-text levels rather than
becoming \code{NA}.}

\item{to_numeric}{Logical. Retained for the common apply-label parameter
contract. LASA017 documents categorical screens, source checklists, and
income bands rather than raw numeric income measures, so this argument
does not change LASA017 columns. Raw amounts documented for the separate
LASAB217 file are not processed here.}

\item{standardize_names}{Logical. If \code{FALSE} (default), source column names
are retained except for renaming requested through \code{split_wavecode}. If
\code{TRUE}, matched LASA017 columns are renamed to their canonical lowercase
suffixes with the wave code removed; \code{"respnr"} is standardized; and
\code{split_wavecode} is always treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{FALSE} (default) and
\code{standardize_names = FALSE}, no wave-code splitting occurs. If \code{TRUE},
matched columns are renamed with the wave prefix removed and a new
\code{"LASA_wave"} column filled with \code{wave} is inserted immediately after
the respondent-number column. It is always treated as \code{TRUE} when
\code{standardize_names = TRUE}.}
}
\value{
\code{data}, with variable/value-label attributes attached to every
matched LASA017 column, optional factor conversion and canonical
renaming, and \code{original_labels}/\code{original_values} attributes preserving
the original SPSS coding. A \code{"LASA_wave"} column is added when requested.
The generic name-matching audit is attached as \code{attr(data, "label_report")} and can be retrieved with \code{\link[=lasa_label_report]{lasa_label_report()}}.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to a LASA017 income data frame, using the wording,
income-category thresholds, routing references, and missing-value codes
documented for the requested wave. By default, matched variables remain
numeric. Optional arguments allow converting the categorical variables to
factors, correcting mismatched column names, and standardizing names to
the canonical LASA documentation spelling.
}
\details{
LASA017 changes across waves. Wave B uses 0/1 screening codes and Dutch
guilder categories; 2B contains respondent and partner income-source
checklists; F through I use progressively revised euro thresholds; J and K
introduce a new lowest category and shift the remaining category numbers;
and K adds response codes for an unreported partner amount and an unknown
combined amount. \code{wave} is therefore required, especially because B, 2B,
3B, and MB all use variable names beginning with \code{"b"}.

The shared C/D/E table prints substantive \code{inccat} category labels only
for wave C. For D and E, this function therefore attaches the documented
routing label but does not infer unprinted category labels. Observed
unlabelled codes are retained as numeric-text factor levels when
\code{to_factor = TRUE}.

\code{LASA017_varinfo.pdf} also contains LASAB217, a separate specification-
income file with income-source amounts. Those variables are deliberately
excluded from this function and belong in \code{apply_lasa217_labels()}.

Column matching tries, in order: (1) an explicit override in
\code{name_corrections}, (2) an exact case-sensitive name match, and (3) a
case-insensitive exact match. Variables documented for the selected wave
but absent from \code{data} are left untouched and recorded as \code{"not found"} in
the generic matching audit.

\code{name_corrections}, \code{to_factor}, \code{to_numeric}, \code{standardize_names}, and
\code{split_wavecode} are part of the parameter contract shared by every
\verb{apply_*_labels()} function in this package. Regardless of conversion,
every matched column keeps its original SPSS value coding in
\code{attr(x, "original_labels")} and \code{attr(x, "original_values")}. The
\code{"respnr"} column is matched in any capitalization and, when
\code{standardize_names = TRUE}, renamed to \code{"respnr"}.
}
\examples{
dat <- data.frame(
  RespNr = 1:3,
  IRESPINC = c(-4, 1, 2),
  IINCCAT = c(-2, 1, 24),
  IINCC8S = c(-2, 1, 2)
)
dat <- apply_lasa017_labels(dat, wave = "I")
attr(dat$IINCCAT, "labels")

dat <- apply_lasa017_labels(
  dat,
  wave = "I",
  to_factor = TRUE,
  standardize_names = TRUE
)
lasa_label_report(dat, problems_only = TRUE)
}
\seealso{
\code{\link[=apply_lasa016_labels]{apply_lasa016_labels()}}, \code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
