% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA133_labels.R
\name{apply_lasa133_labels}
\alias{apply_lasa133_labels}
\title{Apply LASA133 (SF-12 and WHO quality of life) SPSS labels}
\usage{
apply_lasa133_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA133 \code{.sav} file.
Names include wave-prefixed \code{quli01}--\code{quli12}, plus \code{quli13} and
\code{quli14} in waves I--K.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"D"}, \code{"E"}, \code{"F"}, \code{"G"}, \code{"H"},
\code{"3B"}, \code{"I"}, \code{"J"}, or \code{"K"}.}

\item{name_corrections}{Optional named character vector mapping suffixes
without the wave prefix (for example \code{quli01} or \code{quli14}) to actual
names in \code{data}.}

\item{to_factor}{Logical. If \code{TRUE}, matched variables are converted to
factors using the documented labels.}

\item{to_numeric}{Logical. Accepted for interface consistency; LASA133 has
no numeric variables eligible for conversion.}

\item{standardize_names}{Logical. If \code{TRUE}, matched names and \code{respnr} are
standardized and \code{split_wavecode} is treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{TRUE}, matched names lose the wave prefix
and a \code{LASA_wave} column is inserted after \code{respnr}.}
}
\value{
\code{data}, with LASA133 labels, requested conversion or renaming,
preserved original coding, \code{LASA_wave} provenance, and a \code{label_report}.
}
\description{
Attaches SPSS-style variable and value labels to the SF-12 and WHO
quality-of-life items documented in LASA133 for waves D, E, F, G, H, 3B,
I, J, and K.
}
\details{
Every documented wave contains the 12 SF-12 items for general health,
physical functioning, role limitations, pain, mental health, vitality, and
social functioning. Waves I, J, and K additionally contain two WHOQOL
items rating quality of life and whether life feels meaningful. Derived
scores in the separate LASA333 and LASA533 files are intentionally outside
this function's scope.

All LASA133 variables are categorical. \code{to_numeric} is accepted for
interface consistency but does not convert them. \code{to_factor = TRUE}
converts matched variables using the documented value labels.

Matching tries \code{name_corrections}, an exact case-sensitive match, and then
a case-insensitive exact match. Original SPSS coding is preserved;
standardized naming removes the wave prefix and can add \code{LASA_wave}.
}
\examples{
dat <- data.frame(dquli01 = c(1, 5), dquli08 = c(1, 4))
dat <- apply_lasa133_labels(dat, wave = "D", to_factor = TRUE)
attr(dat$dquli01, "labels")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
