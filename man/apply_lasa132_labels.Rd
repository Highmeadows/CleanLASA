% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA132_labels.R
\name{apply_lasa132_labels}
\alias{apply_lasa132_labels}
\title{Apply LASA132 (Filial obligations expectation) SPSS labels}
\usage{
apply_lasa132_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA132 \code{.sav} file.
Expected names are wave-prefixed forms of \code{qfioe01}--\code{qfioe16}.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"D"}, \code{"E"}, or \code{"J"}.}

\item{name_corrections}{Optional named character vector mapping suffixes
without the wave prefix (for example \code{qfioe01}) to actual names in
\code{data}.}

\item{to_factor}{Logical. If \code{TRUE}, matched variables are converted to
factors using the documented labels.}

\item{to_numeric}{Logical. Accepted for interface consistency; LASA132 has
no numeric variables eligible for conversion.}

\item{standardize_names}{Logical. If \code{TRUE}, matched names and \code{respnr} are
standardized and \code{split_wavecode} is treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{TRUE}, matched names lose the wave prefix
and a \code{LASA_wave} column is inserted after \code{respnr}.}
}
\value{
\code{data}, with LASA132 labels, requested conversion or renaming,
preserved original coding, \code{LASA_wave} provenance, and a \code{label_report}.
}
\description{
Attaches SPSS-style variable and value labels to the 16 filial-obligation
expectation items documented in LASA132 for waves D, E, and J.
}
\details{
The items address expectations that children live nearby, help during
illness or financially, maintain contact, accept responsibility, spend
time together, provide personal support and advice, check in, help, and
care. All use the same strongly-disagree--strongly-agree scale. The
aggregate score in the separate LASA332 file is outside this function's
scope.

All LASA132 variables are categorical. \code{to_numeric} is accepted for
interface consistency but does not convert them. \code{to_factor = TRUE}
converts matched variables using the documented value labels.

Matching tries \code{name_corrections}, an exact case-sensitive match, and then
a case-insensitive exact match. Original SPSS coding is preserved;
standardized naming removes the wave prefix and can add \code{LASA_wave}.
}
\examples{
dat <- data.frame(dqfioe01 = c(1, 5), dqfioe16 = c(2, 4))
dat <- apply_lasa132_labels(dat, wave = "D", to_factor = TRUE)
attr(dat$dqfioe01, "labels")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
