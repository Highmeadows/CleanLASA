% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA016_labels.R
\name{apply_lasa016_labels}
\alias{apply_lasa016_labels}
\title{Apply LASA016 (Employment Status) SPSS labels}
\usage{
apply_lasa016_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA016 \code{.sav} file,
for example via \code{\link[haven:read_spss]{haven::read_sav()}}. Expected names include \code{bjob1},
\code{gcjclass}, \code{hpcjsbc}, \code{ijobs}, \code{jrjobchange01}, and \code{kretyr01}.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"B"}, \code{"C"}, \code{"D"}, \code{"E"}, \code{"2B"}, \code{"F"},
\code{"G"}, \code{"H"}, \code{"3B"}, \code{"MB"}, \code{"I"}, \code{"J"}, or \code{"K"}.}

\item{name_corrections}{Optional named character vector for explicit column
name overrides. Names are canonical LASA016 suffixes without the wave
prefix (for example \code{job1}, \code{cjclass}, or \code{pjob6}), and values are actual
column names in \code{data}, for example \code{c(cjclass = "B_CJCLASS")}.}

\item{to_factor}{Logical. If \code{FALSE} (default), categorical variables
remain numeric with SPSS-style value-label attributes. If \code{TRUE}, they
are converted to factors using their value labels. Observed values
without a codebook label remain numeric-text levels. For the documented
duplicate J/K \code{tasks} codes, the first published label is used as the
factor level and the full mapping remains in \code{original_labels}.}

\item{to_numeric}{Logical. If \code{FALSE} (default), numeric measures retain
their SPSS-style missing-reason labels. If \code{TRUE}, numeric measures such
as hours, years, counts, percentages, ages, source occupation codes, and
prestige/productivity scores are restored to ordinary numeric vectors;
negative missing codes become \code{NA}. This takes precedence over
\code{to_factor} for those variables.}

\item{standardize_names}{Logical. If \code{FALSE} (default), source column names
are retained except for renaming requested through \code{split_wavecode}. If
\code{TRUE}, matched LASA016 columns are renamed to their canonical lowercase
suffixes with the wave code removed; \code{"respnr"} is standardized; and
\code{split_wavecode} is always treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{FALSE} (default) and
\code{standardize_names = FALSE}, no wave-code splitting occurs. If \code{TRUE},
matched columns are renamed with the wave prefix removed and a new
\code{"LASA_wave"} column filled with \code{wave} is inserted immediately after
the respondent-number column. It is always treated as \code{TRUE} when
\code{standardize_names = TRUE}.}
}
\value{
\code{data}, with variable/value-label attributes attached to every
matched LASA016 column, optional factor/numeric conversion and canonical
renaming, and \code{original_labels}/\code{original_values} attributes preserving
the original SPSS coding. A \code{"LASA_wave"} column is added when requested.
The generic name-matching audit is attached as \code{attr(data, "label_report")} and can be retrieved with \code{\link[=lasa_label_report]{lasa_label_report()}}.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to a LASA016 employment-status data frame. The
function includes all wave-specific variables and all six classification
tables printed in \code{LASA016_varinfo.pdf}: SBC92 occupational class, month,
skill type, job regularity, job type, and skill/work level.
}
\details{
LASA016 changes substantially across waves. Early waves use SBC92 source
variables and retirement modules, 2B adds respondent/father/partner job
histories, H introduces SBC2010 source variables, 3B and MB add last-job
histories, and J/K add job-change, work-ability, absence, and expanded
retirement modules. \code{wave} is required because B, 2B, 3B, and MB all use
variable names beginning with \code{"b"} while documenting different schemas.

By default, matched variables retain their imported numeric coding. With
\code{to_factor = TRUE}, categorical variables use their codebook labels as
factor levels. With \code{to_numeric = TRUE}, counts, hours, years, percentages,
ages, occupation/prestige scores, and other numeric measures are restored
to plain numeric and negative missing-reason codes become \code{NA}.

Two source anomalies are handled transparently. First, the K-wave table
prints the paid-job variable as \code{kkob1}, although all routing references
call it \code{KJOB1}. The function treats \code{kjob1} as canonical and automatically
accepts \code{kkob1} when that printed spelling is present. Second, the J/K
\code{tasks} rows assign -1 and -2 both to missing reasons and to substantive
task categories. Their \code{labels} and \code{original_labels} attributes preserve
all published entries in order. Because a factor cannot have two meanings
for one numeric code, factor conversion uses the first published meaning
for each duplicated code; the full ambiguous mapping remains available in
\code{original_labels} for auditing.

Several other routing labels that appear to contain typographical errors
(for example \code{"na, see G/JOBC"} and selected 3B cross-references) are also
preserved verbatim. This keeps the metadata directly comparable with the
published PDF instead of silently rewriting its wording.

Column matching tries, in order: (1) an explicit override in
\code{name_corrections}, (2) an exact case-sensitive name match, and (3) a
case-insensitive exact match. Variables documented for the selected wave
but absent from \code{data} are left untouched and recorded as \code{"not found"} in
the generic matching audit.

\code{name_corrections}, \code{to_factor}, \code{to_numeric}, \code{standardize_names}, and
\code{split_wavecode} are part of the parameter contract shared by every
\verb{apply_*_labels()} function in this package. Regardless of conversion,
every matched column keeps its original SPSS value coding in
\code{attr(x, "original_labels")} and \code{attr(x, "original_values")}. The
\code{"respnr"} column is matched in any capitalization and, when
\code{standardize_names = TRUE}, renamed to \code{"respnr"}.
}
\examples{
dat <- data.frame(
  RespNr = 1:3,
  IJOB1 = c(-4, 1, 2),
  IJOBSTAM = c(-3, 1, 12),
  IJOB6 = c(-2, 20, 40)
)
dat <- apply_lasa016_labels(dat, wave = "I")
attr(dat$IJOBSTAM, "labels")

dat <- apply_lasa016_labels(
  dat,
  wave = "I",
  to_factor = TRUE,
  to_numeric = TRUE,
  standardize_names = TRUE
)
lasa_label_report(dat, problems_only = TRUE)
}
\seealso{
\code{\link[=apply_lasa015_labels]{apply_lasa015_labels()}}, \code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
