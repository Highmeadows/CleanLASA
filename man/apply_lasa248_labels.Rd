% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA248_labels.R
\name{apply_lasa248_labels}
\alias{apply_lasa248_labels}
\title{Apply LASA248 (Children Summary and Status) SPSS labels}
\usage{
apply_lasa248_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA248 \code{.sav} file.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"E"}, \code{"2B"}, \code{"F"}, \code{"3B"}, or \code{"MB"}.}

\item{name_corrections}{Optional named character vector mapping canonical
suffixes without the wave prefix (for example \code{data048}, \code{nchild}, or
\code{Nrecs048}) to actual column names in \code{data}.}

\item{to_factor}{Logical. If \code{TRUE}, categorical variables are converted to
factors; observed unlabelled codes remain numeric-text levels.}

\item{to_numeric}{Logical. If \code{TRUE}, documented child-count variables are
converted to plain numeric and negative codes become \code{NA}.}

\item{standardize_names}{Logical. If \code{TRUE}, matched names and \code{respnr} are
standardized, wave prefixes are removed, and \code{LASA_wave} is added.}

\item{split_wavecode}{Logical. If \code{TRUE}, matched columns have the wave
prefix removed and \code{LASA_wave} is added. Implied by
\code{standardize_names = TRUE}.}
}
\value{
\code{data} with LASA248 metadata, requested conversions and renaming,
the \code{LASA_wave} provenance attribute, and a \code{label_report} matching audit.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to the LASA248 summary and data-status variables in
the LASA048 children codebook. The documented files cover waves E, 2B, F,
3B, and MB.
}
\details{
Waves E and F each contain one data-availability/status variable. Waves 2B
and 3B contain eight variables describing numbers and types of children,
childlessness, parental status, and completeness of the linked LASA048
records. MB contains the number of children reported in the face-to-face
demographic interview and the number represented in the medical-interview
LASA048 file. Coding differs materially between 2B and 3B.

Child-count variables are numeric. With \code{to_numeric = TRUE}, their negative
missing/routing codes become \code{NA} and their substantive counts remain plain
numeric. The remaining variables are categorical and can be converted to
factors with \code{to_factor = TRUE}. Original SPSS values and labels are always
retained in \code{original_values} and \code{original_labels}.

Column matching uses an explicit \code{name_corrections} override, an exact
case-sensitive match, and then a case-insensitive exact match. Standardized
naming removes the wave prefix, standardizes \code{respnr}, and inserts a
\code{LASA_wave} column.
}
\examples{
dat <- data.frame(
  RespNr = 1:2,
  bnchild = c(-1, 3),
  bchild = c(0, 2)
)
dat <- apply_lasa248_labels(dat, wave = "3B", to_numeric = TRUE)
attr(dat$bchild, "labels")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}},
\code{\link[=apply_lasa048_labels]{apply_lasa048_labels()}}
}
