% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA197_labels.R
\name{apply_lasa197_labels}
\alias{apply_lasa197_labels}
\title{Apply LASA197 (Awareness of Age-Related Change) SPSS labels}
\usage{
apply_lasa197_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA197 \code{.sav} file.}

\item{wave}{Character scalar identifying wave \code{"K"}, case-insensitively.}

\item{name_corrections}{Optional named character vector mapping canonical
suffixes such as \code{maarc1} to actual columns in \code{data}.}

\item{to_factor}{Logical. Convert labelled categorical items to factors.}

\item{to_numeric}{Logical. Accepted for the shared interface; LASA197 has
no numeric variables eligible for conversion.}

\item{standardize_names}{Logical. Standardize names and \code{respnr}, remove
wave prefixes, and add \code{LASA_wave}.}

\item{split_wavecode}{Logical. Remove wave prefixes and add \code{LASA_wave};
implied by \code{standardize_names = TRUE}.}
}
\value{
\code{data} with LASA197 metadata, optional conversion and renaming, the
\code{LASA_wave} attribute, and a \code{label_report} audit.
}
\description{
Attaches SPSS-style variable and value labels to the ten AARC-SF items
documented for LASA197 in wave K. The items cover age-related gains and
losses in relationships, cognition, health awareness, activities,
knowledge, energy, priorities, dependence, freedom, and motivation. All
use the documented five-level response scale plus the \code{-1} not-asked code.
}
\details{
Matching tries \code{name_corrections}, an exact match, and a case-insensitive
exact match. \code{to_factor = TRUE} converts matched items to factors while
keeping observed undocumented codes. LASA197 has no variables eligible for
\code{to_numeric}. Original SPSS values and labels are preserved. Standardized
naming removes the wave prefix, standardizes \code{respnr}, and adds \code{LASA_wave}.
}
\examples{
dat <- data.frame(RespNr = 1:2, KMAARC1 = c(-1, 1), kmaarc10 = c(2, 5))
apply_lasa197_labels(dat, wave = "K")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
