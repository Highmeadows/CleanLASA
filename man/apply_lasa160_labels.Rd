% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA160_labels.R
\name{apply_lasa160_labels}
\alias{apply_lasa160_labels}
\title{Apply LASA160 (Weight change) SPSS labels}
\usage{
apply_lasa160_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA160 \code{.sav} file.}

\item{wave}{Character scalar identifying wave \code{"B"}, \code{"C"}, \code{"D"}, \code{"E"},
\code{"2B"}, \code{"F"}, \code{"G"}, \code{"H"}, \code{"3B"}, \code{"MB"}, \code{"I"}, or \code{"J"}.}

\item{name_corrections}{Optional named character vector mapping canonical
suffixes without the wave prefix to actual column names.}

\item{to_factor}{Logical. Convert labelled categorical variables to factors.}

\item{to_numeric}{Logical. Restore eligible weight, age, and height
variables to numeric and replace negative missing codes with \code{NA}.}

\item{standardize_names}{Logical. Standardize matched names and \code{respnr},
remove the wave prefix, and add \code{LASA_wave}.}

\item{split_wavecode}{Logical. Remove wave prefixes and add \code{LASA_wave};
implied by \code{standardize_names = TRUE}.}
}
\value{
\code{data} with LASA160 metadata, optional reshaping, a \code{LASA_wave}
attribute, and a \code{label_report} matching audit.
}
\description{
Applies the wave-specific LASA160 labels for recent weight change, amount
gained or lost, stated and coded reasons, lifetime weight history, body-
weight appraisal, weight-loss methods, appetite, stress or illness, and
migrant-cohort unintentional-weight-loss screening. Supported files are B,
C, D, E, 2B, F, G, H, 3B, MB, I, and J.
}
\details{
Matching tries \code{name_corrections}, exact names, then case-insensitive exact
names. \code{to_factor} converts categorical variables to factors while keeping
observed undocumented codes. \code{to_numeric} restores documented weights,
amounts, ages, and heights to plain numeric and converts their negative
missing codes to \code{NA}. Original values and labels remain as attributes.
}
\examples{
dat <- data.frame(bmweightch = c(-1, 1, 3), bmweightchkg = c(-1, 2, 5))
apply_lasa160_labels(dat, wave = "B")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
