% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA127_labels.R
\name{apply_lasa127_labels}
\alias{apply_lasa127_labels}
\title{Apply LASA127 (End-of-life decisions) SPSS labels}
\usage{
apply_lasa127_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA127 \code{.sav} file.
Expected names run from \code{iqeol07}--\code{iqeol11}, with corresponding \code{j} or
\code{k} prefixes in later waves.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"I"}, \code{"J"}, or \code{"K"}.}

\item{name_corrections}{Optional named character vector mapping suffixes
without the wave prefix (for example \code{qeol07} or \code{qeol11}) to actual
names in \code{data}.}

\item{to_factor}{Logical. If \code{TRUE}, matched variables are converted to
factors using the documented labels.}

\item{to_numeric}{Logical. Accepted for interface consistency; LASA127 has
no numeric variables eligible for conversion.}

\item{standardize_names}{Logical. If \code{TRUE}, matched names and \code{respnr} are
standardized and \code{split_wavecode} is treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{TRUE}, matched names lose the wave prefix
and a \code{LASA_wave} column is inserted after \code{respnr}.}
}
\value{
\code{data}, with LASA127 labels, requested conversion or renaming,
preserved original coding, \code{LASA_wave} provenance, and a \code{label_report}.
}
\description{
Attaches SPSS-style variable and value labels to the five end-of-life
decision variables documented in LASA127 for waves I, J, and K.
}
\details{
The items cover lifetime thoughts about death, lifetime wishes for death,
wishes to live and to die during the past week, and the balance between
reasons for living and dying. The PDF notes that these are a subset of
medical-interview items asked in wave F under the separate LASA195 file;
those LASA195 variables are not combined here.

All five LASA127 variables are categorical. \code{to_numeric} is accepted for
interface consistency but does not convert them. \code{to_factor = TRUE}
converts matched items using their documented value labels.

Matching tries \code{name_corrections}, an exact case-sensitive match, and then
a case-insensitive exact match. Original SPSS coding is preserved;
standardized naming removes the wave prefix and can add \code{LASA_wave}.
}
\examples{
dat <- data.frame(iqeol07 = c(1, 2), iqeol09 = c(1, 3))
dat <- apply_lasa127_labels(dat, wave = "I", to_factor = TRUE)
attr(dat$iqeol07, "labels")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
