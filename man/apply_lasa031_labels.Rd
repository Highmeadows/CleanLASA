% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA031_labels.R
\name{apply_lasa031_labels}
\alias{apply_lasa031_labels}
\title{Apply LASA031 (Senses) SPSS labels}
\usage{
apply_lasa031_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA031 \code{.sav} file,
for example via \code{\link[haven:read_spss]{haven::read_sav()}}. Depending on \code{wave}, names include
\code{bsense01}, \code{gmsens01}, \code{hmsens7a}, \code{bmsens20}, and \code{jmsens22b}.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"B"}, \code{"C"}, \code{"D"}, \code{"E"}, \code{"2B"}, \code{"F"},
\code{"G"}, \code{"H"}, \code{"3B"}, \code{"MB"}, \code{"I"}, \code{"J"}, or \code{"K"}.}

\item{name_corrections}{Optional named character vector for explicit column
name overrides. Names are canonical LASA031 suffixes without the wave
prefix (for example \code{sense03}, \code{msens7a}, or \code{msens22b}), and values are
actual column names in \code{data}, for example
\code{c(msens01 = "wears_glasses")}.}

\item{to_factor}{Logical. If \code{FALSE} (default), categorical variables
remain numeric with SPSS-style value-label attributes. If \code{TRUE}, they
are converted to factors using their documented labels. An observed
value without a label remains a numeric-text factor level. J/K hearing
ratings are also factored unless \code{to_numeric = TRUE}.}

\item{to_numeric}{Logical. If \code{FALSE} (default), J/K \code{msens21}, \code{msens22a},
and \code{msens22b} retain their SPSS-style missing/routing labels. If \code{TRUE},
those 0--9 ratings become ordinary numeric, negative codes become \code{NA},
and non-negative ratings are retained. Other variables remain
categorical. This takes precedence over \code{to_factor} for the ratings.}

\item{standardize_names}{Logical. If \code{FALSE} (default), source column names
are retained except for renaming requested through \code{split_wavecode}. If
\code{TRUE}, matched LASA031 columns are renamed to their canonical lowercase
suffixes with the wave code removed; \code{"respnr"} is standardized; and
\code{split_wavecode} is always treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{FALSE} (default) and
\code{standardize_names = FALSE}, no wave-code splitting occurs. If \code{TRUE},
matched columns are renamed with their wave prefix removed and a new
\code{"LASA_wave"} column filled with \code{wave} is inserted immediately after
the respondent-number column. It is always treated as \code{TRUE} when
\code{standardize_names = TRUE}.}
}
\value{
\code{data}, with variable/value-label attributes attached to every
matched LASA031 column, optional factor/numeric conversion and canonical
renaming, and \code{original_labels}/\code{original_values} attributes preserving
the original SPSS coding. A \code{"LASA_wave"} column is added when requested.
The generic name-matching audit is attached as \code{attr(data, "label_report")} and can be retrieved with \code{\link[=lasa_label_report]{lasa_label_report()}}.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to the vision, hearing, tinnitus, and related sensory
variables documented in LASA031. The function covers all 13 waves and
selects the exact main- or medical-interview schema requested by \code{wave}.
Inventories range from nine variables in wave 2B to 29 in waves J/K.
}
\details{
LASA031 changes substantially over time. Early waves use \code{sense} variables;
medical-interview waves use \code{msens}; wave MB contains a reduced combined
with/without-aid questionnaire; and waves J/K split several hearing items
by zero, one, or two hearing aids. The later schemas also add tinnitus,
party-conversation, current-hearing-aid, and 0--9 hearing-rating variables.
This function follows each PDF table independently rather than filling
gaps from another wave.

Three conspicuous routing strings are preserved exactly as printed for
auditability: H \code{msens13} refers to \code{HMSENS7A1}, 3B \code{sense05} refers to
\code{BSENS01}, and J/K \code{msens04} refers to \verb{J/KMSENS01OF 03????}. The function
does not silently repair those source strings.

By default, matched variables remain numeric with their SPSS-style labels.
With \code{to_factor = TRUE}, categorical variables become factors and observed
unlabelled codes remain numeric-text levels. The J/K subjective hearing
ratings (\code{msens21}, \code{msens22a}, and \code{msens22b}) are numeric 0--9 scales:
with \code{to_numeric = TRUE}, their negative routing/missing codes become \code{NA}
and non-negative ratings are retained. Other LASA031 variables remain
categorical. Numeric conversion takes precedence over factor conversion
for those three ratings.

The source PDF also documents LASA231 constructed vision/hearing variables.
Those variables are intentionally outside LASA031 and are not changed by
this function.

Column matching tries, in order: (1) an explicit override in
\code{name_corrections}, (2) an exact case-sensitive name match, and (3) a
case-insensitive exact match. Variables documented for the selected wave
but absent from \code{data} are left untouched and recorded as \code{"not found"} in
the generic matching audit.

\code{name_corrections}, \code{to_factor}, \code{to_numeric}, \code{standardize_names}, and
\code{split_wavecode} are part of the parameter contract shared by every
\verb{apply_*_labels()} function in this package. Regardless of conversion,
every matched column keeps its original SPSS value coding in
\code{attr(x, "original_labels")} and \code{attr(x, "original_values")}. The
\code{"respnr"} column is matched in any capitalization and, when
\code{standardize_names = TRUE}, renamed to \code{"respnr"}.
}
\examples{
dat <- data.frame(
  RespNr = 1:3,
  hmsens01 = c(-5, 1, 2),
  hmsens7a = c(-5, 1, 6),
  hmsens18 = c(-2, 1, 2)
)
dat <- apply_lasa031_labels(dat, wave = "H")
attr(dat$hmsens7a, "labels")
}
\seealso{
\code{\link[=apply_lasa030_labels]{apply_lasa030_labels()}}, \code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
