% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA608_labels.R
\name{apply_lasa608_labels}
\alias{apply_lasa608_labels}
\title{Apply LASA608 (\link{PROXY} religious beliefs and philosophy) SPSS labels}
\usage{
apply_lasa608_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA608 \code{.sav} file.}

\item{wave}{Character scalar \code{"C"}; matching is case-insensitive.}

\item{name_corrections}{Optional named character vector mapping canonical
suffixes without the wave prefix to actual column names in \code{data}.}

\item{to_factor}{Logical. Convert labelled categorical variables to factors.}

\item{to_numeric}{Logical. Convert eligible codebook-numeric variables to
plain numeric; LASA608 has no such variables.}

\item{standardize_names}{Logical. Standardize matched names and \code{respnr},
remove the wave prefix, and add \code{LASA_wave}.}

\item{split_wavecode}{Logical. Remove wave prefixes from matched names and
add \code{LASA_wave}; implied by \code{standardize_names = TRUE}.}
}
\value{
\code{data} with LASA608 metadata, optional conversions and renaming, a
\code{LASA_wave} attribute, and a \code{label_report} matching audit.
}
\description{
Attaches the variable and value labels documented for the two LASA608
proxy telephone-interview items in wave C. Both items use a five-point
agreement scale. The documented second variable is spelled \code{ctprei10} in
the codebook and is therefore represented by canonical suffix \code{tprei10}.
}
\details{
Matching uses an explicit \code{name_corrections} entry, an exact match, then a
case-insensitive exact match. \code{to_factor = TRUE} converts the categorical
variables to factors while retaining observed undocumented codes. LASA608
contains no codebook-numeric variables, so \code{to_numeric} does not alter its
documented variables. Original values and labels remain available in
\code{original_values} and \code{original_labels} attributes.
}
\examples{
dat <- data.frame(RespNr = 1:2, ctprel09 = c(1, 4), ctprei10 = c(2, 5))
apply_lasa608_labels(dat, wave = "C")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
