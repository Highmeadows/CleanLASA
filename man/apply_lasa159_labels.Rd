% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA159_labels.R
\name{apply_lasa159_labels}
\alias{apply_lasa159_labels}
\title{Apply LASA159 (Decubitus) SPSS labels}
\usage{
apply_lasa159_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA159 \code{.sav} file.}

\item{wave}{Character scalar \code{"B"}; matching is case-insensitive.}

\item{name_corrections}{Optional named character vector mapping canonical
suffixes to actual column names.}

\item{to_factor}{Logical. Convert labelled variables to factors.}

\item{to_numeric}{Logical. Accepted for the shared interface; no LASA159
variables are eligible for numeric restoration.}

\item{standardize_names}{Logical. Standardize matched names and \code{respnr},
remove the wave prefix, and add \code{LASA_wave}.}

\item{split_wavecode}{Logical. Remove wave prefixes and add \code{LASA_wave};
implied by \code{standardize_names = TRUE}.}
}
\value{
\code{data} with LASA159 metadata, optional reshaping, the \code{LASA_wave}
attribute, and a \code{label_report} audit.
}
\description{
Attaches the documented labels for the eight wave-B LASA159 decubitus
variables: reported presence and location, most severe location, observed
stage, home and nursing-home treatment, occurrence during the past year,
and frequency. All response and missing-value codes follow the codebook.
}
\details{
Matching tries \code{name_corrections}, exact names, then case-insensitive exact
names. \code{to_factor = TRUE} converts labelled variables to factors while
retaining undocumented observed values. LASA159 has no variables eligible
for \code{to_numeric}. Original values and labels are preserved as attributes.
}
\examples{
dat <- data.frame(bmdecu01 = c(-1, 1, 2), bmdecu04 = c(0, 1, 2))
apply_lasa159_labels(dat, wave = "B")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
