% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA137_labels.R
\name{apply_lasa137_labels}
\alias{apply_lasa137_labels}
\title{Apply LASA137 (Valuation of late life) SPSS labels}
\usage{
apply_lasa137_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA137 \code{.sav} file,
for example via \code{\link[haven:read_spss]{haven::read_sav()}}. Wave-E names include \code{eqvall01}
through \code{eqvall19}; wave-F names use the \code{f} prefix for its 12-item
subset.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"E"} or \code{"F"}.}

\item{name_corrections}{Optional named character vector mapping canonical
suffixes without the wave prefix (\code{qvall01} through \code{qvall19}) to actual
column names in \code{data}.}

\item{to_factor}{Logical. If \code{TRUE}, the categorical valuation items are
converted to factors using the documented response labels.}

\item{to_numeric}{Logical. Retained for the shared parameter contract.
LASA137 contains no numeric count or continuous variables, so it does not
change the documented items.}

\item{standardize_names}{Logical. If \code{TRUE}, matched columns are renamed to
canonical lowercase suffixes without the wave prefix, \code{respnr} is
standardized, and \code{split_wavecode} is treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{TRUE}, matched columns have the wave
prefix removed and a \code{LASA_wave} column containing the selected wave is
inserted immediately after the respondent-number column.}
}
\value{
\code{data}, with LASA137 variable/value labels, optional factor
conversion and standardized naming, preserved original SPSS coding, the
\code{LASA_wave} provenance attribute, and a \code{label_report} matching audit.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to the valuation-of-late-life items documented in
LASA137 for waves E and F.
}
\details{
Wave E contains the full 19-item questionnaire. Wave F contains the 12
documented items 01, 04, 06, 07, 10, 11, 13, 14, 15, 16, 18, and 19. All
use the same agreement scale from strongly agree (1) to strongly disagree
(5), plus -1 for no answer. The total and subscale scores shown in the
source PDF belong to the separate LASA337 scale-score file and are not
handled here.

All LASA137 variables are categorical: \code{to_factor = TRUE} converts them to
factors, while \code{to_numeric} is retained for the shared parameter contract
but does not change them. Column matching tries an explicit
\code{name_corrections} override, an exact match, then a case-insensitive exact
match. Original SPSS coding is preserved. Standardized naming optionally
removes the wave prefix, standardizes \code{respnr}, and adds \code{LASA_wave}.
}
\examples{
dat <- data.frame(RespNr = 1:2, eqvall01 = c(1, 5), eqvall19 = c(-1, 3))
dat <- apply_lasa137_labels(dat, wave = "E", to_factor = TRUE)
attr(dat$eqvall01, "labels")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
