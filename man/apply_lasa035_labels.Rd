% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA035_labels.R
\name{apply_lasa035_labels}
\alias{apply_lasa035_labels}
\title{Apply LASA035 (Chronic diseases) SPSS labels}
\usage{
apply_lasa035_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA035 \code{.sav} file,
for example via \code{\link[haven:read_spss]{haven::read_sav()}}. Depending on \code{wave}, names include
\code{bcara01}, \code{hcance5m}, \code{irh10_1j}, and \code{KCORONA1_LC1}.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"B"}, \code{"C"}, \code{"D"}, \code{"E"}, \code{"2B"}, \code{"F"},
\code{"G"}, \code{"H"}, \code{"3B"}, \code{"MB"}, \code{"I"}, \code{"J"}, or \code{"K"}.}

\item{name_corrections}{Optional named character vector for explicit column
name overrides. Names are canonical LASA035 suffixes without the wave
prefix (for example \code{cara01}, \code{cance5m}, or \code{corona1_lc1}), and values
are actual column names in \code{data}, for example
\code{c(cara01 = "chronic_lung_disease")}.}

\item{to_factor}{Logical. If \code{FALSE} (default), categorical variables
remain numeric with SPSS-style value-label attributes. If \code{TRUE}, they
are converted to factors using their documented labels. An observed
value without a label remains a numeric-text factor level. Numeric
variables are also factored unless \code{to_numeric = TRUE}.}

\item{to_numeric}{Logical. If \code{FALSE} (default), numeric variables retain
their SPSS-style missing/routing labels. If \code{TRUE}, documented numeric
fields become ordinary numeric, negative codes become \code{NA}, and
non-negative values are retained. Other variables remain categorical.
This takes precedence over \code{to_factor} for numeric fields.}

\item{standardize_names}{Logical. If \code{FALSE} (default), source column names
are retained except for renaming requested through \code{split_wavecode}. If
\code{TRUE}, matched LASA035 columns are renamed to their canonical lowercase
suffixes with the wave code removed; \code{"respnr"} is standardized; and
\code{split_wavecode} is always treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{FALSE} (default) and
\code{standardize_names = FALSE}, no wave-code splitting occurs. If \code{TRUE},
matched columns are renamed with their wave prefix removed and a new
\code{"LASA_wave"} column filled with \code{wave} is inserted immediately after
the respondent-number column. It is always treated as \code{TRUE} when
\code{standardize_names = TRUE}.}
}
\value{
\code{data}, with variable/value-label attributes attached to every
matched LASA035 column, optional factor/numeric conversion and canonical
renaming, and \code{original_labels}/\code{original_values} attributes preserving
the original SPSS coding. A \code{"LASA_wave"} column is added when requested.
The generic name-matching audit is attached as \code{attr(data, "label_report")} and can be retrieved with \code{\link[=lasa_label_report]{lasa_label_report()}}.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to the chronic-disease variables documented in
LASA035 and its separate head-trauma supplement. All 13 main-interview
waves are supported. The selected inventories contain between eight
variables in wave MB and 185 variables in wave K.
}
\details{
The function follows seven independently documented schema families (B,
C, 2B/3B, D--H, MB, I/J, and K). It retains changes in disease-history
coding, month/year fields, tumor and metastasis checklists, joint surgery,
knee pain, hypertension, care-limitation items, and the K-wave COVID-19
and vaccination questions. The separate head-trauma codebook contributes
four variables to waves D through K.

Repeated \verb{see ...} rows inherit the complete value-label map of the
referenced earlier item. Month, other-disease, and joint-location tables
are expanded to their full documented value sets. Explicit wave-specific
exceptions are applied where a shared PDF table gives different labels or
routing codes for 2B versus 3B, D--F versus G/H, or I versus J.

By default, matched variables remain numeric with their SPSS-style labels.
With \code{to_factor = TRUE}, categorical variables become factors and observed
unlabelled codes remain numeric-text levels. Age, year, distance, and other
fields whose codebooks contain only negative missing codes qualify
automatically for numeric restoration. Head-trauma counts and the 0--10
knee-pain scales are also numeric: with \code{to_numeric = TRUE}, their negative
routing/missing codes become \code{NA} and non-negative values are retained.
Numeric conversion takes precedence over factor conversion.

The chronic-disease PDF also documents the derived filecodes 235 and 435.
They are intentionally outside LASA035 and are not changed by this
function.

Column matching tries, in order: (1) an explicit override in
\code{name_corrections}, (2) an exact case-sensitive name match, and (3) a
case-insensitive exact match. Variables documented for the selected wave
but absent from \code{data} are left untouched and recorded as \code{"not found"} in
the generic matching audit.

\code{name_corrections}, \code{to_factor}, \code{to_numeric}, \code{standardize_names}, and
\code{split_wavecode} are part of the parameter contract shared by every
\verb{apply_*_labels()} function in this package. Regardless of conversion,
every matched column keeps its original SPSS coding in
\code{attr(x, "original_labels")} and \code{attr(x, "original_values")}. The
\code{"respnr"} column is matched in any capitalization and, when
\code{standardize_names = TRUE}, renamed to \code{"respnr"}.
}
\examples{
dat <- data.frame(
  RespNr = 1:3,
  kcara01 = c(-5, 0, 3),
  kkneep2 = c(-3, 0, 10),
  KCORONA1 = c(-5, 1, 2)
)
dat <- apply_lasa035_labels(dat, wave = "K")
attr(dat$kcara01, "labels")
}
\seealso{
\code{\link[=apply_lasa034_labels]{apply_lasa034_labels()}}, \code{\link[=lasa_label_report]{lasa_label_report()}},
\code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
