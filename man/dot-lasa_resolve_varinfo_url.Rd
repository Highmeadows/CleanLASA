% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lasa_varinfo_resolve.R
\name{.lasa_resolve_varinfo_url}
\alias{.lasa_resolve_varinfo_url}
\title{Resolve a LASA varinfo PDF for a file code (or explicit URL)}
\usage{
.lasa_resolve_varinfo_url(filecode = NULL, url = NULL)
}
\arguments{
\item{filecode}{Optional LASA file code (e.g. \code{"046"}, \code{"z004"},
\code{"zoa1"}), matched via the same normalization \code{lasa_topics()} uses
internally. A bare group
prefix that isn't itself a documented file code (e.g. \code{"zoa"} when
only \code{"zoa1"}/\code{"zoa2"}/\code{"zoa3"} are documented, but all three share one
PDF) also resolves, as long as it's unambiguous.}

\item{url}{Optional explicit HTTPS URL (or local path). When supplied,
\code{filecode} is not looked up at all -- resolution is bypassed entirely.}
}
\value{
A list with \code{filecodes} (character vector: every normalized
file code documented by this PDF, including the requested one),
\code{varinfo_url} (HTTPS URL, or \code{NA_character_} when only resolved via
the offline manifest -- see \code{varinfo_file}), \code{varinfo_file} (the PDF's
own file name, always available), and \code{source} (\code{"explicit"},
\code{"online"}, or \code{"offline"}).
}
\description{
Resolve a LASA varinfo PDF for a file code (or explicit URL)
}
\keyword{internal}
