% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA023_labels.R
\name{apply_lasa023_labels}
\alias{apply_lasa023_labels}
\title{Apply LASA023 (Everyday memory) SPSS labels}
\usage{
apply_lasa023_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA023 \code{.sav} file,
for example via \code{\link[haven:read_spss]{haven::read_sav()}}. Wave-B names are \code{bevmem01} through
\code{bevmem08} and \code{ndays}; wave F contains \code{fevmem08}.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. LASA023 is documented for \code{"B"} and \code{"F"}.}

\item{name_corrections}{Optional named character vector for explicit column
name overrides. Names are canonical LASA023 suffixes without the wave
prefix (for example \code{evmem01}, \code{ndays}, or \code{evmem08}), and values are
actual column names in \code{data}, for example
\code{c(evmem01 = "pencil_choice")}.}

\item{to_factor}{Logical. If \code{FALSE} (default), categorical variables
remain numeric with SPSS-style value-label attributes. If \code{TRUE}, they
are converted to factors using their documented labels. An observed
value without a label remains a numeric-text factor level.}

\item{to_numeric}{Logical. Retained for the common apply-label parameter
contract. LASA023 contains no count or continuous variables, so this
argument does not change the documented categorical variables.}

\item{standardize_names}{Logical. If \code{FALSE} (default), source column names
are retained except for renaming requested through \code{split_wavecode}. If
\code{TRUE}, matched LASA023 columns are renamed to their canonical lowercase
suffixes; \code{"respnr"} is standardized; and \code{split_wavecode} is always
treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{FALSE} (default) and
\code{standardize_names = FALSE}, no wave-code splitting occurs. If \code{TRUE},
wave-prefixed matched columns are renamed with the prefix removed,
\code{ndays} retains its already-unprefixed canonical name, and a new
\code{"LASA_wave"} column filled with \code{wave} is inserted immediately after
the respondent-number column. It is always treated as \code{TRUE} when
\code{standardize_names = TRUE}.}
}
\value{
\code{data}, with variable/value-label attributes attached to every
matched LASA023 column, optional factor conversion and canonical
renaming, and \code{original_labels}/\code{original_values} attributes preserving
the original SPSS coding. A \code{"LASA_wave"} column is added when requested.
The generic name-matching audit is attached as \code{attr(data, "label_report")} and can be retrieved with \code{\link[=lasa_label_report]{lasa_label_report()}}.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to the everyday-memory variables documented in
LASA023. Wave B contains eight task/observation variables plus the
unprefixed \code{ndays} interview-day variable. Wave F contains only the
handedness observation, with a different coding scheme.
}
\details{
The codebook spells wave B's interview-day column \code{ndays}, without the
otherwise consistent \code{b} wave prefix. The function recognizes that exact
spelling, in any capitalization, automatically. An explicit \code{ndays}
entry in \code{name_corrections} still has priority, and a source column named
\code{bndays} can also be matched through the ordinary shared rules.

By default, matched variables remain numeric with their SPSS-style labels.
With \code{to_factor = TRUE}, they become factors using the documented label
text; observed unlabelled codes remain numeric-text levels. All LASA023
variables are categorical task results or observations, so \code{to_numeric}
is retained for the common function contract but does not change them.

Column matching tries, in order: (1) an explicit override in
\code{name_corrections}, (2) an exact case-sensitive name match, and (3) a
case-insensitive exact match. Variables documented for the selected wave
but absent from \code{data} are left untouched and recorded as \code{"not found"} in
the generic matching audit.

\code{name_corrections}, \code{to_factor}, \code{to_numeric}, \code{standardize_names}, and
\code{split_wavecode} are part of the parameter contract shared by every
\verb{apply_*_labels()} function in this package. Regardless of conversion,
every matched column keeps its original SPSS value coding in
\code{attr(x, "original_labels")} and \code{attr(x, "original_values")}. The
\code{"respnr"} column is matched in any capitalization and, when
\code{standardize_names = TRUE}, renamed to \code{"respnr"}.
}
\examples{
dat <- data.frame(
  RespNr = 1:3,
  bevmem01 = c(-5, 0, 2),
  ndays = c(-5, 0, 2),
  bevmem08 = c(-1, 1, 2)
)
dat <- apply_lasa023_labels(dat, wave = "B")
attr(dat$bevmem01, "labels")
attr(dat$ndays, "label")
}
\seealso{
\code{\link[=apply_lasa022_labels]{apply_lasa022_labels()}}, \code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
