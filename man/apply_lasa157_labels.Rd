% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA157_labels.R
\name{apply_lasa157_labels}
\alias{apply_lasa157_labels}
\title{Apply LASA157 (Contrast sensitivity test) SPSS labels}
\usage{
apply_lasa157_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA157 \code{.sav} file.}

\item{wave}{Character scalar: \code{"B"}, \code{"C"}, \code{"D"}, \code{"E"}, \code{"2B"}, or
\code{"F"}; matching is case-insensitive.}

\item{name_corrections}{Optional named character vector mapping canonical
suffixes without the wave prefix to actual column names in \code{data}.}

\item{to_factor}{Logical. Convert labelled categorical variables to factors.}

\item{to_numeric}{Logical. Convert eligible test measurements and scores to
plain numeric and replace documented negative missing codes with \code{NA}.}

\item{standardize_names}{Logical. Standardize matched names and \code{respnr},
remove the wave prefix, and add \code{LASA_wave}.}

\item{split_wavecode}{Logical. Remove wave prefixes from matched names and
add \code{LASA_wave}; implied by \code{standardize_names = TRUE}.}
}
\value{
\code{data} with LASA157 metadata, optional conversions and renaming, a
\code{LASA_wave} attribute, and a \code{label_report} matching audit.
}
\description{
Attaches the variable and value labels documented for LASA157 contrast
sensitivity data. Waves B, C, and D contain functional test variables;
waves E, 2B, and F contain only the six self-reported daily-sight items.
The inventories and missing-value codes differ materially by wave.
}
\details{
Matching uses an explicit \code{name_corrections} entry, an exact match, then a
case-insensitive exact match. \code{to_factor = TRUE} converts categorical
variables to factors while retaining observed undocumented codes.
\code{to_numeric = TRUE} restores contrast measurements and scores whose
codebook labels contain only negative missing-value codes to plain numeric,
replacing those negative codes with \code{NA}. Original values and labels remain
available in \code{original_values} and \code{original_labels} attributes.
}
\examples{
dat <- data.frame(RespNr = 1:2, bmdasig1 = c(-1, 2), bmcontra = c(-1, 12))
apply_lasa157_labels(dat, wave = "B")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
