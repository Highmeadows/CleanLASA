% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA108_labels.R
\name{apply_lasa108_labels}
\alias{apply_lasa108_labels}
\title{Apply LASA108 (Advance care planning) SPSS labels}
\usage{
apply_lasa108_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA108 \code{.sav} file.
Names range from \code{iqacp1a} through \code{iqacp10e} in I and from \code{kqacp1a}
through \code{kqacp7e} in K.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"I"} or \code{"K"}.}

\item{name_corrections}{Optional named character vector mapping canonical
suffixes without the wave prefix (for example \code{qacp1a}, \code{qacp5e}, or
\code{qacp10e}) to actual column names in \code{data}.}

\item{to_factor}{Logical. If \code{TRUE}, matched variables are converted to
factors using the documented labels; observed unlabelled codes remain
numeric-text levels.}

\item{to_numeric}{Logical. Accepted for interface consistency; LASA108 has
no variables eligible for numeric conversion.}

\item{standardize_names}{Logical. If \code{TRUE}, matched columns are renamed to
canonical lowercase suffixes without the wave prefix, \code{respnr} is
standardized, and \code{split_wavecode} is treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{TRUE}, matched names have their wave
prefix removed and \code{LASA_wave} is inserted immediately after the
respondent-number column.}
}
\value{
\code{data}, with LASA108 labels, optional factor conversion and name
standardization, preserved original SPSS coding, the \code{LASA_wave}
provenance attribute, and a \code{label_report} matching audit.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to advance-care-planning variables documented in
LASA108 for waves I and K.
}
\details{
Wave I contains 48 yes/no items across ten planning topics: future health,
a partner's future health, future care and guidance, informal care,
substitute medical decision-making, nursing-home admission,
life-extending treatment, resuscitation, euthanasia, and deathbed wishes.
The four partner-health items additionally use code -2 for not applicable.
Wave K uses a redesigned 35-item mention inventory across seven topics,
with five indicators per topic for thinking, discussion, and a living will.

The source PDF repeats \code{kqacp1a} through \code{kqacp2e} verbatim on its final
page. They are implemented once in their first documented position, so the
wave-K inventory contains 35 unique variables rather than 45 rows.

All LASA108 variables are categorical. \code{to_numeric} is accepted for the
shared interface but does not convert them. With \code{to_factor = TRUE},
matched variables become factors and observed unlabelled codes remain
numeric-text levels.

Column matching tries an explicit \code{name_corrections} override, an exact
case-sensitive name, then a case-insensitive exact name. Matched columns
preserve the original SPSS coding in \code{original_labels} and
\code{original_values}. Requested standardization removes the wave prefix,
standardizes \code{respnr}, and inserts \code{LASA_wave}.
}
\examples{
dat <- data.frame(iqacp1a = c(-1, 1, 2), iqacp2a = c(-2, 1, 2))
dat <- apply_lasa108_labels(dat, wave = "I")
attr(dat$iqacp2a, "labels")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
