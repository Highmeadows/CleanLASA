% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA047_labels.R
\name{apply_lasa047_labels}
\alias{apply_lasa047_labels}
\title{Apply LASA047 (Network domain identification and significance) labels}
\usage{
apply_lasa047_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA047 \code{.sav} file,
for example via \code{\link[haven:read_spss]{haven::read_sav()}}. Depending on \code{wave}, names include
\code{bnwmem}, \code{gnwtrav}, \code{bnwmsupp}, and \code{knwconf}.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"B"}, \code{"C"}, \code{"D"}, \code{"E"}, \code{"2B"}, \code{"F"},
\code{"G"}, \code{"H"}, \code{"3B"}, \code{"I"}, \code{"J"}, or \code{"K"}.}

\item{name_corrections}{Optional named character vector for explicit column
name overrides. Names are canonical LASA047 suffixes without the wave
prefix (for example \code{nwmem}, \code{nwtype}, \code{nwtrav}, \code{emo_rec}, or \code{nwconf}),
and values are actual column names in \code{data}.}

\item{to_factor}{Logical. If \code{FALSE} (default), variables retain their
numeric representation with SPSS-style value-label attributes. If
\code{TRUE}, categorical variables are converted to factors using the
documented labels. Observed unlabelled codes remain numeric-text levels.}

\item{to_numeric}{Logical. If \code{TRUE}, \code{nwmem}, \code{freqran}, and \code{nwtrav} are
restored to plain numeric and negative values are replaced by \code{NA}.
Other LASA047 variables remain categorical. This conversion takes
precedence over \code{to_factor} for eligible numeric fields.}

\item{standardize_names}{Logical. If \code{FALSE} (default), source column names
are retained except for renaming requested through \code{split_wavecode}. If
\code{TRUE}, matched LASA047 columns are renamed to their canonical lowercase
suffixes with the wave prefix removed; \code{"respnr"} is standardized; and
\code{split_wavecode} is always treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{FALSE} (default) and
\code{standardize_names = FALSE}, no wave-code splitting occurs. If \code{TRUE},
matched columns are renamed with their wave prefix removed and a new
\code{"LASA_wave"} column filled with \code{wave} is inserted immediately after
the respondent-number column. It is always treated as \code{TRUE} when
\code{standardize_names = TRUE}.}
}
\value{
\code{data}, with variable/value-label attributes attached to every
matched LASA047 column, optional factor/numeric conversion and canonical
renaming, and \code{original_labels}/\code{original_values} attributes preserving
the original SPSS coding. A \code{"LASA_wave"} column is added when requested.
The generic name-matching audit is attached as \code{attr(data, "label_report")} and can be retrieved with \code{\link[=lasa_label_report]{lasa_label_report()}}.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to member-level social-network variables documented
in LASA047. The implementation covers waves B, C, D, E, 2B, F, G, H, 3B,
I, J, and K; the PDF does not document an MB file.
}
\details{
The file contains one record for each nominated network member. It records
the nomination domain, household status, relationship type, contact
frequency, travel time, support-question order, and four types of support.
Wave B additionally contains partner status and most-supportive-member
indicators. Later waves contain confidant identification instead. The
ranking-tie variable \code{random} is present only through wave G.

This function labels variables stored in the member-level LASA047 files
only. Network-size and aggregate variables stored under constructed
filecode 247 are outside its scope.

\code{nwmem}, \code{freqran}, and \code{nwtrav} are numeric identifiers, ranks, or
measurements. With \code{to_numeric = TRUE}, these fields are restored to plain
numeric and all observed negative values become \code{NA}; substantive values
such as travel-time boundaries 0, 60, and 1440 remain numeric. Other
LASA047 variables are categorical and are unaffected by \code{to_numeric}.
With \code{to_factor = TRUE}, categorical variables become factors and observed
unlabelled codes remain numeric-text levels. For eligible numeric fields,
\code{to_numeric} takes precedence when both conversions are requested.

Column matching tries, in order: (1) an explicit override in
\code{name_corrections}, (2) an exact case-sensitive name match, and (3) a
case-insensitive exact match. Variables documented for the selected wave
but absent from \code{data} are left untouched and recorded as \code{"not found"} in
the generic matching audit.

\code{name_corrections}, \code{to_factor}, \code{to_numeric}, \code{standardize_names}, and
\code{split_wavecode} follow the parameter contract shared by the package's
other \verb{apply_*_labels()} functions. Regardless of conversion, every
matched column keeps its original SPSS coding in
\code{attr(x, "original_labels")} and \code{attr(x, "original_values")}. The
\code{"respnr"} column is matched in any capitalization and, when
\code{standardize_names = TRUE}, renamed to \code{"respnr"}.
}
\examples{
dat <- data.frame(
  RespNr = 1:3,
  gnwmem = c(10101, 10102, 10103),
  gnwfreq = c(-6, 4, 8),
  gnwtrav = c(-7, 60, 1440)
)
dat <- apply_lasa047_labels(dat, wave = "G", to_numeric = TRUE)
attr(dat$gnwfreq, "labels")
}
\seealso{
\code{\link[=apply_lasa045_labels]{apply_lasa045_labels()}}, \code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
