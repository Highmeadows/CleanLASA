% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA106_labels.R
\name{apply_lasa106_labels}
\alias{apply_lasa106_labels}
\title{Apply LASA106 (Work perception) SPSS labels}
\usage{
apply_lasa106_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA106 \code{.sav} file.
Depending on \code{wave}, names include \code{bqworkd}, \code{iqworkd1}, \code{jqworkd18},
\code{kqworkd21}, \code{bqworkYN}, \code{bqworkn1}, and \code{bqworkp13}.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"3B"}, \code{"MB"}, \code{"I"}, \code{"J"}, or \code{"K"}.}

\item{name_corrections}{Optional named character vector mapping canonical
suffixes without the wave prefix (for example \code{qworkd}, \code{qworkd18},
\code{qworkyn}, \code{qworkn1}, or \code{qworkp1}) to actual column names in \code{data}.}

\item{to_factor}{Logical. If \code{TRUE}, matched variables are converted to
factors using the documented labels; observed unlabelled codes remain
numeric-text levels.}

\item{to_numeric}{Logical. Accepted for interface consistency; LASA106 has
no variables eligible for numeric conversion.}

\item{standardize_names}{Logical. If \code{TRUE}, matched variables are renamed
to canonical lowercase suffixes without the wave prefix, \code{respnr} is
standardized, and \code{split_wavecode} is treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{TRUE}, matched names have their wave
prefix removed and a \code{LASA_wave} column is inserted immediately after
the respondent-number column.}
}
\value{
\code{data}, with LASA106 labels, optional factor conversion and name
standardization, preserved original SPSS coding, the \code{LASA_wave}
provenance attribute, and a \code{label_report} matching audit.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to the work-perception variables documented in
LASA106 for waves 3B, MB, I, J, and K.
}
\details{
Waves 3B, I, J, and K contain an employment-history item followed by 21
work-demand items. Items 1--17 permit \verb{not applicable} only in wave I;
social-support items 18--21 permit it in waves I, J, and K. Wave MB uses a
redesigned medical-interview schema: a four-level employment-history item
and 13 paired \verb{work demands}/\verb{work demanded} items, for 27 variables total.
The documented wave-specific inventories, labels, and response maps are
retained.

LASA106 fields are categorical. \code{to_numeric} is accepted for consistency
with the shared interface but does not convert them. With
\code{to_factor = TRUE}, matched variables become factors using the documented
response labels; observed unlabelled codes remain numeric-text levels.

Column matching tries an explicit \code{name_corrections} override, an exact
case-sensitive name, then a case-insensitive exact name. Every matched
field preserves the original SPSS coding in \code{original_labels} and
\code{original_values}. Requested standardization removes the wave prefix,
standardizes \code{respnr}, and inserts \code{LASA_wave}.
}
\examples{
dat <- data.frame(iqworkd = c(1, 2), iqworkd1 = c(1, 5))
dat <- apply_lasa106_labels(dat, wave = "I")
attr(dat$iqworkd1, "labels")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
