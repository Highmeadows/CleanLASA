% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA128_labels.R
\name{apply_lasa128_labels}
\alias{apply_lasa128_labels}
\title{Apply LASA128 (Donorship) SPSS labels}
\usage{
apply_lasa128_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA128 \code{.sav} file.
Names may include \code{iqdona3}, \code{iqdona8}, \code{jqdona1_2}, or \code{jqdona1_5}.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"I"} or \code{"J"}.}

\item{name_corrections}{Optional named character vector mapping suffixes
without the wave prefix (for example \code{qdona3} or \code{qdona1_5}) to actual
names in \code{data}.}

\item{to_factor}{Logical. If \code{TRUE}, categorical variables are converted to
factors using the documented value labels.}

\item{to_numeric}{Logical. If \code{TRUE}, the wave-I registration-year fields
are restored to plain numeric and negative values become \code{NA}.}

\item{standardize_names}{Logical. If \code{TRUE}, matched names and \code{respnr} are
standardized and \code{split_wavecode} is treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{TRUE}, matched names lose the wave prefix
and a \code{LASA_wave} column is inserted after \code{respnr}.}
}
\value{
\code{data}, with LASA128 labels, requested conversion or renaming,
preserved original coding, \code{LASA_wave} provenance, and a \code{label_report}.
}
\description{
Attaches SPSS-style variable and value labels to donorship variables
documented in LASA128 for waves I and J.
}
\details{
Wave I has eight variables on blood or plasma donor registration, former
registration years and reasons for stopping, recent donation frequency,
and organ-donation decisions. Wave J has five variables: current donor
registration and questions about decisions before and after the new donor
law.

The two wave-I registration-year fields are numeric and eligible for
\code{to_numeric}; their negative routing/missing codes become \code{NA}. The other
fields are categorical and can be converted with \code{to_factor}.

Matching tries \code{name_corrections}, an exact case-sensitive match, and then
a case-insensitive exact match. Original SPSS coding is preserved;
standardized naming removes the wave prefix and can add \code{LASA_wave}.
}
\examples{
dat <- data.frame(iqdona1 = c(1, 3), iqdona3 = c(-2, 1995))
dat <- apply_lasa128_labels(dat, wave = "I", to_numeric = TRUE)
attr(dat$iqdona1, "labels")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
