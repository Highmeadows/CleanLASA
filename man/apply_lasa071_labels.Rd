% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA071_labels.R
\name{apply_lasa071_labels}
\alias{apply_lasa071_labels}
\title{Apply LASA071 (Attitude toward ageing) SPSS labels}
\usage{
apply_lasa071_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASAB071 or LASAK071
\code{.sav} file.}

\item{wave}{Character scalar identifying wave \code{"B"} or \code{"K"}; matching is
case-insensitive.}

\item{name_corrections}{Optional named character vector mapping canonical
suffixes without the wave prefix (\code{eval01} through \code{eval04} at B, and
\code{eval01}, \code{eval02}, \code{beval03}, and \code{eval04} at K) to actual names.}

\item{to_factor}{Logical. Convert matched variables to factors using the
documented value labels; observed undocumented codes remain text levels.}

\item{to_numeric}{Logical. Accepted for interface consistency; LASA071 has
no eligible numeric variables.}

\item{standardize_names}{Logical. Rename matched columns to lowercase
canonical suffixes, standardize \code{respnr}, and imply wave splitting.}

\item{split_wavecode}{Logical. Remove the wave prefix from matched names and
insert \code{LASA_wave} after the respondent-number column.}
}
\value{
The labelled data, with requested factor conversion and canonical
names, preserved original coding, \code{LASA_wave}, and \code{label_report}.
}
\description{
Attaches the variable and value labels documented for the four evaluation-
of-age-norms items in LASA071. The file is documented at waves B and K.
Items concern daily pursuits, financial situation, contact with family or
friends, and current affairs. Both waves use no/more-or-less/yes response
categories, with wave-specific missing-reason labels and codes.
}
\details{
The documented wave-K name for the third item is \code{kbeval03}, unlike
\code{keval01}, \code{keval02}, and \code{keval04}; this spelling is preserved. Its
canonical suffix for matching and standardized output is \code{beval03}.

All variables are categorical, so \code{to_numeric} is accepted for the shared
interface but does not convert them. Matching tries \code{name_corrections},
exact names, and then case-insensitive exact names. Conversion and renaming
preserve original coding and produce the generic \code{label_report} audit.
}
\examples{
dat <- data.frame(beval01 = c(-4, 1, 3))
dat <- apply_lasa071_labels(dat, wave = "B")
attr(dat$beval01, "labels")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
