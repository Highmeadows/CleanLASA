% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA015_labels.R
\name{apply_lasa015_labels}
\alias{apply_lasa015_labels}
\title{Apply LASA015 (Ownership and Neighbourhood) SPSS labels}
\usage{
apply_lasa015_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA015 \code{.sav} file,
for example via \code{\link[haven:read_spss]{haven::read_sav()}}. Expected wave-prefixed names include
\code{bhowner}, \code{cmovefut}, \code{ghouse2}, and \code{gh2locsp}.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"B"}, \code{"C"}, \code{"D"}, \code{"E"}, \code{"2B"}, \code{"F"},
\code{"G"}, \code{"H"}, \code{"3B"}, \code{"I"}, \code{"J"}, or \code{"K"}.}

\item{name_corrections}{Optional named character vector for explicit column
name overrides. Names are canonical LASA015 suffixes without the wave
prefix (for example \code{howner}, \code{movefut}, or \code{h2loc}), and values are the
actual column names in \code{data}, for example
\code{c(howner = "B_HOWNER")}.}

\item{to_factor}{Logical. If \code{FALSE} (default), categorical variables
remain numeric with SPSS-style value-label attributes. If \code{TRUE}, they
are converted to factors using their value labels as levels. Observed
values without a codebook label are retained as numeric-text levels
rather than converted to \code{NA}.}

\item{to_numeric}{Logical. If \code{FALSE} (default), years in the neighbourhood
retains its SPSS-style missing-reason labels. If \code{TRUE}, \code{yneigh} is
restored to an ordinary numeric vector, negative missing-reason codes
are replaced with \code{NA}, and non-negative years are retained. This takes
precedence over \code{to_factor} for \code{yneigh}; all other variables remain
labelled or become factors according to \code{to_factor}.}

\item{standardize_names}{Logical. If \code{FALSE} (default), source column names
are retained except for renaming requested through \code{split_wavecode}. If
\code{TRUE}, matched LASA015 columns are renamed to their canonical lowercase
suffixes with the wave code removed; \code{"respnr"} in any capitalization is
renamed to \code{"respnr"}; and \code{split_wavecode} is always treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{FALSE} (default) and
\code{standardize_names = FALSE}, no wave-code splitting occurs. If \code{TRUE},
matched columns are renamed with the wave prefix removed (for example,
\code{ghouse2} becomes \code{house2}) and a new \code{"LASA_wave"} column filled with
\code{wave} is inserted immediately after the respondent-number column. It is
always treated as \code{TRUE} when \code{standardize_names = TRUE}.}
}
\value{
\code{data}, with variable- and value-label attributes attached to every
matched LASA015 column, optional factor/numeric conversion and canonical
renaming, and \code{original_labels}/\code{original_values} attributes preserving
the original SPSS coding. A \code{"LASA_wave"} column is added after
\code{"respnr"} when \code{standardize_names = TRUE} or \code{split_wavecode = TRUE}.
The generic variable-name audit is attached as \code{attr(data, "label_report")} and can be retrieved with \code{\link[=lasa_label_report]{lasa_label_report()}}.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to a LASA015 ownership-and-neighbourhood data frame,
using the variable names, wording, routing references, and missing-value
codes documented for the requested wave. By default, matched variables
remain numeric. Optional arguments allow converting categorical variables
to factors, restoring years-in-neighbourhood to plain numeric, correcting
mismatched column names, and standardizing names to the canonical LASA
documentation spelling.
}
\details{
LASA015 changes substantially across waves. Wave B uses a short-version
missing code and 0/1 response codes; wave 2B adds detailed intended-move
variables and an interview-terminated code; wave G adds a second-home
module; and wave 3B omits the routing code from two neighbourhood items.
The remaining waves share a core schema, although \code{movefut} is documented
only for C, D, and E. \code{wave} is therefore required, especially because B,
2B, and 3B all use variable names beginning with \code{"b"}.

The LASA015 PDF records the \code{gh22owns} routing label as
\code{"na, see GH21OWN"}, even though the variable describes an \code{gh22own}
follow-up. This function preserves the published wording verbatim so its
value-label metadata remains auditable against the source document.

Column matching tries, in order: (1) an explicit override in
\code{name_corrections}, (2) an exact case-sensitive name match, and (3) a
case-insensitive exact match. Variables documented for the selected wave
but absent from \code{data} are left untouched and recorded as \code{"not found"} in
the generic matching audit.

\code{name_corrections}, \code{to_factor}, \code{to_numeric}, \code{standardize_names}, and
\code{split_wavecode} are part of the parameter contract shared by every
\verb{apply_*_labels()} function in this package. Regardless of
\code{to_factor}/\code{to_numeric}, every matched column keeps its original SPSS
value coding in \code{attr(x, "original_labels")} and
\code{attr(x, "original_values")}. The \code{"respnr"} column is matched in any
capitalization and, when \code{standardize_names = TRUE}, renamed to
\code{"respnr"}.
}
\examples{
dat_b <- data.frame(
  BHOWNER = c(-4, 1, 2),
  BMORTGA = c(-2, 1, 2),
  BYNEIGH = c(-4, 8, 20)
)
dat_b <- apply_lasa015_labels(dat_b, wave = "B")
attr(dat_b$BHOWNER, "labels")

dat_g <- data.frame(
  RespNr = 1:3,
  GHOUSE2 = c(-1, 1, 2),
  GHS21 = c(-2, 0, 1),
  GH2DURA = c(-2, 1, 4)
)
dat_g <- apply_lasa015_labels(
  dat_g,
  wave = "G",
  to_factor = TRUE,
  standardize_names = TRUE
)
lasa_label_report(dat_g, problems_only = TRUE)
}
\seealso{
\code{\link[=apply_lasa014_labels]{apply_lasa014_labels()}}, \code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
