% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA226_labels.R
\name{apply_lasa226_labels}
\alias{apply_lasa226_labels}
\title{Apply LASA226 (Anxiety scale scores) SPSS labels}
\usage{
apply_lasa226_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA226 \code{.sav} file.}

\item{wave}{Character scalar identifying B, C, D, E, F, G, H, 3B, MB, I,
or J.}

\item{name_corrections}{Optional named character vector mapping canonical
suffixes for the selected wave to actual columns.}

\item{to_factor}{Logical. Convert labelled categorical variables to factors
unless numeric restoration takes precedence.}

\item{to_numeric}{Logical. Restore the anxiety score to plain numeric and
replace negative values with \code{NA}.}

\item{standardize_names}{Logical. Standardize names and \code{respnr}, remove
wave prefixes, and add \code{LASA_wave}.}

\item{split_wavecode}{Logical. Remove wave prefixes and add \code{LASA_wave};
implied by \code{standardize_names = TRUE}.}
}
\value{
\code{data} with LASA226 metadata, optional conversion and renaming, the
\code{LASA_wave} attribute, and a \code{label_report} audit.
}
\description{
Attaches SPSS-style labels to the anxiety total score and reason-missing
variable documented in LASA226 for waves B, C, D, E, F, G, H, 3B, MB, I,
and J. The score ranges from 0 to 21. B, the regular later waves, and MB
use materially different names, variable-label wording, and value maps.
}
\details{
The total score is numeric: \code{to_numeric = TRUE} restores plain numeric
values and replaces negative codes with \code{NA}. The reason field remains
categorical and supports \code{to_factor = TRUE}. B includes code 4 for refused
test; later and MB reason maps end at code 3. Matching, original-code
preservation, standardized naming, \code{respnr}, \code{LASA_wave}, and the final
\code{label_report} follow the shared label engine.
}
\examples{
dat <- data.frame(RespNr = 1:2, canxint = c(-2, 12), crmanx = c(3, -2))
apply_lasa226_labels(dat, wave = "C", to_numeric = TRUE)
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
