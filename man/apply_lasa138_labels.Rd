% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA138_labels.R
\name{apply_lasa138_labels}
\alias{apply_lasa138_labels}
\title{Apply LASA138 (Religiousness) SPSS labels}
\usage{
apply_lasa138_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASAE138 \code{.sav} file,
for example via \code{\link[haven:read_spss]{haven::read_sav()}}, containing any of \code{eqreli02}
through \code{eqreli07}.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. LASA138 is documented only for \code{"E"}.}

\item{name_corrections}{Optional named character vector mapping canonical
suffixes without the wave prefix (\code{qreli02} through \code{qreli07}) to actual
column names in \code{data}.}

\item{to_factor}{Logical. If \code{TRUE}, the categorical religiousness items
are converted to factors using the documented response labels.}

\item{to_numeric}{Logical. Retained for the shared parameter contract.
LASA138 contains no numeric count or continuous variables, so it does not
change the documented items.}

\item{standardize_names}{Logical. If \code{TRUE}, matched columns are renamed to
canonical lowercase suffixes without the wave prefix, \code{respnr} is
standardized, and \code{split_wavecode} is treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{TRUE}, matched columns have the wave
prefix removed and a \code{LASA_wave} column containing \code{"E"} is inserted
immediately after the respondent-number column.}
}
\value{
\code{data}, with LASA138 variable/value labels, optional factor
conversion and standardized naming, preserved original SPSS coding, the
\code{LASA_wave} provenance attribute, and a \code{label_report} matching audit.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to the six religiousness items documented in
LASAE138: life after death, heaven, hell, the devil, Adam and Eve, and the
Bible as the word of God.
}
\details{
Every item uses no answer (-1), yes (1), and no (2). The orthodoxy scale
score shown in the source PDF belongs to the separate LASA338 scale-score
file and is intentionally not labelled here.

LASA138 is documented only for wave E and all six variables are
categorical. \code{to_factor = TRUE} converts them to factors; \code{to_numeric} is
retained for the shared parameter contract but does not change them.
Column matching tries an explicit \code{name_corrections} override, an exact
match, then a case-insensitive exact match. Standardized naming optionally
removes the wave prefix, standardizes \code{respnr}, and adds \code{LASA_wave} while
preserving original SPSS labels and values.
}
\examples{
dat <- data.frame(RespNr = 1:2, eqreli02 = c(1, 2), eqreli07 = c(-1, 1))
dat <- apply_lasa138_labels(dat, wave = "E", to_factor = TRUE)
attr(dat$eqreli02, "labels")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
