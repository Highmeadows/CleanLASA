% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA119_labels.R
\name{apply_lasa119_labels}
\alias{apply_lasa119_labels}
\title{Apply LASA119 (Fertility in women) SPSS labels}
\usage{
apply_lasa119_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from LASAB119.}

\item{wave}{Character scalar. Only \code{"B"} is documented and accepted;
matching is case-insensitive.}

\item{name_corrections}{Optional named character vector whose names are
canonical suffixes without the \code{b} prefix, such as \code{qmeno01}, and whose
values are actual column names in \code{data}.}

\item{to_factor}{Logical. Convert matched categorical variables to factors
using documented value labels.}

\item{to_numeric}{Logical. Restore eligible age, count, and duration
variables to plain numeric and replace negative missing codes with \code{NA}.}

\item{standardize_names}{Logical. Standardize matched names and respondent
number, remove the \code{b} wave prefix, and add \code{LASA_wave}.}

\item{split_wavecode}{Logical. Remove the wave prefix from matched names
and add \code{LASA_wave}; implied by \code{standardize_names = TRUE}.}
}
\value{
\code{data} with LASA119 metadata, optional conversion and renaming,
original-coding attributes, \code{LASA_wave}, and a \code{label_report} audit.
}
\description{
Attaches SPSS-style variable and value labels to the 13 fertility and
menopause variables documented in LASAB119. The self-administered module
covers reproductive timing and number of children, menopause, oral
contraceptive use, and five menopause-related symptoms.
}
\details{
Age, count, and duration variables (\code{qmeno01}, \code{qmeno02}, \code{qmeno03},
\code{qmeno05}, and \code{qmeno08}) are numeric and can be restored with
\code{to_numeric = TRUE}; all negative missing codes then become \code{NA}. The
remaining variables are categorical and can be converted with
\code{to_factor = TRUE}. The common missing codes are \code{-3} for no answer,
skipped, \code{-2} for male respondent, and \code{-1} for no answer.

Matching tries \code{name_corrections}, an exact name, then a case-insensitive
exact name. Original labels and values are retained in \code{original_labels}
and \code{original_values} attributes.
}
\examples{
dat <- data.frame(
  RespNr = 1:3,
  BQMENO01 = c(-2, 13, 15),
  bqmeno06 = c(-2, 1, 3)
)
dat <- apply_lasa119_labels(dat, wave = "B", to_numeric = TRUE)
dat$BQMENO01
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
