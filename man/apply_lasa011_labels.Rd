% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA011_labels.R
\name{apply_lasa011_labels}
\alias{apply_lasa011_labels}
\title{Apply LASA011 (Household Composition) SPSS labels}
\usage{
apply_lasa011_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA011 \code{.sav} file,
for example via \code{\link[haven:read_spss]{haven::read_sav()}}.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"B"}, \code{"C"}, \code{"D"}, \code{"E"}, \code{"2B"}, \code{"F"},
\code{"G"}, \code{"H"}, \code{"3B"}, \code{"MB"}, \code{"I"}, \code{"J"}, or \code{"K"}.}

\item{name_corrections}{Optional named character vector for explicit column
name overrides. Names are canonical LASA011 suffixes without the wave
prefix (for example \code{nupers} or \code{hhpart}), and values are the actual
column names in \code{data}, for example \code{c(nupers = "BNUPER")}.}

\item{to_factor}{Logical. If \code{FALSE} (default), categorical variables remain
numeric with SPSS-style value-label attributes. If \code{TRUE}, every variable
not restored to numeric by \code{to_numeric} (see below) is converted to a
factor using its value labels as levels; observed values without a
codebook label are retained as numeric-text levels.}

\item{to_numeric}{Logical. If \code{FALSE} (default), count variables retain
their SPSS-style value-label attributes. If \code{TRUE}, count variables
(\code{nupers}, \code{hhnch}, \code{hhnstch}, \code{hhnsib}, \code{hhnothf}, \code{hhnnonk}) are
converted to ordinary numeric vectors, negative missing-reason codes are
replaced with \code{NA}, and non-negative counts are retained. This takes
precedence over \code{to_factor} for these variables.}

\item{standardize_names}{Logical. If \code{FALSE} (default), original column
names are retained (except for any renaming triggered by
\code{split_wavecode}, see below). If \code{TRUE}, matched LASA011 columns are
renamed to their canonical lowercase documentation names with the wave
code removed, such as \code{nupers}, \code{hhpart}, or \code{hhnnonk}, \code{"respnr"} (in
any capitalization) is renamed to \code{"respnr"}, and \code{split_wavecode} is
always treated as \code{TRUE} as well, regardless of what was passed for
\code{split_wavecode}.}

\item{split_wavecode}{Logical. If \code{FALSE} (default) and
\code{standardize_names = FALSE}, no wave-code splitting occurs. If \code{TRUE},
matched columns are renamed with the wave-letter prefix removed (e.g.
\code{bnupers} becomes \code{nupers}), and a new \code{"LASA_wave"} column, filled with
\code{wave}, is inserted right after \code{"respnr"}. Always treated as \code{TRUE}
when \code{standardize_names = TRUE}.}
}
\value{
\code{data}, with variable and value labels attached to every matched
LASA011 column, optionally transformed and renamed, plus reference
\code{original_labels}/\code{original_values} attributes preserving the original
SPSS value coding regardless of that reshaping. A new \code{"LASA_wave"}
column is added after \code{"respnr"} when \code{standardize_names = TRUE} or
\code{split_wavecode = TRUE}. A variable-name matching audit is attached as
\code{attr(data, "label_report")} and can be retrieved with
\code{\link[=lasa_label_report]{lasa_label_report()}}.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to a LASA011 household-composition data frame, using
the coding scheme documented for the requested wave. By default, variables
remain numeric. Optional arguments allow converting categorical variables
to factors, restoring count variables to plain numeric, correcting
mismatched column names, and standardizing names to the canonical LASA
documentation spelling.
}
\details{
LASA011 changes across waves. Wave B records the total number of persons in
the household; later waves record other household members, excluding the
respondent's partner. From wave F onward, the file also distinguishes the
partner, children, stepchildren, siblings, other family members, and nonkin
household members. \code{wave} is therefore required, especially because B, 2B,
3B, and MB all use the variable-name prefix \code{"b"}.

Column matching tries, in order: (1) an explicit override in
\code{name_corrections}, (2) an exact case-sensitive name match, and (3) a
case-insensitive name match. Unmatched documented variables are left
untouched and recorded as \code{"not found"} in the generic matching audit.

The LASAB211 variables shown in the LASA011 codebook are not processed by
this function because LASAB211 is a separate LASA file code.

Regardless of \code{to_factor}/\code{to_numeric}, every matched column also keeps
its original SPSS value coding available as reference attributes --
\code{attr(x, "original_labels")} and \code{attr(x, "original_values")} -- and the
\code{"respnr"} column (in any capitalization) is matched and, when
\code{standardize_names = TRUE}, renamed to \code{"respnr"}.
}
\examples{
dat_b <- data.frame(BNUPERS = c(1, 2, -2))
dat_b <- apply_lasa011_labels(dat_b, wave = "B")
attr(dat_b$BNUPERS, "label")
attr(dat_b$BNUPERS, "labels")

dat_f <- data.frame(
  FHHPart = c(1, 0, -2),
  FHHNCh = c(2, 0, -2)
)
dat_f <- apply_lasa011_labels(
  dat_f,
  wave = "F",
  to_factor = TRUE,
  to_numeric = TRUE,
  standardize_names = TRUE
)
lasa_label_report(dat_f, problems_only = TRUE)
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}},
\code{\link[=apply_lasa046_labels]{apply_lasa046_labels()}}
}
