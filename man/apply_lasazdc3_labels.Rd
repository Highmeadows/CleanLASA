% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASAzdc3_labels.R
\name{apply_lasazdc3_labels}
\alias{apply_lasazdc3_labels}
\title{Apply LASAzdc3 (Diabetes Mellitus Algorithm) SPSS labels}
\usage{
apply_lasazdc3_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASAzdc3 \code{.sav} file.}

\item{wave}{Character scalar identifying wave \code{"Z"}.}

\item{name_corrections}{Optional named character vector mapping documented
names such as \code{b_DM} or \code{i_alg_DM_ruw} to actual columns.}

\item{to_factor}{Logical. Convert variables carrying value labels to
factors while retaining observed undocumented values.}

\item{to_numeric}{Logical. Request numeric restoration for eligible
variables. LASAzdc3 has no eligible variables.}

\item{standardize_names}{Logical. Standardize matched names and \code{respnr}
and add \code{LASA_wave}.}

\item{split_wavecode}{Logical. Add \code{LASA_wave} and standardize matched names;
implied by \code{standardize_names = TRUE}.}
}
\value{
\code{data} with LASAzdc3 metadata, optional factor conversion and
renaming, the \code{LASA_wave} attribute, and a \code{label_report} matching audit.
}
\description{
Attaches SPSS-style variable and value labels to the three diabetes
mellitus algorithm variables documented for LASAzdc3. The Z-file contains
the 3B-cohort diabetes status (\code{b_DM}), the I-wave status with dropouts
defined (\code{i_DM}), and the unadjusted I-wave algorithm result
(\code{i_alg_DM_ruw}).
}
\details{
LASAzdc3 is a wave-Z file. All three variables are categorical: status
codes distinguish missing, no diabetes, definite diabetes, possible
diabetes, and contradictory information; the two I-wave fields additionally
define code 5 as dropout. Therefore none is eligible for \code{to_numeric}
restoration. \code{to_factor = TRUE} applies the documented category text while
retaining observed undocumented codes. Matching tries corrections, exact
names, and then case-insensitive exact names. Standardized naming lowercases
the documented names, standardizes \code{respnr}, and adds \code{LASA_wave}.
}
\examples{
dat <- data.frame(RespNr = 1:2, b_DM = c(0, 1), i_DM = c(1, 5))
apply_lasazdc3_labels(dat, wave = "Z", to_factor = TRUE)
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
