% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA225_labels.R
\name{apply_lasa225_labels}
\alias{apply_lasa225_labels}
\title{Apply LASA225 (CES-D scale scores) SPSS labels}
\usage{
apply_lasa225_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA225 \code{.sav} file.}

\item{wave}{Character scalar identifying B, C, D, E, 2B, F, G, H, 3B, MB,
I, J, or K.}

\item{name_corrections}{Optional named character vector mapping \code{cesdint}
or \code{rmcesd} to actual columns.}

\item{to_factor}{Logical. Convert labelled categorical variables to factors
unless numeric restoration takes precedence.}

\item{to_numeric}{Logical. Restore the CES-D score to plain numeric and
replace negative values with \code{NA}.}

\item{standardize_names}{Logical. Standardize names and \code{respnr}, remove
wave prefixes, and add \code{LASA_wave}.}

\item{split_wavecode}{Logical. Remove wave prefixes and add \code{LASA_wave};
implied by \code{standardize_names = TRUE}.}
}
\value{
\code{data} with LASA225 metadata, optional conversion and renaming, the
\code{LASA_wave} attribute, and a \code{label_report} audit.
}
\description{
Attaches SPSS-style labels to the CES-D total score and reason-missing
variable documented in LASA225 for waves B, C, D, E, 2B, F, G, H, 3B, MB,
I, J, and K. The score ranges from 0 to 60 with the documented cut-off at
16. Its \code{-2} missing code and the reason categories vary by wave group.
}
\details{
The CES-D total is numeric: \code{to_numeric = TRUE} restores a plain numeric
vector and replaces negative values with \code{NA}. The reason field remains
categorical and can be converted with \code{to_factor = TRUE}. The function
reproduces the B, C/D/E, 2B, F-through-K, and MB branches, including MB's
code 4 for no valid data. Matching, original-label preservation,
standardized names, \code{respnr}, \code{LASA_wave}, and the matching audit use the
shared label engine.
}
\examples{
dat <- data.frame(RespNr = 1:2, fcesdint = c(-2, 24), frmcesd = c(3, -2))
apply_lasa225_labels(dat, wave = "F", to_numeric = TRUE)
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
