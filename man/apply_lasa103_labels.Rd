% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA103_labels.R
\name{apply_lasa103_labels}
\alias{apply_lasa103_labels}
\title{Apply LASA103 (Singing and making music) SPSS labels}
\usage{
apply_lasa103_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA103 \code{.sav} file,
for example via \code{\link[haven:read_spss]{haven::read_sav()}}. Depending on \code{wave}, names include
\code{hqmus1}, \code{iqsmus1}, \code{jqmussl}, \code{jqmussl_y1}, and \code{jqmuslc}.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"H"}, \code{"I"}, or \code{"J"}.}

\item{name_corrections}{Optional named character vector mapping canonical
LASA103 suffixes without the wave prefix (for example \code{qmus1}, \code{qsmus1},
\code{qmussl}, or \code{qmussl_y1}) to actual column names in \code{data}.}

\item{to_factor}{Logical. If \code{TRUE}, categorical variables with documented
value labels are converted to factors. Observed unlabelled codes remain
numeric-text levels.}

\item{to_numeric}{Logical. If \code{TRUE}, documented counts, frequencies,
durations, ages, years, and other numeric fields are restored to plain
numeric and negative values are replaced by \code{NA}.}

\item{standardize_names}{Logical. If \code{TRUE}, matched columns are renamed to
canonical lowercase suffixes without the wave prefix, \code{respnr} is
standardized, and \code{split_wavecode} is treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{TRUE}, matched columns have the wave
prefix removed and a \code{LASA_wave} column containing the selected wave is
inserted immediately after the respondent-number column.}
}
\value{
\code{data}, with LASA103 variable/value labels, optional conversion and
standardized naming, preserved original SPSS coding, the \code{LASA_wave}
provenance attribute, and a \code{label_report} matching audit.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to the singing-and-music variables documented in
LASA103 for waves H, I, and J.
}
\details{
Waves H and I share a 12-variable self-administered-questionnaire schema:
making music, instrument type, singing, frequency and duration, six music
genres, and an open specification for another genre. Wave J uses a
29-variable schema covering music or singing lessons for up to six
instruments (including voice), current music-making, whether this is done
alone or with others, music listening, and concert attendance.

Frequencies, durations, ages, years, counts, and variables whose only
documented codes are negative missing-reason codes are eligible for
\code{to_numeric}. With \code{to_numeric = TRUE}, these fields are restored to plain
numeric and observed negative values become \code{NA}. Free-text instrument and
genre-specification fields are not converted. With \code{to_factor = TRUE},
categorical variables become factors and observed unlabelled codes remain
numeric-text levels. \code{to_numeric} takes precedence for eligible fields.

Column matching tries, in order: (1) an explicit override in
\code{name_corrections}, (2) an exact case-sensitive match, and (3) a
case-insensitive exact match. Variables documented for the selected wave
but absent from \code{data} are recorded as \code{"not found"} in the matching
audit.

The shared reshaping arguments preserve each matched column's original
SPSS coding in \code{original_labels} and \code{original_values}. When requested,
matched names are standardized by removing the wave prefix, \code{respnr} is
standardized, and a \code{LASA_wave} column is inserted.
}
\examples{
dat <- data.frame(RespNr = 1:2, hqmus1 = c(1, 2), hqmus4 = c(-2, 5))
dat <- apply_lasa103_labels(dat, wave = "H", to_numeric = TRUE)
attr(dat$hqmus1, "labels")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
