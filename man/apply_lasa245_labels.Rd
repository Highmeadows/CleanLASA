% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA245_labels.R
\name{apply_lasa245_labels}
\alias{apply_lasa245_labels}
\title{Apply LASA245 (Medical Specialists) SPSS labels}
\usage{
apply_lasa245_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA245 \code{.sav} file.
Expected names include \code{bimspec#}, \code{cimspec01}, and \code{himspec36}.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"B"}, \code{"C"}, \code{"D"}, \code{"E"}, \code{"2B"}, \code{"F"},
\code{"G"}, \code{"H"}, \code{"3B"}, or \code{"I"}.}

\item{name_corrections}{Optional named character vector mapping canonical
suffixes without the wave prefix (for example \code{imspec01} or \code{mspec31})
to actual column names in \code{data}.}

\item{to_factor}{Logical. If \code{TRUE}, matched specialist-type indicators are
converted to factors. Observed unlabelled values remain numeric-text
levels.}

\item{to_numeric}{Logical. If \code{TRUE}, the specialist count is converted to
plain numeric and its negative missing/routing codes become \code{NA}.}

\item{standardize_names}{Logical. If \code{TRUE}, matched names and \code{respnr} are
standardized, wave prefixes are removed, and \code{LASA_wave} is added.}

\item{split_wavecode}{Logical. If \code{TRUE}, matched columns have the wave
prefix removed and \code{LASA_wave} is added. Implied by
\code{standardize_names = TRUE}.}
}
\value{
\code{data} with LASA245 metadata, requested conversions and renaming,
the \code{LASA_wave} provenance attribute, and a \code{label_report} matching audit.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to the medical-specialist variables documented as
LASA245 in the LASA045 hospitalization codebook. LASA245 specifies the
number and types of medical specialists contacted during the past six
months.
}
\details{
The available files cover waves B, C, D, E, 2B, F, G, H, 3B, and I. All
waves contain the specialist count and types 01--30. Types 31--32 occur in
B through G but not H, 3B, or I; types 33--34 start in D; type 35 is a
tropical doctor in 2B and a geneticist in F through I; and type 36 occurs
only in H, 3B, and I. The source marks MB245 and J245 as not yet available,
so those waves are not accepted here.

The specialist count is numeric and has only documented negative
missing/routing codes. With \code{to_numeric = TRUE}, it becomes plain numeric
and negative values become \code{NA}. Specialist-type indicators are
categorical (\verb{not reported}/\code{reported}) and can be converted to factors.
Original SPSS values and labels are preserved in \code{original_values} and
\code{original_labels}.

Column matching uses an explicit \code{name_corrections} override, an exact
case-sensitive match, and then a case-insensitive exact match. Standardized
naming removes the wave prefix, standardizes \code{respnr}, and inserts a
\code{LASA_wave} column.
}
\examples{
dat <- data.frame(
  RespNr = 1:2,
  `bimspec#` = c(-2, 2),
  bimspec01 = c(0, 1),
  check.names = FALSE
)
dat <- apply_lasa245_labels(dat, wave = "B", to_numeric = TRUE)
attr(dat$bimspec01, "labels")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}},
\code{\link[=apply_lasa045_labels]{apply_lasa045_labels()}}
}
