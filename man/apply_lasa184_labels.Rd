% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA184_labels.R
\name{apply_lasa184_labels}
\alias{apply_lasa184_labels}
\title{Apply LASA184 (Diet) SPSS labels}
\usage{
apply_lasa184_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA184 \code{.sav} file.}

\item{wave}{Character scalar identifying wave \code{"C"} or \code{"D"}; matching is
case-insensitive.}

\item{name_corrections}{Optional named character vector mapping canonical
suffixes without the wave prefix (for example \code{mdiet}) to actual columns.}

\item{to_factor}{Logical. Convert labelled variables to factors.}

\item{to_numeric}{Logical. Convert weekly milk-product counts to plain
numeric and replace negative missing codes with \code{NA}.}

\item{standardize_names}{Logical. Standardize matched names and respondent
number, remove wave prefixes, and add \code{LASA_wave}.}

\item{split_wavecode}{Logical. Remove wave prefixes and add \code{LASA_wave};
implied by \code{standardize_names = TRUE}.}
}
\value{
\code{data} with LASA184 metadata, optional conversion and renaming, a
\code{LASA_wave} attribute, and a \code{label_report} matching audit.
}
\description{
Attaches SPSS-style variable and value labels to the diet and milk-product
variables documented for LASA184. The file is documented for waves C and
D. Both waves contain the diet indicator and five diet-type indicators;
wave C additionally contains high-calorie and other-diet indicators, five
weekly milk-product frequencies, and milk-consumption histories for three
age periods.
}
\details{
Weekly milk-product frequencies are numeric counts. With
\code{to_numeric = TRUE}, their negative missing codes become \code{NA}; the other
variables remain categorical. With \code{to_factor = TRUE}, labelled variables
become factors and observed unlabelled values are retained as numeric-text
levels. Original values and labels remain available in \code{original_values}
and \code{original_labels}.

Matching tries an explicit \code{name_corrections} override, an exact match,
then a case-insensitive exact match. Optional standardized naming removes
the wave prefix, standardizes \code{respnr}, and adds \code{LASA_wave}.
}
\examples{
dat <- data.frame(cmdiet = c(-1, 1, 2), cmmilkmlk = c(-1, 0, 7))
apply_lasa184_labels(dat, wave = "C", to_numeric = TRUE)
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
