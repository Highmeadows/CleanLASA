% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA134_labels.R
\name{apply_lasa134_labels}
\alias{apply_lasa134_labels}
\title{Apply LASA134 (Birth weight) SPSS labels}
\usage{
apply_lasa134_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA134 \code{.sav} file.
Names are \code{eqbwght}/\code{eqbwghts} in E and \code{bqbwght}/\code{bqbwghts} in 2B and
3B; \code{bqbwghtw} occurs only in 3B.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"E"}, \code{"2B"}, or \code{"3B"}.}

\item{name_corrections}{Optional named character vector mapping suffixes
without the wave prefix (for example \code{qbwght} or \code{qbwghtw}) to actual
names in \code{data}.}

\item{to_factor}{Logical. If \code{TRUE}, categorical source variables are
converted to factors using the documented value labels.}

\item{to_numeric}{Logical. If \code{TRUE}, birth weight is restored to plain
numeric and negative values become \code{NA}.}

\item{standardize_names}{Logical. If \code{TRUE}, matched names and \code{respnr} are
standardized and \code{split_wavecode} is treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{TRUE}, matched names lose the wave prefix
and a \code{LASA_wave} column is inserted after \code{respnr}.}
}
\value{
\code{data}, with LASA134 labels, requested conversion or renaming,
preserved original coding, \code{LASA_wave} provenance, and a \code{label_report}.
}
\description{
Attaches SPSS-style variable and value labels to birth-weight variables
documented in LASA134 for waves E, 2B, and 3B.
}
\details{
All three waves contain birth weight in grams and a source-quality item.
Wave 3B additionally records which document supplied the birth weight.
The weight variable is numeric and eligible for \code{to_numeric}; a negative
missing code becomes \code{NA}. Source variables are categorical and can be
converted with \code{to_factor}.

Matching tries \code{name_corrections}, an exact case-sensitive match, and then
a case-insensitive exact match. Both replenishment waves use the documented
\code{b} variable prefix, while the \code{LASA_wave} provenance distinguishes \verb{2B}
from \verb{3B}. Original SPSS coding is preserved.
}
\examples{
dat <- data.frame(eqbwght = c(-1, 3500), eqbwghts = c(1, 3))
dat <- apply_lasa134_labels(dat, wave = "E", to_numeric = TRUE)
attr(dat$eqbwghts, "labels")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
