% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA356_labels.R
\name{apply_lasa356_labels}
\alias{apply_lasa356_labels}
\title{Apply LASA356 (15-Word Test Constructed Scores) SPSS labels}
\usage{
apply_lasa356_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA356 \code{.sav} file.
Variables use their documented wave prefix followed by suffixes such as
\code{mtotal}, \code{mtmax}, \code{mdelta}, \code{mret1pc}, and \code{mret2pc}.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"B"}, \code{"C"}, \code{"D"}, \code{"E"}, \code{"2B"},
\code{"F"}, \code{"G"}, \code{"H"}, \code{"3B"}, \code{"I"}, \code{"J"}, or \code{"K"}.}

\item{name_corrections}{Optional named character vector mapping canonical
LASA356 suffixes without the wave prefix to actual column names in
\code{data}.}

\item{to_factor}{Logical. If \code{TRUE}, variables carrying documented value
labels are converted to factors. Observed unlabelled scores remain
numeric-text levels.}

\item{to_numeric}{Logical. If \code{TRUE}, all LASA356 variables are restored to
plain numeric and negative values are replaced by \code{NA}.}

\item{standardize_names}{Logical. If \code{TRUE}, matched columns are renamed to
canonical lowercase suffixes without the wave prefix, \code{respnr} is
standardized, and \code{split_wavecode} is treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{TRUE}, matched columns have the wave
prefix removed and a \code{LASA_wave} column containing the selected wave is
inserted immediately after the respondent-number column.}
}
\value{
\code{data}, with LASA356 variable/value labels, optional conversion and
standardized naming, preserved original SPSS coding, the \code{LASA_wave}
provenance attribute, and a \code{label_report} matching audit.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to the constructed 15-word-test variables documented
in LASA356 for waves B through K, including replenishment waves 2B and 3B.
}
\details{
Wave B contains trial, delayed-recall, total, maximum, learning-effect,
retention, and recall-interval variables. Wave C contains the same
constructed cognitive scores except the recall interval. From wave D
onward, LASA356 contains five derived scores: total, maximum, learning
effect, and two retention percentages. The much larger set of test
administration and item variables in the same source belongs to LASA156
and is intentionally outside this function's inventory.

The source prints the second B/C retention variable as \code{mret1pc} a second
time. This function uses \code{mret2pc}, consistent with its documented
retention-2 formula and with the explicit \code{mret1pc}/\code{mret2pc} distinction
in every later wave. Wave-specific missing codes and variable-label
capitalization are otherwise reproduced from the source.

All LASA356 variables are numeric. With \code{to_numeric = TRUE}, documented
negative codes become \code{NA}; \code{to_numeric} therefore takes precedence over
\code{to_factor}. With \code{to_factor = TRUE} alone, documented missing codes become
factor levels and observed unlabelled scores remain numeric-text levels.

Column matching tries, in order: (1) an explicit override in
\code{name_corrections}, (2) an exact case-sensitive match, and (3) a
case-insensitive exact match. Variables absent from \code{data} are recorded as
\code{"not found"} in the matching audit. Original labels and values are
preserved, and optional standardized naming removes the wave prefix,
standardizes \code{respnr}, and adds \code{LASA_wave}.
}
\examples{
dat <- data.frame(RespNr = 1:2, dmtotal = c(-2, 34), dmret1pc = c(-1, 75))
dat <- apply_lasa356_labels(dat, wave = "D", to_numeric = TRUE)
attr(dat$dmtotal, "label")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
