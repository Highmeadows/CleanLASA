% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA124_labels.R
\name{apply_lasa124_labels}
\alias{apply_lasa124_labels}
\title{Apply LASA124 (Metamemory) SPSS labels}
\usage{
apply_lasa124_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA124 \code{.sav} file.
Expected names include \code{bqmemo01}, \code{cqmemo31}, and \code{dqmemo41}.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"B"}, \code{"C"}, or \code{"D"}.}

\item{name_corrections}{Optional named character vector mapping canonical
suffixes without the wave prefix (for example \code{qmemo01} or \code{qmemo39}) to
actual column names in \code{data}.}

\item{to_factor}{Logical. If \code{TRUE}, matched variables are converted to
factors using the documented value labels.}

\item{to_numeric}{Logical. Accepted for interface consistency; LASA124 has
no numeric variables eligible for conversion.}

\item{standardize_names}{Logical. If \code{TRUE}, matched names are standardized,
\code{respnr} is standardized, and \code{split_wavecode} is treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{TRUE}, matched columns have the wave
prefix removed and a \code{LASA_wave} column is inserted after \code{respnr}.}
}
\value{
\code{data}, with LASA124 labels, requested conversion or renaming,
preserved original coding, a \code{LASA_wave} provenance attribute, and a
\code{label_report} matching audit.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to the metamemory items documented in LASA124 for
waves B, C, and D.
}
\details{
Wave B contains 30 items. Waves C and D contain the same 30 items plus 11
additional items, for 41 variables in total. Items 1--10 measure memory
strategies on a never--always scale; items 11 onward use a
strongly-disagree--strongly-agree scale. Scale scores stored in the
separate LASA324 files are intentionally outside this function's scope.

All LASA124 variables are categorical. Consequently, \code{to_numeric} is
accepted for interface consistency but does not convert them. With
\code{to_factor = TRUE}, matched variables become factors and observed
unlabelled codes remain numeric-text levels.

Column matching tries an explicit \code{name_corrections} override, an exact
case-sensitive match, and then a case-insensitive exact match. Every
documented variable is recorded in the final \code{label_report}, including
variables absent from \code{data}.

Original SPSS codes and labels are preserved in \code{original_values} and
\code{original_labels}. Standardized naming removes the wave prefix,
standardizes \code{respnr}, and inserts a \code{LASA_wave} column.
}
\examples{
dat <- data.frame(bqmemo01 = c(1, 5), bqmemo11 = c(2, 4))
dat <- apply_lasa124_labels(dat, wave = "B", to_factor = TRUE)
attr(dat$bqmemo01, "labels")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
