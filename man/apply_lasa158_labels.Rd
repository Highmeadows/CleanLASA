% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA158_labels.R
\name{apply_lasa158_labels}
\alias{apply_lasa158_labels}
\title{Apply LASA158 (Peak flow test) SPSS labels}
\usage{
apply_lasa158_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA158 \code{.sav} file.}

\item{wave}{Character scalar: \code{"B"}, \code{"C"}, \code{"D"}, \code{"E"}, \code{"G"}, or
\code{"H"}; matching is case-insensitive.}

\item{name_corrections}{Optional named character vector mapping canonical
suffixes to actual column names.}

\item{to_factor}{Logical. Convert labelled categorical variables to factors.}

\item{to_numeric}{Logical. Convert eligible peak-flow trials and scores to
plain numeric and replace negative missing codes with \code{NA}.}

\item{standardize_names}{Logical. Standardize matched names and \code{respnr},
remove wave prefixes, and add \code{LASA_wave}.}

\item{split_wavecode}{Logical. Remove wave prefixes and add \code{LASA_wave};
implied by \code{standardize_names = TRUE}.}
}
\value{
\code{data} with LASA158 metadata, optional reshaping, the \code{LASA_wave}
attribute, and a \code{label_report} audit.
}
\description{
Applies the variable labels, response labels, and wave-specific
missing-value codes documented for the LASA158 peak-flow test. LASA158 is
available in waves B, C, D, E, G, and H. The trial, completion, reason,
understanding, cooperation, position, and maximum-score inventories differ
across the B, C, and later-wave forms.
}
\details{
Matching tries \code{name_corrections}, an exact match, then a case-insensitive
exact match. \code{to_factor} converts categorical variables to factors and
retains observed undocumented values. \code{to_numeric} restores trial and
score variables to plain numeric and replaces documented negative missing
codes with \code{NA}. Original values and labels are preserved as attributes.
}
\examples{
dat <- data.frame(bmpeakf1 = c(-2, 250), bmpeakfp = c(-1, 1))
apply_lasa158_labels(dat, wave = "B")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
