% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA117_labels.R
\name{apply_lasa117_labels}
\alias{apply_lasa117_labels}
\title{Apply LASA117 (Sleep habits) SPSS labels}
\usage{
apply_lasa117_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA117 \code{.sav} file.}

\item{wave}{Character scalar identifying \code{"B"}, \code{"C"}, \code{"D"}, \code{"E"},
\code{"2B"}, \code{"F"}, \code{"G"}, \code{"H"}, \code{"3B"}, \code{"I"}, \code{"J"}, or \code{"K"}.}

\item{name_corrections}{Optional named character vector whose names are
canonical suffixes without a wave prefix, such as \code{qsleep1} or
\code{qsleeph}, and whose values are actual column names.}

\item{to_factor}{Logical. Convert matched value-labelled variables to
factors using documented labels.}

\item{to_numeric}{Logical. Restore documented numeric variables to plain
numeric and replace negative missing-value codes with \code{NA}.}

\item{standardize_names}{Logical. Standardize matched names and respondent
number, remove wave prefixes, and add \code{LASA_wave}.}

\item{split_wavecode}{Logical. Remove wave prefixes from matched names and
add \code{LASA_wave}; implied by \code{standardize_names = TRUE}.}
}
\value{
\code{data} with LASA117 metadata, optional conversion and renaming,
original-coding attributes, \code{LASA_wave}, and a \code{label_report} audit.
}
\description{
Attaches SPSS-style variable and value labels to the sleep-habit variables
documented for LASA117. Waves B, C, D, E, 2B, and F contain four items:
hours slept and three sleep-problem frequency items. Waves G, H, 3B, I, J,
and K additionally contain bedtime hour and minute plus a fifth item on
sleep quality.
}
\details{
Hours slept and bedtime hour/minute are numeric variables. Their documented
missing-value maps vary by wave: bedtime hour and minute have no value map
in G; hour has \code{-1} in H, 3B, I, J, and K; minute has \code{-1} in H, 3B, I,
and J, while the PDF documents no value for K. With \code{to_numeric = TRUE},
these numeric variables become plain numeric and negative codes become
\code{NA}. Sleep-problem and sleep-quality items are categorical and can be
converted with \code{to_factor = TRUE}.

Matching tries \code{name_corrections}, an exact match, then a case-insensitive
exact match. Original labels and values are retained in \code{original_labels}
and \code{original_values}. The sleeping-problems scale score belongs to
LASA317 and is not included here.
}
\examples{
dat <- data.frame(
  RespNr = 1:3,
  GQSLEEP1 = c(-1, 6, 8),
  gqsleep2 = c(1, 2, 4)
)
dat <- apply_lasa117_labels(dat, wave = "G", to_numeric = TRUE)
dat$GQSLEEP1
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
