% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA171_labels.R
\name{apply_lasa171_labels}
\alias{apply_lasa171_labels}
\title{Apply LASA171 (calf measurement) SPSS labels}
\usage{
apply_lasa171_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA171 \code{.sav} file.
Names use the wave-specific \code{b}, \code{d}, \code{e}, \code{f}, \code{g}, or \code{h} prefix; wave
2B and 3B both use \code{b}.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"B"}, \code{"D"}, \code{"E"}, \code{"2B"}, \code{"F"},
\code{"G"}, \code{"H"}, or \code{"3B"}.}

\item{name_corrections}{Optional named character vector mapping canonical
suffixes without the wave prefix (for example, \code{mcalf01}) to actual
column names in \code{data}.}

\item{to_factor}{Logical. If \code{TRUE}, variables with documented value labels
are converted to factors unless they are converted to numeric.}

\item{to_numeric}{Logical. If \code{TRUE}, documented calf measurements are
restored to plain numeric and negative values are replaced by \code{NA}.}

\item{standardize_names}{Logical. If \code{TRUE}, matched columns are renamed to
canonical lowercase suffixes without the wave prefix, \code{respnr} is
standardized, and \code{split_wavecode} is treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{TRUE}, matched columns have the wave
prefix removed and a \code{LASA_wave} column is inserted after \code{respnr}.}
}
\value{
\code{data}, with LASA171 labels, optional conversion and standardized
naming, preserved original SPSS coding, the \code{LASA_wave} provenance
attribute, and a \code{label_report} matching audit.
}
\description{
Attaches SPSS-style variable and value labels to the calf-circumference
variables documented in LASA171 for waves B, D, E, 2B, F, G, H, and 3B.
The inventory and coding changed materially across waves: wave B used a
single measurement plus detailed remarks; D/E and G used duplicate
measurements and separate particularity indicators; 2B used one
measurement; F used a single combined particularities variable; and H/3B
used one measurement with reason-missing status. From wave I onward calf
measurement is documented in LASA161 instead.
}
\details{
Calf measurements are eligible for \code{to_numeric = TRUE}; documented
negative missing codes then become \code{NA}. Status and particularity variables
remain categorical. With \code{to_factor = TRUE}, their documented codes become
factor levels while observed unlabelled codes are retained.

Matching tries \code{name_corrections}, an exact match, then a case-insensitive
exact match. Original labels and values are preserved. Optional
standardized naming removes the wave prefix, standardizes \code{respnr}, and
adds \code{LASA_wave}.
}
\examples{
dat <- data.frame(RespNr = 1:2, hmcalf01 = c(-1, 35), hmcalf02 = c(0, 1))
apply_lasa171_labels(dat, wave = "H", to_numeric = TRUE)
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
