% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA185_labels.R
\name{apply_lasa185_labels}
\alias{apply_lasa185_labels}
\title{Apply LASA185 (Foot problems) SPSS labels}
\usage{
apply_lasa185_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA185 \code{.sav} file.}

\item{wave}{Character scalar identifying wave \code{"C"}, \code{"D"}, \code{"E"}, or
\code{"F"}; matching is case-insensitive.}

\item{name_corrections}{Optional named character vector mapping suffixes
without the wave prefix (for example \code{mfeetinsp}) to actual columns.}

\item{to_factor}{Logical. Convert labelled categorical variables to factors.}

\item{to_numeric}{Logical. Restore ankle, knee, and pulse widths to plain
numeric and replace negative missing codes with \code{NA}.}

\item{standardize_names}{Logical. Standardize matched names and respondent
number, remove wave prefixes, and add \code{LASA_wave}.}

\item{split_wavecode}{Logical. Remove wave prefixes and add \code{LASA_wave};
implied by \code{standardize_names = TRUE}.}
}
\value{
\code{data} with LASA185 metadata, optional conversion and renaming, a
\code{LASA_wave} attribute, and a \code{label_report} matching audit.
}
\description{
Attaches SPSS-style variable and value labels to the foot-inspection
variables documented for LASA185 in waves C, D, E, and F. Waves C and D
contain the full inspection battery: whole-foot inspection, eight finding
indicators, an other-specification field, pull-up toes, and smack feet.
Wave C additionally contains two ankle, two knee, and two pulse-width
measurements that the codebook notes were processed in LASAC185 although
their questionnaire source was LASAC161. Waves E and F contain only the
pull-up-toes item.
}
\details{
The six wave-C width measurements are numeric. With \code{to_numeric = TRUE},
their negative missing codes become \code{NA}. \code{to_factor = TRUE} converts
labelled categorical variables to factors and retains unlabelled observed
codes. Original coding is preserved in \code{original_values} and
\code{original_labels}.

Matching tries a manual \code{name_corrections} override, an exact match, then a
case-insensitive exact match. Standardized naming removes the wave prefix,
standardizes \code{respnr}, and adds \code{LASA_wave}.
}
\examples{
dat <- data.frame(cmfeetinsp = c(-1, 1, 2), cmankle1 = c(-1, 60, 72))
apply_lasa185_labels(dat, wave = "C", to_numeric = TRUE)
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
