% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA036_labels.R
\name{apply_lasa036_labels}
\alias{apply_lasa036_labels}
\title{Apply LASA036 (Self-perceived health) SPSS labels}
\usage{
apply_lasa036_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA036 \code{.sav} file,
for example via \code{\link[haven:read_spss]{haven::read_sav()}}. Names include \code{bsubhea1},
\code{bsubhea3}, and \code{ksubhea4}, depending on \code{wave}.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"B"}, \code{"C"}, \code{"D"}, \code{"E"}, \code{"2B"}, \code{"F"},
\code{"G"}, \code{"H"}, \code{"3B"}, \code{"I"}, \code{"J"}, or \code{"K"}.}

\item{name_corrections}{Optional named character vector for explicit column
name overrides. Names are canonical LASA036 suffixes without the wave
prefix (\code{subhea1}, \code{subhea2}, \code{subhea3}, or \code{subhea4}), and values are
actual column names in \code{data}, for example
\code{c(subhea1 = "current_self_rated_health")}.}

\item{to_factor}{Logical. If \code{FALSE} (default), variables remain numeric
with SPSS-style value-label attributes. If \code{TRUE}, they are converted to
factors using the documented labels. An observed value without a label
remains a numeric-text factor level.}

\item{to_numeric}{Logical. Accepted for interface consistency. LASA036 has
no numeric variables eligible for conversion, so this argument does not
alter matched columns.}

\item{standardize_names}{Logical. If \code{FALSE} (default), source column names
are retained except for renaming requested through \code{split_wavecode}. If
\code{TRUE}, matched LASA036 columns are renamed to their canonical lowercase
suffixes with the wave code removed; \code{"respnr"} is standardized; and
\code{split_wavecode} is always treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{FALSE} (default) and
\code{standardize_names = FALSE}, no wave-code splitting occurs. If \code{TRUE},
matched columns are renamed with their wave prefix removed and a new
\code{"LASA_wave"} column filled with \code{wave} is inserted immediately after
the respondent-number column. It is always treated as \code{TRUE} when
\code{standardize_names = TRUE}.}
}
\value{
\code{data}, with variable/value-label attributes attached to every
matched LASA036 column, optional factor conversion and canonical
renaming, and \code{original_labels}/\code{original_values} attributes preserving
the original SPSS coding. A \code{"LASA_wave"} column is added when requested.
The generic name-matching audit is attached as \code{attr(data, "label_report")} and can be retrieved with \code{\link[=lasa_label_report]{lasa_label_report()}}.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to the two or three self-perceived-health variables
documented in LASA036. Twelve waves are supported; the PDF does not
document a wave-MB file.
}
\details{
Every supported wave contains current self-perceived health and health
compared with age peers. Wave 2B additionally asks for a comparison with
ten years ago. Waves F through K instead contain a retrospective health
rating for three (or four) years ago. The age-peer category at code 3 is
printed as \code{"do not know/just as good"} through wave 3B and as
\code{"do not know/just as good as other"} from wave F onward; this wording
difference is retained.

All LASA036 variables are categorical. Consequently, \code{to_numeric} is
accepted for consistency with the shared apply-labels interface but does
not convert these variables. With \code{to_factor = TRUE}, matched variables
become factors and observed unlabelled codes remain numeric-text levels.

Column matching tries, in order: (1) an explicit override in
\code{name_corrections}, (2) an exact case-sensitive name match, and (3) a
case-insensitive exact match. Variables documented for the selected wave
but absent from \code{data} are left untouched and recorded as \code{"not found"} in
the generic matching audit.

\code{name_corrections}, \code{to_factor}, \code{to_numeric}, \code{standardize_names}, and
\code{split_wavecode} are part of the parameter contract shared by every
\verb{apply_*_labels()} function in this package. Regardless of conversion,
every matched column keeps its original SPSS coding in
\code{attr(x, "original_labels")} and \code{attr(x, "original_values")}. The
\code{"respnr"} column is matched in any capitalization and, when
\code{standardize_names = TRUE}, renamed to \code{"respnr"}.
}
\examples{
dat <- data.frame(
  RespNr = 1:3,
  fsubhea1 = c(-5, 1, 5),
  fsubhea2 = c(-1, 2, 4),
  fsubhea4 = c(-1, 3, 5)
)
dat <- apply_lasa036_labels(dat, wave = "F")
attr(dat$fsubhea2, "labels")
}
\seealso{
\code{\link[=apply_lasa035_labels]{apply_lasa035_labels()}}, \code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
