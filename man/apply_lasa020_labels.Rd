% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA020_labels.R
\name{apply_lasa020_labels}
\alias{apply_lasa020_labels}
\title{Apply LASA020 (Digit Span) SPSS labels}
\usage{
apply_lasa020_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LAS3B020 \code{.sav} file,
for example via \code{\link[haven:read_spss]{haven::read_sav()}}. Expected names are \code{bDSf01} through
\code{bDSf08} and \code{bDSb01} through \code{bDSb07}.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. LASA020 is documented only for \code{"3B"}.}

\item{name_corrections}{Optional named character vector for explicit column
name overrides. Names are canonical LASA020 suffixes without the \code{b}
wave prefix (for example \code{DSf01} or \code{DSb07}), and values are actual
column names in \code{data}, for example \code{c(DSf01 = "forward_span_2")}.}

\item{to_factor}{Logical. If \code{FALSE} (default), matched items remain
numeric with SPSS-style value-label attributes. If \code{TRUE}, they are
converted to factors using the documented correctness and missing-value
labels. An observed unlabelled code is retained as a numeric-text level.}

\item{to_numeric}{Logical. If \code{FALSE} (default), matched items retain their
SPSS-style labels. If \code{TRUE}, they are restored to ordinary numeric,
negative missing-reason codes become \code{NA}, and non-negative scores are
retained. This takes precedence over \code{to_factor}.}

\item{standardize_names}{Logical. If \code{FALSE} (default), source column names
are retained except for renaming requested through \code{split_wavecode}. If
\code{TRUE}, matched LASA020 columns are renamed to their canonical lowercase
suffixes with the wave code removed; \code{"respnr"} is standardized; and
\code{split_wavecode} is always treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{FALSE} (default) and
\code{standardize_names = FALSE}, no wave-code splitting occurs. If \code{TRUE},
matched columns are renamed with their \code{b} prefix removed and a new
\code{"LASA_wave"} column filled with \code{"3B"} is inserted immediately after
the respondent-number column. It is always treated as \code{TRUE} when
\code{standardize_names = TRUE}.}
}
\value{
\code{data}, with variable/value-label attributes attached to every
matched LASA020 column, optional factor/numeric conversion and canonical
renaming, and \code{original_labels}/\code{original_values} attributes preserving
the original SPSS coding. A \code{"LASA_wave"} column is added when requested.
The generic name-matching audit is attached as \code{attr(data, "label_report")} and can be retrieved with \code{\link[=lasa_label_report]{lasa_label_report()}}.
}
\description{
Attaches the SPSS-style variable labels (\code{attr(x, "label")}) and value
labels (\code{attr(x, "labels")}) documented for the 15 separate Digit Span
item scores in LAS3B020. The file contains eight forwards items, covering
spans of two through nine numbers, and seven backwards items, covering
spans of two through eight numbers. Each item records the number of
correct answers across attempts 1 and 2.
}
\details{
LASA020 is documented only for wave 3B. By default, matched variables
remain numeric with codes -3 through 2 and their SPSS labels attached.
With \code{to_factor = TRUE}, they become factors using the documented labels.
With \code{to_numeric = TRUE}, they become plain numeric counts: negative
missing-reason codes become \code{NA}, while zero, one, two, and any observed
non-negative unlabelled value are retained. \code{to_numeric} takes precedence
over \code{to_factor} for every Digit Span item.

The same PDF also documents LAS3B220, which contains total scores and the
reason-missing variable. Those variables are intentionally outside
LASA020 and are not changed by this function; they belong to
\code{apply_lasa220_labels()}.

Column matching tries, in order: (1) an explicit override in
\code{name_corrections}, (2) an exact case-sensitive name match, and (3) a
case-insensitive exact match. Variables documented for LAS3B020 but absent
from \code{data} are left untouched and recorded as \code{"not found"} in the
generic matching audit.

\code{name_corrections}, \code{to_factor}, \code{to_numeric}, \code{standardize_names}, and
\code{split_wavecode} are part of the parameter contract shared by every
\verb{apply_*_labels()} function in this package. Regardless of conversion,
every matched column keeps its original SPSS value coding in
\code{attr(x, "original_labels")} and \code{attr(x, "original_values")}. The
\code{"respnr"} column is matched in any capitalization and, when
\code{standardize_names = TRUE}, renamed to \code{"respnr"}.
}
\examples{
dat <- data.frame(
  RespNr = 1:4,
  bDSf01 = c(-3, 0, 1, 2),
  bDSb01 = c(-2, -1, 1, 2)
)
dat <- apply_lasa020_labels(dat, wave = "3B")
attr(dat$bDSf01, "label")
attr(dat$bDSf01, "labels")

numeric_dat <- apply_lasa020_labels(
  dat, wave = "3B", to_numeric = TRUE,
  standardize_names = TRUE
)
numeric_dat$dsf01
}
\seealso{
\code{\link[=apply_lasa019_labels]{apply_lasa019_labels()}}, \code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
