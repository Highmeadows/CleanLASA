% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA038_labels.R
\name{apply_lasa038_labels}
\alias{apply_lasa038_labels}
\title{Apply LASA038 (Religious beliefs and philosophy) SPSS labels}
\usage{
apply_lasa038_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA038 \code{.sav} file,
for example via \code{\link[haven:read_spss]{haven::read_sav()}}. Depending on \code{wave}, names include
\code{crelig01}, \code{drelig1a}, \code{brelig08}, and \code{krelig8a}.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"C"}, \code{"D"}, \code{"E"}, \code{"2B"}, \code{"F"}, \code{"G"},
\code{"H"}, \code{"3B"}, \code{"MB"}, \code{"I"}, \code{"J"}, or \code{"K"}.}

\item{name_corrections}{Optional named character vector for explicit column
name overrides. Names are canonical LASA038 suffixes without the wave
prefix (for example \code{relig01}, \code{relig1a}, \code{relig8a}, or \code{rmgl}), and
values are actual column names in \code{data}.}

\item{to_factor}{Logical. If \code{FALSE} (default), variables remain numeric
with SPSS-style value-label attributes. If \code{TRUE}, they are converted to
factors using the documented labels. An observed value without a label
remains a numeric-text factor level.}

\item{to_numeric}{Logical. Accepted for interface consistency. LASA038 has
no numeric variables eligible for conversion, so this argument does not
alter matched columns.}

\item{standardize_names}{Logical. If \code{FALSE} (default), source column names
are retained except for renaming requested through \code{split_wavecode}. If
\code{TRUE}, matched LASA038 columns are renamed to their canonical lowercase
suffixes with the wave code removed; \code{"respnr"} is standardized; and
\code{split_wavecode} is always treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{FALSE} (default) and
\code{standardize_names = FALSE}, no wave-code splitting occurs. If \code{TRUE},
matched columns are renamed with their wave prefix removed and a new
\code{"LASA_wave"} column filled with \code{wave} is inserted immediately after
the respondent-number column. It is always treated as \code{TRUE} when
\code{standardize_names = TRUE}.}
}
\value{
\code{data}, with variable/value-label attributes attached to every
matched LASA038 column, optional factor conversion and canonical
renaming, and \code{original_labels}/\code{original_values} attributes preserving
the original SPSS coding. A \code{"LASA_wave"} column is added when requested.
The generic name-matching audit is attached as \code{attr(data, "label_report")} and can be retrieved with \code{\link[=lasa_label_report]{lasa_label_report()}}.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to the religious-belief variables documented in
LASA038. The function covers waves C, D, E, 2B, F, G, H, 3B, MB, I, J,
and K. LASA038 does not document wave A or B files.
}
\details{
The wave schemas are not interchangeable. Wave C contains the original
belief, salience, and cosmic-transcendence item sets. Waves D, E, and 2B
add church attendance and prayer frequency, with church denomination
documented only at D. Later waves retain smaller subsets. Wave 3B has its
own interview-termination code, and wave MB uses a distinct church-
membership classification and an eight-category attendance scale. These
differences are preserved exactly as documented.

This function labels only variables stored in the LASA038 files. The
longitudinal church-affiliation variables documented separately in
\code{LASAz038_varinfo.pdf} and the constructed scales in LASA238 are outside
its scope.

All LASA038 variables are categorical. Consequently, \code{to_numeric} is
accepted for consistency with the shared apply-labels interface but does
not convert them. With \code{to_factor = TRUE}, matched variables become
factors and observed unlabelled codes remain numeric-text levels.

Column matching tries, in order: (1) an explicit override in
\code{name_corrections}, (2) an exact case-sensitive name match, and (3) a
case-insensitive exact match. Variables documented for the selected wave
but absent from \code{data} are left untouched and recorded as \code{"not found"} in
the generic matching audit.

\code{name_corrections}, \code{to_factor}, \code{to_numeric}, \code{standardize_names}, and
\code{split_wavecode} follow the parameter contract shared by the package's
other \verb{apply_*_labels()} functions. Regardless of conversion, every
matched column keeps its original SPSS coding in
\code{attr(x, "original_labels")} and \code{attr(x, "original_values")}. The
\code{"respnr"} column is matched in any capitalization and, when
\code{standardize_names = TRUE}, renamed to \code{"respnr"}.
}
\examples{
dat <- data.frame(
  RespNr = 1:3,
  frelig1b = c(-2, 1, 6),
  frelig08 = c(-1, 1, 2),
  frelig8a = c(-1, 4, 8)
)
dat <- apply_lasa038_labels(dat, wave = "F")
attr(dat$frelig1b, "labels")
}
\seealso{
\code{\link[=apply_lasa036_labels]{apply_lasa036_labels()}}, \code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
