% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA135_labels.R
\name{apply_lasa135_labels}
\alias{apply_lasa135_labels}
\title{Apply LASA135 (EuroQol/VAS) SPSS labels}
\usage{
apply_lasa135_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA135 \code{.sav} file,
for example via \code{\link[haven:read_spss]{haven::read_sav()}}. Documented names run from
\code{eqeq5d1} through \code{eqeqvas} in wave E, with the corresponding \code{f}, \code{g},
\code{h}, \code{b} (3B), or \code{i} prefix in later files.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"E"}, \code{"F"}, \code{"G"}, \code{"H"}, \code{"3B"}, or
\code{"I"}.}

\item{name_corrections}{Optional named character vector mapping canonical
LASA135 suffixes without the wave prefix (\code{qeq5d1} through \code{qeq5d5}, or
\code{qeqvas}) to actual column names in \code{data}.}

\item{to_factor}{Logical. If \code{TRUE}, variables with documented value labels
are converted to factors. Observed unlabelled codes remain numeric-text
levels.}

\item{to_numeric}{Logical. If \code{TRUE}, EQVAS is restored to plain numeric
and negative values are replaced by \code{NA}.}

\item{standardize_names}{Logical. If \code{TRUE}, matched columns are renamed to
canonical lowercase suffixes without the wave prefix, \code{respnr} is
standardized, and \code{split_wavecode} is treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{TRUE}, matched columns have the wave
prefix removed and a \code{LASA_wave} column containing the selected wave is
inserted immediately after the respondent-number column.}
}
\value{
\code{data}, with LASA135 variable/value labels, optional conversion and
standardized naming, preserved original SPSS coding, the \code{LASA_wave}
provenance attribute, and a \code{label_report} matching audit.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to the six EuroQol variables documented in LASA135
for waves E, F, G, H, 3B, and I.
}
\details{
The inventory contains the five three-level EQ-5D dimensions (mobility,
self-care, usual activities, pain/discomfort, and anxiety/depression) and
the 0--100 EQ visual analogue scale. The two TTO index variables shown in
the same source document belong to the separate LASA335 scale-score file
and are intentionally not labelled by this LASA135 function.

The EQ-5D dimensions are categorical. The EQVAS score is numeric and its
documented -1 missing code becomes \code{NA} when \code{to_numeric = TRUE}. With
\code{to_factor = TRUE}, variables carrying value labels become factors and
observed unlabelled codes remain numeric-text levels. \code{to_numeric} takes
precedence for EQVAS.

Column matching tries, in order: (1) an explicit override in
\code{name_corrections}, (2) an exact case-sensitive match, and (3) a
case-insensitive exact match. Variables absent from \code{data} are recorded as
\code{"not found"} in the matching audit. Original labels and values are
preserved, and optional standardized naming removes the wave prefix,
standardizes \code{respnr}, and adds \code{LASA_wave}.
}
\examples{
dat <- data.frame(RespNr = 1:2, eqeq5d1 = c(1, 2), eqeqvas = c(-1, 80))
dat <- apply_lasa135_labels(dat, wave = "E", to_numeric = TRUE)
attr(dat$eqeq5d1, "labels")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
