% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA024_labels.R
\name{apply_lasa024_labels}
\alias{apply_lasa024_labels}
\title{Apply LASA024 (Evaluation Raven) SPSS labels}
\usage{
apply_lasa024_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASAB024 \code{.sav} file,
for example via \code{\link[haven:read_spss]{haven::read_sav()}}, containing some or all of \code{bmecog1},
\code{bmecog2}, and \code{bmecog3}.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. LASA024 is documented only for \code{"B"}.}

\item{name_corrections}{Optional named character vector for explicit column
name overrides. Names are canonical LASA024 suffixes without the \code{b}
prefix (\code{mecog1}, \code{mecog2}, or \code{mecog3}), and values are actual column
names in \code{data}, for example \code{c(mecog1 = "raven_relaxation")}.}

\item{to_factor}{Logical. If \code{FALSE} (default), categorical variables
remain numeric with SPSS-style value-label attributes. If \code{TRUE}, they
are converted to factors using their documented labels. An observed
value without a label remains a numeric-text factor level.}

\item{to_numeric}{Logical. Retained for the common apply-label parameter
contract. LASA024 contains no count or continuous variables, so this
argument does not change the documented categorical variables.}

\item{standardize_names}{Logical. If \code{FALSE} (default), source column names
are retained except for renaming requested through \code{split_wavecode}. If
\code{TRUE}, matched LASA024 columns are renamed to their canonical lowercase
suffixes with the \code{b} prefix removed; \code{"respnr"} is standardized; and
\code{split_wavecode} is always treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{FALSE} (default) and
\code{standardize_names = FALSE}, no wave-code splitting occurs. If \code{TRUE},
matched columns are renamed with the \code{b} prefix removed and a new
\code{"LASA_wave"} column filled with \code{"B"} is inserted immediately after the
respondent-number column. It is always treated as \code{TRUE} when
\code{standardize_names = TRUE}.}
}
\value{
\code{data}, with variable/value-label attributes attached to every
matched LASA024 column, optional factor conversion and canonical
renaming, and \code{original_labels}/\code{original_values} attributes preserving
the original SPSS coding. A \code{"LASA_wave"} column is added when requested.
The generic name-matching audit is attached as \code{attr(data, "label_report")} and can be retrieved with \code{\link[=lasa_label_report]{lasa_label_report()}}.
}
\description{
Attaches the SPSS-style variable labels (\code{attr(x, "label")}) and value
labels (\code{attr(x, "labels")}) documented for the three LASAB024 Raven
evaluation variables. The variables assess relaxation during the test,
present capability, and expected future capability. They share the same
missing-response codes but use three distinct substantive five-point
scales.
}
\details{
LASA024 is documented only for wave B. By default, matched variables remain
numeric with their SPSS-style labels. With \code{to_factor = TRUE}, they become
factors using the documented response text; observed unlabelled codes
remain numeric-text levels. The variables are categorical evaluations, so
\code{to_numeric} is retained for the common function contract but does not
change them.

Column matching tries, in order: (1) an explicit override in
\code{name_corrections}, (2) an exact case-sensitive name match, and (3) a
case-insensitive exact match. Variables absent from \code{data} are left
untouched and recorded as \code{"not found"} in the generic matching audit.

\code{name_corrections}, \code{to_factor}, \code{to_numeric}, \code{standardize_names}, and
\code{split_wavecode} are part of the parameter contract shared by every
\verb{apply_*_labels()} function in this package. Regardless of conversion,
every matched column keeps its original SPSS value coding in
\code{attr(x, "original_labels")} and \code{attr(x, "original_values")}. The
\code{"respnr"} column is matched in any capitalization and, when
\code{standardize_names = TRUE}, renamed to \code{"respnr"}.
}
\examples{
dat <- data.frame(
  RespNr = 1:3,
  bmecog1 = c(-5, 1, 5),
  bmecog2 = c(-1, 3, 5),
  bmecog3 = c(0, 2, 4)
)
dat <- apply_lasa024_labels(dat, wave = "B")
attr(dat$bmecog1, "labels")
}
\seealso{
\code{\link[=apply_lasa023_labels]{apply_lasa023_labels()}}, \code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
