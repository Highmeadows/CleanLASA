% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA221_labels.R
\name{apply_lasa221_labels}
\alias{apply_lasa221_labels}
\title{Apply LASA221 (MMSE maximum score) SPSS labels}
\usage{
apply_lasa221_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA221 \code{.sav} file.}

\item{wave}{Character scalar identifying B, C, D, E, 2B, F, G, H, 3B, MB,
I, J, or K; matching is case-insensitive.}

\item{name_corrections}{Optional named character vector mapping canonical
suffixes (\code{mmsesc}, or MB's \code{msesc1} and \code{msesc2}) to actual columns.}

\item{to_factor}{Logical. Convert variables carrying labels to factors
unless numeric restoration takes precedence.}

\item{to_numeric}{Logical. Restore score variables to plain numeric and
replace negative missing codes with \code{NA}.}

\item{standardize_names}{Logical. Standardize names and \code{respnr}, remove
wave prefixes, and add \code{LASA_wave}.}

\item{split_wavecode}{Logical. Remove wave prefixes and add \code{LASA_wave};
implied by \code{standardize_names = TRUE}.}
}
\value{
\code{data} with LASA221 metadata, optional conversion and renaming, the
\code{LASA_wave} attribute, and a \code{label_report} audit.
}
\description{
Attaches SPSS-style variable and value labels to the constructed MMSE
scores in LASA221. For the regular cohorts, the score uses the maximum of
spelling backwards and serial subtraction. The Migrant Baseline file has
both the maximum score and an illiterate score based on spelling.
}
\details{
LASA221 is documented for waves B, C, D, E, 2B, F, G, H, 3B, MB, I, J,
and K. B has a minimum labelled score of 2; C has a minimum of 1; D onward
additionally document \code{-3} for interview termination. MB additionally uses
\code{-5} and \code{-4} missing codes and contains two scores. All scores are numeric:
\code{to_numeric = TRUE} restores plain numeric values and replaces negative
codes with \code{NA}. Matching and standardized naming follow the shared LASA
label-engine conventions, including \code{respnr}, \code{LASA_wave}, preserved
original coding, and a final \code{label_report}.
}
\examples{
dat <- data.frame(RespNr = 1:2, dmmsesc = c(-3, 27))
apply_lasa221_labels(dat, wave = "D", to_numeric = TRUE)
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
