% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA174_labels.R
\name{apply_lasa174_labels}
\alias{apply_lasa174_labels}
\title{Apply LASA174 (food supplements) SPSS labels}
\usage{
apply_lasa174_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA174 \code{.sav} file.
Wave-I names begin with \code{im}; wave-J names begin with \code{jmD} or \code{jmMV}.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"I"} or \code{"J"}.}

\item{name_corrections}{Optional named character vector mapping canonical
LASA174 suffixes without the initial wave prefix (for example,
\code{msupquant1} or \code{mDsupquant1}) to actual column names in \code{data}.}

\item{to_factor}{Logical. If \code{TRUE}, variables with documented value labels
are converted to factors unless converted to numeric.}

\item{to_numeric}{Logical. If \code{TRUE}, documented counts, quantities, and
times-per-period variables are restored to plain numeric and negative
values are replaced by \code{NA}.}

\item{standardize_names}{Logical. If \code{TRUE}, matched columns are renamed to
canonical lowercase suffixes without the wave prefix, \code{respnr} is
standardized, and \code{split_wavecode} is treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{TRUE}, matched columns have the wave
prefix removed and a \code{LASA_wave} column is inserted after \code{respnr}.}
}
\value{
\code{data}, with LASA174 labels, optional conversion and standardized
naming, preserved original SPSS coding, the \code{LASA_wave} provenance
attribute, and a \code{label_report} matching audit.
}
\description{
Attaches SPSS-style variable labels and value labels to the food-supplement
variables documented in LASA174 for waves I and J. Wave I records a general
supplement list. Wave J separates vitamin D and multivitamin supplements.
Both computer-assisted files allow a sample-dependent number of repeated
supplement records; this function labels every documented repeat index
present in \code{data} (and every repeat named in \code{name_corrections}).
}
\details{
Supplement counts, quantities, and use frequencies are eligible for
\code{to_numeric = TRUE}; negative missing codes then become \code{NA}. Use,
measurement-unit, period, dosage-form, and seasonal-use variables remain
categorical. Text fields receive variable labels without invented value
labels. With \code{to_factor = TRUE}, documented categorical codes become factor
levels while observed unlabelled codes are retained.

The source PDF also contains LASA374, which adds derived vitamin D content.
Those variables belong to a separate filecode and are intentionally not
labelled here. Matching tries \code{name_corrections}, an exact match, then a
case-insensitive exact match. Original labels and values are preserved;
optional standardized naming removes the wave prefix and adds \code{LASA_wave}.
}
\examples{
dat <- data.frame(RespNr = 1:2, imsup = c(1, 2), `im#sup` = c(0, 1))
apply_lasa174_labels(dat, wave = "I", to_factor = TRUE)
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
