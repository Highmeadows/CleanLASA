% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA118_labels.R
\name{apply_lasa118_labels}
\alias{apply_lasa118_labels}
\title{Apply LASA118 (Diet/food quality) SPSS labels}
\usage{
apply_lasa118_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA118 \code{.sav} file.}

\item{wave}{Character scalar identifying \code{"B"}, \code{"C"}, \code{"D"}, \code{"H"},
\code{"I"}, \code{"J"}, or \code{"K"}; matching is case-insensitive.}

\item{name_corrections}{Optional named character vector whose names are
canonical suffixes without the wave prefix, such as \code{qfood01} or
\code{qapp1}, and whose values are actual column names in \code{data}.}

\item{to_factor}{Logical. Convert matched categorical variables to factors
using documented value labels.}

\item{to_numeric}{Logical. Restore eligible wave-B frequency variables to
plain numeric and replace negative missing codes with \code{NA}.}

\item{standardize_names}{Logical. Standardize matched names and respondent
number, remove wave prefixes, and add \code{LASA_wave}.}

\item{split_wavecode}{Logical. Remove wave prefixes from matched names and
add \code{LASA_wave}; implied by \code{standardize_names = TRUE}.}
}
\value{
\code{data} with LASA118 metadata, optional conversion and renaming,
original-coding attributes, \code{LASA_wave}, and a \code{label_report} audit.
}
\description{
Attaches SPSS-style metadata to the diet, food-quality, and appetite
variables documented for LASA118. The inventory changes substantially by
wave: B contains 20 chewing, meal-frequency, diet, fruit, and vegetable
items; C and D contain one chewing item; H, I, and J contain seven eating-
risk items; and K contains those seven items plus four appetite items.
}
\details{
Wave-B meal-frequency and fruit/vegetable frequency variables are numeric
with \code{-1} for no answer. With \code{to_numeric = TRUE}, these variables become
plain numeric and negative codes become \code{NA}. All remaining variables are
categorical and can be converted with \code{to_factor = TRUE}. Value labels,
variable labels, and spelling are reproduced from the codebook.

Column matching tries \code{name_corrections}, an exact name, then a case-
insensitive exact name. Original labels and values are retained in
\code{original_labels} and \code{original_values} attributes.
}
\examples{
dat <- data.frame(
  RespNr = 1:3,
  BQFOOD01 = c(-1, 1, 4),
  bqfood02 = c(-1, 2, 4)
)
dat <- apply_lasa118_labels(dat, wave = "B", to_numeric = TRUE)
dat$bqfood02
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
