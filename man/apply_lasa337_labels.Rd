% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA337_labels.R
\name{apply_lasa337_labels}
\alias{apply_lasa337_labels}
\title{Apply LASA337 (Valuation of Late Life Scale Scores) SPSS labels}
\usage{
apply_lasa337_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA337 \code{.sav} file.
Documented names are \code{eqvall}, \code{eqres}, \code{eqamb}, and \code{eqzest} in wave E,
with the corresponding \code{f} prefix in wave F.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"E"} or \code{"F"}.}

\item{name_corrections}{Optional named character vector mapping canonical
LASA337 suffixes without the wave prefix (\code{qvall}, \code{qres}, \code{qamb}, or
\code{qzest}) to actual column names in \code{data}.}

\item{to_factor}{Logical. If \code{TRUE}, variables with documented value labels
are converted to factors. Observed unlabelled scores remain numeric-text
levels.}

\item{to_numeric}{Logical. If \code{TRUE}, all four scale scores are restored to
plain numeric and negative values are replaced by \code{NA}.}

\item{standardize_names}{Logical. If \code{TRUE}, matched columns are renamed to
canonical lowercase suffixes without the wave prefix, \code{respnr} is
standardized, and \code{split_wavecode} is treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{TRUE}, matched columns have the wave
prefix removed and a \code{LASA_wave} column containing the selected wave is
inserted immediately after the respondent-number column.}
}
\value{
\code{data}, with LASA337 variable/value labels, optional conversion and
standardized naming, preserved original SPSS coding, the \code{LASA_wave}
provenance attribute, and a \code{label_report} matching audit.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to the four Valuation of Late Life scale scores
documented in LASA337 for waves E and F.
}
\details{
The inventory comprises the total Valuation of Late Life score and its
resilience, ambition, and zest-for-life subscales. These scale scores are
distinct from the item-level LASA137 variables listed in the same source
document. Every LASA337 score uses \code{-2} for no valid data. Documented low
and high endpoint labels differ slightly by wave for the total score and
ambition subscale and are reproduced exactly.

All four variables are numeric. With \code{to_numeric = TRUE}, their negative
missing code becomes \code{NA}; \code{to_numeric} therefore takes precedence over
\code{to_factor}. With \code{to_factor = TRUE} alone, documented endpoints become
factor levels and observed unlabelled interior scores are retained as
numeric-text levels.

Column matching tries, in order: (1) an explicit override in
\code{name_corrections}, (2) an exact case-sensitive match, and (3) a
case-insensitive exact match. Variables absent from \code{data} are recorded as
\code{"not found"} in the matching audit. Original labels and values are
preserved, and optional standardized naming removes the wave prefix,
standardizes \code{respnr}, and adds \code{LASA_wave}.
}
\examples{
dat <- data.frame(RespNr = 1:2, eqvall = c(-2, 48), eqres = c(12, 20))
dat <- apply_lasa337_labels(dat, wave = "E", to_numeric = TRUE)
attr(dat$eqvall, "label")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
