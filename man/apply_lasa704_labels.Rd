% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA704_labels.R
\name{apply_lasa704_labels}
\alias{apply_lasa704_labels}
\title{Apply LASA704 (\link{RESP} vision and hearing) SPSS labels}
\usage{
apply_lasa704_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA704 \code{.sav} file.}

\item{wave}{Character scalar: \code{"C"}, \code{"D"}, \code{"E"}, \code{"F"}, \code{"G"}, \code{"H"},
\code{"I"}, \code{"J"}, or \code{"K"}; matching is case-insensitive.}

\item{name_corrections}{Optional named character vector mapping canonical
suffixes without the wave prefix to actual column names in \code{data}.}

\item{to_factor}{Logical. Convert labelled categorical variables to factors.}

\item{to_numeric}{Logical. Convert eligible codebook-numeric variables to
plain numeric; LASA704 contains no such variables.}

\item{standardize_names}{Logical. Standardize matched names and \code{respnr},
remove wave prefixes, and add \code{LASA_wave}.}

\item{split_wavecode}{Logical. Remove wave prefixes from matched names and
add \code{LASA_wave}; implied by \code{standardize_names = TRUE}.}
}
\value{
\code{data} with LASA704 metadata, optional conversion and reshaping,
an \code{LASA_wave} attribute, and a \code{label_report} matching audit.
}
\description{
Attaches the variable and value labels documented for LASA704 respondent
telephone-interview data in waves C through K. Wave C contains two
questions about change in vision and hearing during the previous three
years. Waves D through I contain six questions about glasses, hearing aids,
reading small print, and following a conversation. Waves J and K replace
the earlier hearing-aid block with six more detailed questions about aid
possession, use, current wearing, and conversation.
}
\details{
All documented LASA704 variables are categorical. Consequently,
\code{to_factor = TRUE} converts them to factors while retaining observed
undocumented codes, whereas \code{to_numeric} does not alter them. Original
values and value labels remain available in \code{original_values} and
\code{original_labels} attributes.

Matching tries an explicit \code{name_corrections} entry, an exact match, and a
case-insensitive exact match, in that order. Standardized naming removes
the wave prefix; \code{standardize_names} and \code{split_wavecode} add \code{LASA_wave}.
}
\examples{
dat <- data.frame(RespNr = 1:2, ctrsight = c(3, 4), ctrhear = c(3, 5))
apply_lasa704_labels(dat, wave = "C")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
