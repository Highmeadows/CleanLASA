% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA126_labels.R
\name{apply_lasa126_labels}
\alias{apply_lasa126_labels}
\title{Apply LASA126 (Oral health) SPSS labels}
\usage{
apply_lasa126_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA126 \code{.sav} file.
Names may include \code{iqorh3c}, \code{iqorh8a}, \code{jqorh3d}, or \code{kqorh8i}.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"I"}, \code{"J"}, or \code{"K"}.}

\item{name_corrections}{Optional named character vector mapping canonical
suffixes without the wave prefix (for example \code{qorh3c} or \code{qorh8i}) to
actual names in \code{data}.}

\item{to_factor}{Logical. If \code{TRUE}, matched categorical variables are
converted to factors using the documented labels.}

\item{to_numeric}{Logical. Accepted for interface consistency; LASA126 has
no numeric variables eligible for conversion.}

\item{standardize_names}{Logical. If \code{TRUE}, matched names and \code{respnr} are
standardized and \code{split_wavecode} is treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{TRUE}, matched names lose the wave prefix
and a \code{LASA_wave} column is inserted after \code{respnr}.}
}
\value{
\code{data}, with LASA126 labels, requested conversion or renaming,
preserved original coding, \code{LASA_wave} provenance, and a \code{label_report}.
}
\description{
Attaches SPSS-style variable and value labels to the oral-health variables
documented in LASA126 for waves I, J, and K.
}
\details{
Wave I contains 22 items covering self-rated oral health, own teeth,
dentures, oral hygiene, dental care, gingivitis, and nine recent oral-health
problems. Waves J and K use a shorter eight-item schema and introduce
frequency scales for poorly fitting dentures, toothache while chewing, and
dry mouth.

The documented variables are categorical, including the grouped own-teeth
categories. \code{to_numeric} is therefore accepted for interface consistency
but does not convert them. \code{to_factor = TRUE} converts matched variables to
factors while retaining observed unlabelled codes as numeric-text levels.

Matching tries \code{name_corrections}, an exact case-sensitive match, and then
a case-insensitive exact match. Original SPSS coding is preserved;
standardized naming removes the wave prefix and can add \code{LASA_wave}.
}
\examples{
dat <- data.frame(iqorh1 = c(1, 5), iqorh8a = c(1, 2))
dat <- apply_lasa126_labels(dat, wave = "I", to_factor = TRUE)
attr(dat$iqorh1, "labels")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
