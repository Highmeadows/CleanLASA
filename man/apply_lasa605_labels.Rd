% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA605_labels.R
\name{apply_lasa605_labels}
\alias{apply_lasa605_labels}
\title{Apply LASA605 (\link{PROXY} falling and fractures) SPSS labels}
\usage{
apply_lasa605_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA605 \code{.sav} file.}

\item{wave}{Character scalar: \code{"C"}, \code{"D"}, \code{"E"}, \code{"F"}, \code{"G"}, \code{"H"},
\code{"I"}, \code{"J"}, or \code{"K"}; matching is case-insensitive.}

\item{name_corrections}{Optional named character vector mapping canonical
suffixes without the wave prefix to actual column names in \code{data}.}

\item{to_factor}{Logical. Convert labelled categorical variables to factors.}

\item{to_numeric}{Logical. Convert eligible fall/fracture counts to plain
numeric and replace documented negative missing codes with \code{NA}.}

\item{standardize_names}{Logical. Standardize matched names and \code{respnr},
remove the wave prefix, and add \code{LASA_wave}.}

\item{split_wavecode}{Logical. Remove wave prefixes from matched names and
add \code{LASA_wave}; implied by \code{standardize_names = TRUE}.}
}
\value{
\code{data} with LASA605 metadata, optional conversions and renaming, a
\code{LASA_wave} attribute, and a \code{label_report} matching audit.
}
\description{
Attaches the variable and value labels documented for LASA605 proxy
telephone-interview data. Wave C records falls and up to three fractures
in a ten-variable inventory. Wave D adds a fracture count and separate
occurrence questions for each fracture. Waves E through K retain the
eleven fracture variables but omit the two fall questions.
}
\details{
Fracture type uses the documented 13-code table, from pulse/wrist/Colles
fracture through unknown type. Matching uses an explicit
\code{name_corrections} entry, an exact match, then a case-insensitive exact
match. \code{to_factor = TRUE} converts categorical variables to factors while
retaining observed undocumented codes. \code{to_numeric = TRUE} restores the
fall and fracture counts to plain numeric and replaces their negative
missing codes with \code{NA}. Original values and labels remain available in
\code{original_values} and \code{original_labels} attributes.
}
\examples{
dat <- data.frame(RespNr = 1:2, ctpfall = c(1, 2), ctpfalln = c(-2, 3))
apply_lasa605_labels(dat, wave = "C")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
