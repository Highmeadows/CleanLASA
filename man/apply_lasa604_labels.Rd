% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA604_labels.R
\name{apply_lasa604_labels}
\alias{apply_lasa604_labels}
\title{Apply LASA604 (\link{PROXY} vision and hearing) SPSS labels}
\usage{
apply_lasa604_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA604 \code{.sav} file.}

\item{wave}{Character scalar: \code{"C"}, \code{"D"}, \code{"E"}, \code{"F"}, \code{"G"}, \code{"H"},
\code{"I"}, \code{"J"}, or \code{"K"}; matching is case-insensitive.}

\item{name_corrections}{Optional named character vector mapping canonical
suffixes without the wave prefix to actual column names in \code{data}.}

\item{to_factor}{Logical. Convert labelled categorical variables to factors.}

\item{to_numeric}{Logical. Convert eligible codebook-numeric variables to
plain numeric; LASA604 has no such variables.}

\item{standardize_names}{Logical. Standardize matched names and \code{respnr},
remove the wave prefix, and add \code{LASA_wave}.}

\item{split_wavecode}{Logical. Remove wave prefixes from matched names and
add \code{LASA_wave}; implied by \code{standardize_names = TRUE}.}
}
\value{
\code{data} with LASA604 metadata, optional conversions and renaming, a
\code{LASA_wave} attribute, and a \code{label_report} matching audit.
}
\description{
Attaches the variable and value labels documented for LASA604 proxy
telephone-interview data. Waves C, D, and E contain two questions about
change in vision and hearing during the previous three years. Waves F
through K contain six questions about glasses, hearing aids, reading small
print, and following a conversation. Codes 5 for not using an aid are
documented only in waves H through K.
}
\details{
Matching uses an explicit \code{name_corrections} entry, an exact match, then a
case-insensitive exact match. \code{to_factor = TRUE} converts labelled
categorical variables to factors while retaining observed undocumented
codes. LASA604 contains no codebook-numeric variables, so \code{to_numeric}
does not alter its documented categorical variables. Original values and
labels remain available in \code{original_values} and \code{original_labels}
attributes.
}
\examples{
dat <- data.frame(RespNr = 1:2, ctpsight = c(3, 4), ctphear = c(3, 5))
apply_lasa604_labels(dat, wave = "C")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
