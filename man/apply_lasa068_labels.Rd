% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA068_labels.R
\name{apply_lasa068_labels}
\alias{apply_lasa068_labels}
\title{Apply LASA068 (Care in country of origin) SPSS labels}
\usage{
apply_lasa068_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASMB068 \code{.sav} file,
for example via \code{\link[haven:read_spss]{haven::read_sav()}}. Names include \code{bmcarco}, \code{bmcarc1},
and \code{bmcarr10}.}

\item{wave}{Character scalar identifying the LASA wave. Only \code{"MB"} is
documented and accepted; matching is case-insensitive.}

\item{name_corrections}{Optional named character vector for explicit column
name overrides. Names are canonical LASA068 suffixes without the \code{b}
prefix (for example \code{mcarco}, \code{mcarc1}, or \code{mcarr10}), and values are
actual column names in \code{data}.}

\item{to_factor}{Logical. If \code{FALSE} (default), variables remain numeric
with SPSS-style value-label attributes. If \code{TRUE}, they are converted to
factors using the documented labels. An observed unlabelled code remains
a numeric-text factor level.}

\item{to_numeric}{Logical. Accepted for interface consistency. LASA068 has
no numeric variables eligible for conversion, so this argument does not
alter matched columns.}

\item{standardize_names}{Logical. If \code{FALSE} (default), source column names
are retained except for renaming requested through \code{split_wavecode}. If
\code{TRUE}, matched columns are renamed to their canonical lowercase suffixes
with the \code{b} prefix removed; \code{"respnr"} is standardized; and
\code{split_wavecode} is always treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{FALSE} (default) and
\code{standardize_names = FALSE}, no wave-code splitting occurs. If \code{TRUE},
matched columns are renamed with their \code{b} prefix removed and a new
\code{"LASA_wave"} column filled with \code{"MB"} is inserted immediately after
the respondent-number column. It is always treated as \code{TRUE} when
\code{standardize_names = TRUE}.}
}
\value{
\code{data}, with variable/value-label attributes attached to every
matched LASA068 column, optional factor conversion and canonical
renaming, and \code{original_labels}/\code{original_values} attributes preserving
the original SPSS coding. The generic matching audit is attached as
\code{attr(data, "label_report")} and can be retrieved with
\code{\link[=lasa_label_report]{lasa_label_report()}}.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to the 17 variables documented in the LASMB068 care-
in-country-of-origin file.
}
\details{
The file contains one screening item for medical care during a stay in the
country of origin in the past year, six types of provider contacted, and
ten reasons for obtaining that care. The 16 follow-up items share the same
documented routing and mentioned/not-mentioned value map.

All LASA068 variables are categorical. Consequently, \code{to_numeric} is
accepted for consistency with the shared apply-labels interface but does
not convert them. With \code{to_factor = TRUE}, matched variables become
factors and observed unlabelled codes remain numeric-text levels.

Column matching tries, in order: (1) an explicit override in
\code{name_corrections}, (2) an exact case-sensitive name match, and (3) a
case-insensitive exact match. Documented variables absent from \code{data} are
left untouched and recorded as \code{"not found"} in the generic matching audit.

\code{name_corrections}, \code{to_factor}, \code{to_numeric}, \code{standardize_names}, and
\code{split_wavecode} follow the parameter contract shared by the package's
other \verb{apply_*_labels()} functions. Regardless of conversion, every
matched column keeps its original SPSS coding in
\code{attr(x, "original_labels")} and \code{attr(x, "original_values")}. The
\code{"respnr"} column is matched in any capitalization and, when
\code{standardize_names = TRUE}, renamed to \code{"respnr"}.
}
\examples{
dat <- data.frame(
  RespNr = 1:3,
  bmcarco = c(-4, 1, 2),
  bmcarc1 = c(-2, 0, 1),
  bmcarr01 = c(-2, 1, 0)
)
dat <- apply_lasa068_labels(dat, wave = "MB")
attr(dat$bmcarc1, "labels")
}
\seealso{
\code{\link[=apply_lasa048_labels]{apply_lasa048_labels()}}, \code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
