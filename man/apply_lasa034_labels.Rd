% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA034_labels.R
\name{apply_lasa034_labels}
\alias{apply_lasa034_labels}
\title{Apply LASA034 (physical performance tests) SPSS labels}
\usage{
apply_lasa034_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA034 \code{.sav} file.}

\item{wave}{Character scalar identifying the LASA wave. One of \code{"B"},
\code{"C"}, \code{"D"}, \code{"E"}, \code{"2B"}, \code{"F"}, \code{"G"}, \code{"H"}, \code{"3B"}, \code{"MB"},
\code{"I"}, \code{"J"}, or \code{"K"} (matched case-insensitively).}

\item{name_corrections}{Optional named character vector mapping canonical
LASA034 suffixes (without a wave prefix), such as \code{walk04a}, to actual
column names in \code{data}.}

\item{to_factor}{Logical. If \code{TRUE}, variables with substantive value
labels are converted to factors. Observed but unlabelled values are
retained as levels containing their numeric code.}

\item{to_numeric}{Logical. If \code{TRUE}, variables whose value labels consist
only of negative missing-reason codes are converted to plain numeric and
all negative values are replaced by \code{NA}. This takes precedence over
\code{to_factor}.}

\item{standardize_names}{Logical. If \code{TRUE}, matched variables are renamed
to their canonical lowercase LASA034 names with the wave code removed,
\code{"respnr"} (in any capitalization) is renamed to \code{"respnr"}, and
\code{split_wavecode} is always treated as \code{TRUE} as well, regardless of what
was passed for \code{split_wavecode}.}

\item{split_wavecode}{Logical. If \code{TRUE}, matched columns are renamed with
the wave-letter prefix removed (e.g. \code{bwalk04} becomes \code{walk04}), and a
new \code{"LASA_wave"} column, filled with \code{wave}, is inserted right after
\code{"respnr"}. Always treated as \code{TRUE} when \code{standardize_names = TRUE}.}
}
\value{
\code{data}, with labels applied and an audit in its \code{label_report}
attribute, plus reference \code{original_labels}/\code{original_values}
attributes preserving the original SPSS value coding regardless of
\code{to_factor}/\code{to_numeric} reshaping. A new \code{"LASA_wave"} column is added
after \code{"respnr"} when \code{standardize_names = TRUE} or \code{split_wavecode = TRUE}.
}
\description{
Attaches the documented SPSS variable labels (\code{attr(x, "label")}) and
value labels (\code{attr(x, "labels")}) to variables from a LASA034 physical
performance file. The implementation covers waves B, C, D, E, 2B, F, G,
H, 3B, MB, I, J, and K, including their wave-specific tests and codings.
}
\details{
The shared reshaping arguments have the same names, defaults, and behavior
as in \code{\link[=apply_lasa046_labels]{apply_lasa046_labels()}}. Column matching tries an explicit
\code{name_corrections} override, an exact match, and a case-insensitive exact
match, in that order. Results are recorded in the generic \code{label_report}
attribute and can be inspected with \code{\link[=lasa_label_report]{lasa_label_report()}}.

Regardless of \code{to_factor}/\code{to_numeric}, every matched column also keeps
its original SPSS value coding available as reference attributes --
\code{attr(x, "original_labels")} and \code{attr(x, "original_values")} -- and the
\code{"respnr"} column (in any capitalization) is matched and, when
\code{standardize_names = TRUE}, renamed to \code{"respnr"}.
}
\examples{
dat <- data.frame(BWALK01 = c(1, 2), BWALK04 = c(8.2, -6))
dat <- apply_lasa034_labels(dat, wave = "B")
attr(dat$BWALK01, "label")
attr(dat$BWALK01, "labels")

dat <- apply_lasa034_labels(
  dat, wave = "B", to_factor = TRUE, to_numeric = TRUE,
  standardize_names = TRUE
)
}
\seealso{
\code{\link[=apply_lasa046_labels]{apply_lasa046_labels()}}, \code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
