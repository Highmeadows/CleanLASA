% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA175_labels.R
\name{apply_lasa175_labels}
\alias{apply_lasa175_labels}
\title{Apply LASA175 (attitudes towards nursing home care) SPSS labels}
\usage{
apply_lasa175_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA175 \code{.sav} file.
Documented names are \code{jmnh01} through \code{jmnh09} in wave J and the
corresponding \code{kmnh} names in wave K.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"J"} or \code{"K"}.}

\item{name_corrections}{Optional named character vector mapping canonical
LASA175 suffixes without the wave prefix (for example, \code{mnh01}) to actual
column names in \code{data}.}

\item{to_factor}{Logical. If \code{TRUE}, variables with documented value labels
are converted to factors.}

\item{to_numeric}{Logical. Accepted for the shared interface. LASA175 has
no documented continuous numeric variables.}

\item{standardize_names}{Logical. If \code{TRUE}, matched columns are renamed to
canonical lowercase suffixes without the wave prefix, \code{respnr} is
standardized, and \code{split_wavecode} is treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{TRUE}, matched columns have the wave
prefix removed and a \code{LASA_wave} column is inserted after \code{respnr}.}
}
\value{
\code{data}, with LASA175 labels, optional factor conversion and
standardized naming, preserved original SPSS coding, the \code{LASA_wave}
provenance attribute, and a \code{label_report} matching audit.
}
\description{
Attaches SPSS-style variable and value labels to the nine attitude items
documented in LASA175 for waves J and K. Items cover perceived quality,
privacy, time outside, social contact, autonomy, visitors, and respectful
treatment in nursing homes. All share a five-point agreement scale plus the
documented asked-missing code.
}
\details{
All LASA175 variables are categorical and none is converted by
\code{to_numeric = TRUE}. With \code{to_factor = TRUE}, documented codes become
factor levels while observed unlabelled codes are retained. Matching tries
\code{name_corrections}, an exact match, then a case-insensitive exact match.
Original labels and values are preserved. Optional standardized naming
removes the wave prefix, standardizes \code{respnr}, and adds \code{LASA_wave}.
}
\examples{
dat <- data.frame(RespNr = 1:2, jmnh01 = c(1, 5), jmnh02 = c(2, 4))
apply_lasa175_labels(dat, wave = "J", to_factor = TRUE)
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
