% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA139_labels.R
\name{apply_lasa139_labels}
\alias{apply_lasa139_labels}
\title{Apply LASA139 (Partner questions) SPSS labels}
\usage{
apply_lasa139_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA139 \code{.sav} file,
for example via \code{\link[haven:read_spss]{haven::read_sav()}}. Names include wave-prefixed forms
such as \code{eqprel1}, \code{bqpadl6}, \code{iqpdeme}, and \code{kqprespc4}.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"E"}, \code{"F"}, \code{"G"}, \code{"H"}, \code{"3B"}, \code{"I"},
\code{"J"}, or \code{"K"}.}

\item{name_corrections}{Optional named character vector mapping canonical
LASA139 suffixes without the wave prefix (for example \code{qprel1}, \code{qpdis1},
\code{qpadl6}, or \code{qprespc1}) to actual column names in \code{data}.}

\item{to_factor}{Logical. If \code{TRUE}, categorical variables with documented
value labels are converted to factors. Observed unlabelled codes remain
numeric-text levels.}

\item{to_numeric}{Logical. Retained for the shared parameter contract.
LASA139 contains no numeric count or continuous variables, so it does not
change the documented variables.}

\item{standardize_names}{Logical. If \code{TRUE}, matched columns are renamed to
canonical lowercase suffixes without the wave prefix, \code{respnr} is
standardized, and \code{split_wavecode} is treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{TRUE}, matched columns have the wave
prefix removed and a \code{LASA_wave} column containing the selected wave is
inserted immediately after the respondent-number column.}
}
\value{
\code{data}, with LASA139 variable/value labels, optional factor
conversion and standardized naming, preserved original SPSS coding, the
\code{LASA_wave} provenance attribute, and a \code{label_report} matching audit.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to the partner-question variables documented in
LASA139 for waves E, F, G, H, 3B, I, J, and K.
}
\details{
Every wave contains six relationship-satisfaction items, important
conversation frequency, five partner functional-limitation items, partner
memory problems, and partner dementia. Waves E through J (but not K) also
contain two relationship evaluations, four dispute items, and public
transportation. Dementia is routed from partner status through 3B and from
partner memory problems in I--K. Waves I--K additionally contain four
respite-care items. Consequently, the documented inventories contain 21
variables in E/F/G/H/3B, 25 in I/J, and 18 in K.

All documented variables are categorical. With \code{to_factor = TRUE}, they
become factors and observed unlabelled codes (including intermediate 2--9
relationship-evaluation scores) remain numeric-text levels. \code{to_numeric}
is retained for the shared parameter contract but does not change them.

Column matching tries an explicit \code{name_corrections} override, an exact
case-sensitive match, then a case-insensitive exact match. Absent variables
are recorded as \code{"not found"}. Original SPSS labels and values are
preserved. Standardized naming optionally removes the wave prefix,
standardizes \code{respnr}, and adds \code{LASA_wave}.
}
\examples{
dat <- data.frame(RespNr = 1:2, eqprel1 = c(1, 5), eqpdeme = c(-2, 2))
dat <- apply_lasa139_labels(dat, wave = "E", to_factor = TRUE)
attr(dat$eqprel1, "labels")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
