% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lasa_label_db.R
\name{restore_lasa_labels}
\alias{restore_lasa_labels}
\title{Restore/reset the local LASA label database}
\usage{
restore_lasa_labels(
  filecode = NULL,
  wave = NULL,
  variable = NULL,
  rebuild = FALSE
)
}
\arguments{
\item{filecode}{Optional LASA file code. Scopes which manual overrides
are discarded; \code{NULL} (the default) discards overrides for every file
code.}

\item{wave}{Optional LASA wave code, or \code{"all"}. Scopes which manual
overrides are discarded within \code{filecode}; \code{NULL} (the default, same
as \code{"all"}) discards overrides for every wave.}

\item{variable}{Optional wave-specific or canonical variable name.
Scopes which manual overrides are discarded; \code{NULL} (the default)
discards every matched override, regardless of variable. Resolved the
same way \code{\link[=manual_update_lasa_labels]{manual_update_lasa_labels()}} resolves it, so a correction
originally recorded with \code{wave = "all"} (one row per wave) is found
and removed in full from a single call.}

\item{rebuild}{Logical, default \code{FALSE}. If \code{TRUE}, also replace the
cached \code{variables}/\code{value_labels}/\code{value_labels_harmonized} tables
with fresh copies from the currently-installed package's bundled
database -- always the whole database (there is no per-filecode base
layer to refresh selectively), regardless of any \code{filecode}/\code{wave}/
\code{variable} scope above.}
}
\value{
Invisibly, a list describing what happened: \code{filecode}, \code{wave},
\code{variable} (the requested scope, as supplied), \code{overrides_removed}
(how many manual-override rows, across both tables, were discarded),
\code{rebuilt} (whether the base tables were refreshed), and
\code{cache_deleted} (whether the local cache file was removed entirely,
vs. rewritten with what remains).
}
\description{
The inverse of \code{\link[=manual_update_lasa_labels]{manual_update_lasa_labels()}}: discards recorded manual
overrides (entirely, or scoped to a \code{filecode}/\code{wave}/\code{variable}), and/or
rebuilds the cached base tables from whichever bundled database the
currently-installed package ships -- useful after a package update that
adds coverage or corrects a label the local cache would otherwise keep
shadowing indefinitely (see Details).
}
\details{
\code{\link[=manual_update_lasa_labels]{manual_update_lasa_labels()}} persists a \emph{full} snapshot of the label
database to a per-user cache file the first time it's called, not just
the correction itself -- so once that cache exists, it takes precedence
over the package's bundled data on every subsequent \code{\link[=lasa_label_db]{lasa_label_db()}}
call, for every file code, not only the one that was corrected. A later
package update with new or corrected coverage has no effect until that
cache is refreshed. \code{rebuild = TRUE} does that refresh, and (unless a
\code{filecode}/\code{wave}/\code{variable} scope leaves a manual override in place)
also has nothing left to shadow the bundled data with, so the cache
file itself is removed entirely -- meaning any \emph{future} package update
takes effect automatically, with no need to call this again.
}
\examples{
\dontrun{
# Undo every manual override ever recorded locally, keeping whatever
# base data is currently cached:
restore_lasa_labels()

# Undo overrides for just one file code:
restore_lasa_labels(filecode = "046")

# Undo one variable's override:
restore_lasa_labels(filecode = "046", variable = "lphya01")

# Full reset: discard every override AND refresh the base data from the
# currently-installed package (removes the local cache file entirely):
restore_lasa_labels(rebuild = TRUE)
}
}
\seealso{
\code{\link[=manual_update_lasa_labels]{manual_update_lasa_labels()}}, \code{\link[=lasa_label_db]{lasa_label_db()}}
}
