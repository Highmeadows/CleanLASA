% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA022_labels.R
\name{apply_lasa022_labels}
\alias{apply_lasa022_labels}
\title{Apply LASA022 (Raven) SPSS labels}
\usage{
apply_lasa022_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA022 \code{.sav} file,
for example via \code{\link[haven:read_spss]{haven::read_sav()}}. Expected names include \code{brava1},
\code{dravb12}, \code{bmravamis}, and \code{gmrabmis}, depending on \code{wave}.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"B"}, \code{"C"}, \code{"D"}, \code{"E"}, \code{"2B"}, \code{"F"},
or \code{"G"}.}

\item{name_corrections}{Optional named character vector for explicit column
name overrides. Names are canonical LASA022 suffixes without the wave
prefix (for example \code{rava1}, \code{mravb12}, or \code{mrabmis}), and values are
actual column names in \code{data}, for example
\code{c(mrava1 = "raven_a_item_1")}.}

\item{to_factor}{Logical. If \code{FALSE} (default), categorical Raven items
remain numeric with SPSS-style value-label attributes. If \code{TRUE}, they
are converted to factors. Codes other than the documented correct
response remain numeric-text levels. Missing-answer counts are also
factored when they have labels unless \code{to_numeric = TRUE}.}

\item{to_numeric}{Logical. If \code{FALSE} (default), missing-answer counts
retain their SPSS-style labels, where documented. If \code{TRUE}, those count
variables become ordinary numeric, negative missing/routing codes become
\code{NA}, and non-negative counts are retained. Raven item responses remain
categorical. This takes precedence over \code{to_factor} for counts.}

\item{standardize_names}{Logical. If \code{FALSE} (default), source column names
are retained except for renaming requested through \code{split_wavecode}. If
\code{TRUE}, matched LASA022 columns are renamed to their canonical lowercase
suffixes with the wave code removed; \code{"respnr"} is standardized; and
\code{split_wavecode} is always treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{FALSE} (default) and
\code{standardize_names = FALSE}, no wave-code splitting occurs. If \code{TRUE},
matched columns are renamed with their wave prefix removed and a new
\code{"LASA_wave"} column filled with \code{wave} is inserted immediately after
the respondent-number column. It is always treated as \code{TRUE} when
\code{standardize_names = TRUE}.}
}
\value{
\code{data}, with variable/value-label attributes attached to every
matched LASA022 column, optional factor/numeric conversion and canonical
renaming, and attributes preserving the original SPSS coding. A
\code{"LASA_wave"} column is added when requested. The generic name-matching
audit is attached as \code{attr(data, "label_report")} and can be retrieved
with \code{\link[=lasa_label_report]{lasa_label_report()}}.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to the raw Raven's Coloured Progressive Matrices
variables documented in LASA022. Every supported wave contains 12 Section
A items, 12 Section B items, and three missing-answer counts. Waves B
through D used the main interview; wave E onward used the medical
interview and added \code{m} to the variable suffixes.
}
\details{
For each Raven item the codebook labels only the correct response code,
which differs by item. Other observed response codes are deliberately not
assigned inferred \code{"incorrect"} labels. With \code{to_factor = TRUE}, those
unlabelled responses remain numeric-text factor levels. Missing-answer
variables are counts; with \code{to_numeric = TRUE}, their documented negative
routing/missing codes become \code{NA} and non-negative counts are retained.
Numeric conversion takes precedence over factor conversion for the count
variables, while the individual Raven items remain categorical.

The function preserves two irregularities printed in the source PDF:
Section B item 2 is labelled \code{"C. Raven Section A: item 2"} in every wave,
and the LAS2B022 value label for \code{bmrava9} refers to
\code{"C/DRMCRAV in LAS2B222"}. Neither wording is silently corrected.

The source PDF also documents LASA222 constructed totals and
reason-missing variables. Those variables are intentionally outside
LASA022 and are not changed by this function.

Column matching tries, in order: (1) an explicit override in
\code{name_corrections}, (2) an exact case-sensitive name match, and (3) a
case-insensitive exact match. Variables documented for the selected wave
but absent from \code{data} are left untouched and recorded as \code{"not found"} in
the generic matching audit.

\code{name_corrections}, \code{to_factor}, \code{to_numeric}, \code{standardize_names}, and
\code{split_wavecode} are part of the parameter contract shared by every
\verb{apply_*_labels()} function in this package. Regardless of conversion,
every matched column keeps its original SPSS value coding in
\code{attr(x, "original_labels")} (when labels exist) and
\code{attr(x, "original_values")}. The \code{"respnr"} column is matched in any
capitalization and, when \code{standardize_names = TRUE}, renamed to
\code{"respnr"}.
}
\examples{
dat <- data.frame(
  RespNr = 1:4,
  gmrava1 = c(-2, -1, 4, 6),
  gmravamis = c(-2, 0, 1, 3)
)
dat <- apply_lasa022_labels(dat, wave = "G")
attr(dat$gmrava1, "labels")

numeric_dat <- apply_lasa022_labels(dat, wave = "G", to_numeric = TRUE)
numeric_dat$gmravamis
}
\seealso{
\code{\link[=apply_lasa021_labels]{apply_lasa021_labels()}}, \code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
