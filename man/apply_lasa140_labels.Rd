% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA140_labels.R
\name{apply_lasa140_labels}
\alias{apply_lasa140_labels}
\title{Apply LASA140 (Parental data) SPSS labels}
\usage{
apply_lasa140_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LAS2B140 or LAS3B140
\code{.sav} file, for example via \code{\link[haven:read_spss]{haven::read_sav()}}, containing any of
\code{bqmo_ch}, \code{bqfa_ch}, \code{bqmo_emp}, \code{bqmo_vol}, \code{bqparsep}, or \code{bqparsey}.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"2B"} or \code{"3B"}.}

\item{name_corrections}{Optional named character vector mapping canonical
suffixes without the \code{b} prefix (\code{qmo_ch}, \code{qfa_ch}, \code{qmo_emp},
\code{qmo_vol}, \code{qparsep}, or \code{qparsey}) to actual column names in \code{data}.}

\item{to_factor}{Logical. If \code{TRUE}, categorical variables with documented
value labels are converted to factors. Observed unlabelled codes remain
numeric-text levels.}

\item{to_numeric}{Logical. If \code{TRUE}, the parental separation year is
restored to plain numeric and negative values are replaced by \code{NA}.}

\item{standardize_names}{Logical. If \code{TRUE}, matched columns are renamed to
canonical lowercase suffixes without the wave prefix, \code{respnr} is
standardized, and \code{split_wavecode} is treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{TRUE}, matched columns have the \code{b}
prefix removed and a \code{LASA_wave} column containing the selected wave is
inserted immediately after the respondent-number column.}
}
\value{
\code{data}, with LASA140 variable/value labels, optional conversion and
standardized naming, preserved original SPSS coding, the \code{LASA_wave}
provenance attribute, and a \code{label_report} matching audit.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to the six parental-data variables documented in
LASA140 for replenishment waves 2B and 3B.
}
\details{
Both waves contain parental church membership, maternal employment and
volunteer work during the respondent's youth, parental separation/divorce,
and year of separation/divorce. The church-denomination coding differs
materially: 2B uses eight substantive categories, whereas 3B uses ten and
distinguishes several Reformed and Protestant denominations. Both waves
store these variables with the \code{b} prefix.

Church membership and yes/no variables are categorical. The separation
year is numeric; with \code{to_numeric = TRUE}, its documented -2 and -1 missing
codes become \code{NA}. With \code{to_factor = TRUE}, categorical fields become
factors and observed unlabelled codes remain numeric-text levels.

Column matching tries an explicit \code{name_corrections} override, an exact
case-sensitive match, then a case-insensitive exact match. Original SPSS
labels and values are preserved. Standardized naming optionally removes
the \code{b} prefix, standardizes \code{respnr}, and adds the selected \code{LASA_wave}.
}
\examples{
dat <- data.frame(RespNr = 1:2, bqmo_ch = c(1, 5), bqparsey = c(-2, 1990))
dat <- apply_lasa140_labels(dat, wave = "2B", to_numeric = TRUE)
attr(dat$bqmo_ch, "labels")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
