% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA606_labels.R
\name{apply_lasa606_labels}
\alias{apply_lasa606_labels}
\title{Apply LASA606 (\link{PROXY} depressive affect) SPSS labels}
\usage{
apply_lasa606_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA606 \code{.sav} file.}

\item{wave}{Character scalar: \code{"C"}, \code{"D"}, or \code{"E"}; matching is
case-insensitive.}

\item{name_corrections}{Optional named character vector mapping canonical
suffixes without the wave prefix to actual column names in \code{data}.}

\item{to_factor}{Logical. Convert labelled categorical variables to factors.}

\item{to_numeric}{Logical. Convert the eligible depressive-affect score to
plain numeric and replace its negative missing code with \code{NA}.}

\item{standardize_names}{Logical. Standardize matched names and \code{respnr},
remove the wave prefix, and add \code{LASA_wave}.}

\item{split_wavecode}{Logical. Remove wave prefixes from matched names and
add \code{LASA_wave}; implied by \code{standardize_names = TRUE}.}
}
\value{
\code{data} with LASA606 metadata, optional conversions and renaming, a
\code{LASA_wave} attribute, and a \code{label_report} matching audit.
}
\description{
Attaches the variable and value labels documented for LASA606 proxy
telephone-interview data in waves C, D, and E. Each wave contains four
depressive-affect items scored from rarely or never (0) to mostly or
always (3), followed by the depressive-affect total score.
}
\details{
Matching uses an explicit \code{name_corrections} entry, an exact match, then a
case-insensitive exact match. \code{to_factor = TRUE} converts the four
categorical items to factors while retaining observed undocumented codes.
\code{to_numeric = TRUE} restores the total score to plain numeric and replaces
its documented negative missing code with \code{NA}. Original values and labels
remain available in \code{original_values} and \code{original_labels} attributes.
}
\examples{
dat <- data.frame(RespNr = 1:2, ctpces01 = c(0, 2), ctpcedep = c(-1, 5))
apply_lasa606_labels(dat, wave = "C")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
