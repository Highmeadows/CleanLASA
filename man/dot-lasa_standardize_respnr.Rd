% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lasa_io.R
\name{.lasa_standardize_respnr}
\alias{.lasa_standardize_respnr}
\title{Standardize the "respnr" (respondent number) column name}
\usage{
.lasa_standardize_respnr(data, standardize_names = FALSE)
}
\arguments{
\item{data}{A data frame or tibble that may contain a respondent-number
column.}

\item{standardize_names}{Logical. If \code{TRUE}, a matched respnr column is
renamed to \code{"respnr"}.}
}
\value{
A list with \code{data} (possibly renamed), \code{matched_name} (the
original column name found, or \code{NA_character_} if none was found),
\code{method} (\code{"exact"}, \code{"case-insensitive exact"}, or \code{"not found"}), and
\code{respnr_name} (the column's name in the returned \code{data}: \code{"respnr"}
when it was renamed, otherwise \code{matched_name}).
}
\description{
Internal helper shared by every \verb{apply_*_labels()} function in this
package. The LASA respondent-number identifier column is present in
essentially every LASA data file but is spelled inconsistently across
files/waves (e.g. \code{"RespNr"}, \code{"RESPNR"}, \code{"respnr"}). This helper finds
it by an exact, then case-insensitive, name match and -- only when
\code{standardize_names = TRUE} -- renames it to the canonical lowercase
spelling \code{"respnr"}.
}
\keyword{internal}
