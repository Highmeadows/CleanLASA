% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA196_labels.R
\name{apply_lasa196_labels}
\alias{apply_lasa196_labels}
\title{Apply LASA196 (Health literacy) SPSS labels}
\usage{
apply_lasa196_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA196 \code{.sav} file.}

\item{wave}{Character scalar identifying the LASA wave. Only \code{"K"} is
documented; matching is case-insensitive.}

\item{name_corrections}{Optional named character vector mapping canonical
suffixes without the wave prefix (for example \code{mhlq1}) to actual columns.}

\item{to_factor}{Logical. Convert labelled categorical items to factors.}

\item{to_numeric}{Logical. Accepted for the shared interface; LASA196 has
no numeric variables eligible for conversion.}

\item{standardize_names}{Logical. Standardize matched names and \code{respnr},
remove the wave prefix, and add \code{LASA_wave}.}

\item{split_wavecode}{Logical. Remove the wave prefix and add \code{LASA_wave};
implied by \code{standardize_names = TRUE}.}
}
\value{
\code{data} with LASA196 metadata, optional conversion and renaming, the
\code{LASA_wave} provenance attribute, and a \code{label_report} matching audit.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to the 16 Health Literacy Questionnaire items
documented for LASA196 in wave K. Items cover finding, understanding,
judging, and applying treatment, prevention, and health-promotion
information. All use the documented four-level ease/difficulty response
scale and the \code{-1} not-asked code.
}
\details{
Column matching tries an explicit \code{name_corrections} override, an exact
case-sensitive match, and a case-insensitive exact match, in that order.
\code{to_factor = TRUE} converts matched items to factors while retaining
observed undocumented codes as numeric-text levels. LASA196 has no numeric
variables eligible for \code{to_numeric}. Original values and labels are
preserved, and optional standardized naming removes the wave prefix,
standardizes \code{respnr}, and adds \code{LASA_wave}.
}
\examples{
dat <- data.frame(RespNr = 1:2, KMHLQ1 = c(-1, 1), kmhlq16 = c(2, 4))
apply_lasa196_labels(dat, wave = "K")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
