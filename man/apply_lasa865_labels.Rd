% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA865_labels.R
\name{apply_lasa865_labels}
\alias{apply_lasa865_labels}
\title{Apply LASA865 (Thyroid Function) SPSS labels}
\usage{
apply_lasa865_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASAC865 \code{.sav} file.}

\item{wave}{Character scalar: \code{"C"}; matching is case-insensitive.}

\item{name_corrections}{Optional named character vector mapping canonical
suffixes without the wave prefix to actual column names in \code{data}.}

\item{to_factor}{Logical. Convert value-labelled variables to factors.}

\item{to_numeric}{Logical. Restore laboratory measures to plain numeric and
replace negative missing codes with \code{NA}.}

\item{standardize_names}{Logical. Standardize matched names and \code{respnr},
remove the wave prefix, and add \code{LASA_wave}.}

\item{split_wavecode}{Logical. Remove wave prefixes from matched names and
add \code{LASA_wave}; implied by \code{standardize_names = TRUE}.}
}
\value{
\code{data} with LASA865 metadata, optional conversions and renaming, a
\code{LASA_wave} attribute, and a \code{label_report} matching audit.
}
\description{
Attaches the variable and value labels documented for the LASAC865
thyroid-function laboratory file. The wave-C inventory contains
thyroid-stimulating hormone, free T4, and free T3. Code \code{-1} denotes no
determination; free T4 and free T3 additionally use \code{-2} routing codes
referring to the preceding thyroid measurements.
}
\details{
Matching tries \code{name_corrections}, an exact name, then a case-insensitive
exact name. \code{to_numeric = TRUE} restores the laboratory measures to plain
numeric and replaces negative missing codes with \code{NA}. \code{to_factor = TRUE}
converts value-labelled columns to factors when numeric restoration is not
requested. Original values and labels are preserved in attributes.
}
\examples{
dat <- data.frame(RespNr = 1:2, cmtsh2 = c(-1, 2.4))
apply_lasa865_labels(dat, wave = "C", to_numeric = TRUE)
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
