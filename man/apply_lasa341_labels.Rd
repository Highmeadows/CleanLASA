% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA341_labels.R
\name{apply_lasa341_labels}
\alias{apply_lasa341_labels}
\title{Apply LASA341 (Need for Affiliation Score) SPSS labels}
\usage{
apply_lasa341_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA341 \code{.sav} file,
containing \code{bqan} for wave 2B or \code{gqan} for wave G.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"2B"} or \code{"G"}.}

\item{name_corrections}{Optional named character vector mapping the
canonical LASA341 suffix without the wave prefix (\code{qan}) to the actual
column name in \code{data}.}

\item{to_factor}{Logical. If \code{TRUE}, the score is converted to a factor.
Observed unlabelled interior scores remain numeric-text levels.}

\item{to_numeric}{Logical. If \code{TRUE}, the score is restored to plain
numeric and negative values are replaced by \code{NA}.}

\item{standardize_names}{Logical. If \code{TRUE}, the matched column is renamed
to canonical lowercase \code{qan}, \code{respnr} is standardized, and
\code{split_wavecode} is treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{TRUE}, the matched column has its wave
prefix removed and a \code{LASA_wave} column containing the selected wave is
inserted immediately after the respondent-number column.}
}
\value{
\code{data}, with the LASA341 variable/value labels, optional conversion
and standardized naming, preserved original SPSS coding, the \code{LASA_wave}
provenance attribute, and a \code{label_report} matching audit.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to the constructed six-item need-for-affiliation
score documented in LASA341 for waves 2B and G.
}
\details{
LASA341 contains one numeric score, \code{bqan} in wave 2B and \code{gqan} in wave
G. The documented range runs from low affiliation need (6) to strong
affiliation need (18), with \code{-1} denoting no valid data. The six source
questionnaire items belong to LASA141 and are intentionally outside this
function's inventory.

With \code{to_numeric = TRUE}, the negative missing code becomes \code{NA};
\code{to_numeric} therefore takes precedence over \code{to_factor}. With
\code{to_factor = TRUE} alone, documented endpoints become factor levels and
observed unlabelled interior scores are retained as numeric-text levels.

Column matching tries, in order: (1) an explicit override in
\code{name_corrections}, (2) an exact case-sensitive match, and (3) a
case-insensitive exact match. Variables absent from \code{data} are recorded as
\code{"not found"} in the matching audit. Original labels and values are
preserved, and optional standardized naming removes the wave prefix,
standardizes \code{respnr}, and adds \code{LASA_wave}.
}
\examples{
dat <- data.frame(RespNr = 1:3, bqan = c(-1, 10, 18))
dat <- apply_lasa341_labels(dat, wave = "2B", to_numeric = TRUE)
attr(dat$bqan, "label")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
