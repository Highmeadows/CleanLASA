% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA044_labels.R
\name{apply_lasa044_labels}
\alias{apply_lasa044_labels}
\title{Apply LASA044 (Contact with health/social services) SPSS labels}
\usage{
apply_lasa044_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA044 \code{.sav} file,
for example via \code{\link[haven:read_spss]{haven::read_sav()}}. Depending on \code{wave}, names include
\code{blst1k01}, \code{glst1c1n}, \code{bfacil}, and \code{ilst2c20}.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"B"}, \code{"C"}, \code{"D"}, \code{"E"}, \code{"2B"}, \code{"F"},
\code{"G"}, \code{"H"}, \code{"3B"}, \code{"MB"}, \code{"I"}, or \code{"J"}.}

\item{name_corrections}{Optional named character vector for explicit column
name overrides. Names are canonical LASA044 suffixes without the wave
prefix (for example \code{lst1c01}, \code{lst1c1n}, \code{lst2k09}, or \code{facil}), and
values are actual column names in \code{data}.}

\item{to_factor}{Logical. If \code{FALSE} (default), variables retain their
numeric representation with SPSS-style value-label attributes. If
\code{TRUE}, categorical variables are converted to factors using the
documented labels. Observed unlabelled codes remain numeric-text levels.}

\item{to_numeric}{Logical. If \code{TRUE}, family-physician contact-count fields
are restored to plain numeric and negative values are replaced by \code{NA}.
Other LASA044 variables remain categorical. This conversion takes
precedence over \code{to_factor} for eligible count variables.}

\item{standardize_names}{Logical. If \code{FALSE} (default), source column names
are retained except for renaming requested through \code{split_wavecode}. If
\code{TRUE}, matched LASA044 columns are renamed to their canonical lowercase
suffixes with the wave prefix removed; \code{"respnr"} is standardized; and
\code{split_wavecode} is always treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{FALSE} (default) and
\code{standardize_names = FALSE}, no wave-code splitting occurs. If \code{TRUE},
matched columns are renamed with their wave prefix removed and a new
\code{"LASA_wave"} column filled with \code{wave} is inserted immediately after
the respondent-number column. It is always treated as \code{TRUE} when
\code{standardize_names = TRUE}.}
}
\value{
\code{data}, with variable/value-label attributes attached to every
matched LASA044 column, optional factor/numeric conversion and canonical
renaming, and \code{original_labels}/\code{original_values} attributes preserving
the original SPSS coding. A \code{"LASA_wave"} column is added when requested.
The generic name-matching audit is attached as \code{attr(data, "label_report")} and can be retrieved with \code{\link[=lasa_label_report]{lasa_label_report()}}.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to the health- and social-service variables
documented in LASA044. The implementation covers waves B, C, D, E, 2B,
F, G, H, 3B, MB, I, and J.
}
\details{
LASA044 changes substantially across waves. Wave B distinguishes knowing
about a service from contacting it and contains 68 variables. Waves C
through G retain the contact series, with a family-physician contact count
added at G. Waves H and 3B use reduced service lists and revised
termination codes. Wave MB adds four accessibility-facility variables,
and waves I and J introduce social-alarm and group-daycare items. The
documented inventories, ordering, routing references, missing codes, and
wording differences are retained for each wave.

Family-physician contact-count fields are numeric measures with documented
negative missing codes and a labelled upper category at 98. With
\code{to_numeric = TRUE}, these fields are restored to plain numeric and all
observed negative values become \code{NA}; the value 98 remains 98. Other
LASA044 variables are categorical and are unaffected by \code{to_numeric}.
With \code{to_factor = TRUE}, categorical variables become factors and observed
unlabelled codes remain numeric-text levels. For contact counts,
\code{to_numeric} takes precedence when both conversion options are requested.

Column matching tries, in order: (1) an explicit override in
\code{name_corrections}, (2) an exact case-sensitive name match, and (3) a
case-insensitive exact match. Variables documented for the selected wave
but absent from \code{data} are left untouched and recorded as \code{"not found"} in
the generic matching audit.

\code{name_corrections}, \code{to_factor}, \code{to_numeric}, \code{standardize_names}, and
\code{split_wavecode} follow the parameter contract shared by the package's
other \verb{apply_*_labels()} functions. Regardless of conversion, every
matched column keeps its original SPSS coding in
\code{attr(x, "original_labels")} and \code{attr(x, "original_values")}. The
\code{"respnr"} column is matched in any capitalization and, when
\code{standardize_names = TRUE}, renamed to \code{"respnr"}.
}
\examples{
dat <- data.frame(
  RespNr = 1:3,
  glst1c01 = c(-5, 0, 1),
  glst1c1n = c(-2, 3, 98),
  glst2c01 = c(-3, 0, 1)
)
dat <- apply_lasa044_labels(dat, wave = "G", to_numeric = TRUE)
attr(dat$glst1c01, "labels")
}
\seealso{
\code{\link[=apply_lasa039_labels]{apply_lasa039_labels()}}, \code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
