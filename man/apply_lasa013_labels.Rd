% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA013_labels.R
\name{apply_lasa013_labels}
\alias{apply_lasa013_labels}
\title{Apply LASA013 (Residence in 1944) SPSS labels}
\usage{
apply_lasa013_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASAB013 \code{.sav} file,
for example via \code{\link[haven:read_spss]{haven::read_sav()}}. Expected wave-prefixed names are
\code{bcity}, \code{bprovinc}, \code{bcountry}, and \code{bremarks}.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. LASA013 is documented only for \code{"B"}.}

\item{name_corrections}{Optional named character vector for explicit column
name overrides. Names are canonical LASA013 suffixes without the wave
prefix (\code{city}, \code{provinc}, \code{country}, or \code{remarks}), and values are the
actual column names in \code{data}, for example
\code{c(country = "B_COUNTRY")}.}

\item{to_factor}{Logical. If \code{FALSE} (default), all four categorical
variables remain numeric with SPSS-style value-label attributes. If
\code{TRUE}, they are converted to factors using their documented value
labels as levels. Observed city or country codes omitted by the
abbreviated PDF classification are retained as numeric-text levels.}

\item{to_numeric}{Logical. Retained for the common apply-label parameter
contract. LASA013 contains no documented count or continuous variable,
so this argument does not change any LASA013 column.}

\item{standardize_names}{Logical. If \code{FALSE} (default), source column names
are retained except for renaming requested through \code{split_wavecode}. If
\code{TRUE}, matched LASA013 columns are renamed to their canonical lowercase
suffixes with the wave code removed (\code{city}, \code{provinc}, \code{country}, and
\code{remarks}); \code{"respnr"} in any capitalization is renamed to \code{"respnr"};
and \code{split_wavecode} is always treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{FALSE} (default) and
\code{standardize_names = FALSE}, no wave-code splitting occurs. If \code{TRUE},
matched columns are renamed with the B prefix removed (for example,
\code{bcountry} becomes \code{country}) and a new \code{"LASA_wave"} column filled with
\code{"B"} is inserted immediately after the respondent-number column. It is
always treated as \code{TRUE} when \code{standardize_names = TRUE}.}
}
\value{
\code{data}, with variable- and value-label attributes attached to every
matched LASA013 column, optional factor conversion and canonical
renaming, and \code{original_labels}/\code{original_values} attributes preserving
the original SPSS coding. A \code{"LASA_wave"} column is added after
\code{"respnr"} when \code{standardize_names = TRUE} or \code{split_wavecode = TRUE}.
The generic variable-name audit is attached as \code{attr(data, "label_report")} and can be retrieved with \code{\link[=lasa_label_report]{lasa_label_report()}}.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to the four variables documented for the LASAB013
residence-in-1944 file. By default, matched variables remain numeric.
Optional arguments allow converting categorical variables to factors,
correcting mismatched column names, and standardizing names to the
canonical LASA documentation spelling.
}
\details{
LASA013 is documented only for wave B. Its codebook prints the complete
province and special-remarks classifications, but abbreviates the city
classification between AALSMEER (1) and ZWOLLE-KERSPEL (565), and the
country classification between ANTILLES (1) and VIETNAM (26), with
ellipses. This function therefore attaches only the city and country
labels explicitly recorded in \code{LASA013_varinfo.pdf}; it does not invent
the omitted intermediate labels. When \code{to_factor = TRUE}, an observed
omitted code is retained as a numeric-text factor level instead of being
converted to \code{NA}.

Column matching tries, in order: (1) an explicit override in
\code{name_corrections}, (2) an exact case-sensitive name match, and (3) a
case-insensitive exact match. Variables documented for LASAB013 but absent
from \code{data} are left untouched and recorded as \code{"not found"} in the
generic matching audit.

\code{name_corrections}, \code{to_factor}, \code{to_numeric}, \code{standardize_names}, and
\code{split_wavecode} are part of the parameter contract shared by every
\verb{apply_*_labels()} function in this package. Regardless of
\code{to_factor}/\code{to_numeric}, every matched column keeps its original SPSS
value coding in \code{attr(x, "original_labels")} and
\code{attr(x, "original_values")}. The \code{"respnr"} column is matched in any
capitalization and, when \code{standardize_names = TRUE}, renamed to
\code{"respnr"}.
}
\examples{
dat <- data.frame(
  RespNr = 1:3,
  BCITY = c(-4, 1, 565),
  BPROVINC = c(-2, 8, 12),
  BCOUNTRY = c(-2, 1, 26),
  BREMARKS = c(-2, 1, 14)
)
dat <- apply_lasa013_labels(dat, wave = "B")
attr(dat$BCITY, "label")
attr(dat$BPROVINC, "labels")

dat_factor <- apply_lasa013_labels(
  dat,
  wave = "B",
  to_factor = TRUE,
  standardize_names = TRUE
)
lasa_label_report(dat_factor, problems_only = TRUE)
}
\seealso{
\code{\link[=apply_lasa012_labels]{apply_lasa012_labels()}}, \code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
