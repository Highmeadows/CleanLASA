% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA028_labels.R
\name{apply_lasa028_labels}
\alias{apply_lasa028_labels}
\title{Apply LASA028 (Perceived self-efficacy/ALCOS) SPSS labels}
\usage{
apply_lasa028_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA028 \code{.sav} file,
for example via \code{\link[haven:read_spss]{haven::read_sav()}}, containing wave-prefixed \code{alcos01}
through \code{alcos12} and up to four missing-count variables.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"B"}, \code{"C"}, \code{"D"}, \code{"E"}, \code{"2B"}, \code{"F"},
\code{"G"}, \code{"H"}, \code{"3B"}, \code{"I"}, \code{"J"}, or \code{"K"}.}

\item{name_corrections}{Optional named character vector for explicit column
name overrides. Names are canonical LASA028 suffixes without the wave
prefix (for example \code{alcos01}, \code{alcmv}, or \code{compmv}), and values are
actual column names in \code{data}, for example
\code{c(alcos01 = "plans_item")}.}

\item{to_factor}{Logical. If \code{FALSE} (default), categorical variables
remain numeric with SPSS-style value-label attributes. If \code{TRUE}, they
are converted to factors using their documented labels. An observed
value without a label remains a numeric-text factor level. Missing-value
counts are also factored unless \code{to_numeric = TRUE}.}

\item{to_numeric}{Logical. If \code{FALSE} (default), the four missing-value
counts retain their SPSS-style missing/routing label. If \code{TRUE}, they
become ordinary numeric, negative codes become \code{NA}, and non-negative
counts are retained. ALCOS items remain categorical. This takes
precedence over \code{to_factor} for count variables.}

\item{standardize_names}{Logical. If \code{FALSE} (default), source column names
are retained except for renaming requested through \code{split_wavecode}. If
\code{TRUE}, matched LASA028 columns are renamed to their canonical lowercase
suffixes with the wave code removed; \code{"respnr"} is standardized; and
\code{split_wavecode} is always treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{FALSE} (default) and
\code{standardize_names = FALSE}, no wave-code splitting occurs. If \code{TRUE},
matched columns are renamed with their wave prefix removed and a new
\code{"LASA_wave"} column filled with \code{wave} is inserted immediately after
the respondent-number column. It is always treated as \code{TRUE} when
\code{standardize_names = TRUE}.}
}
\value{
\code{data}, with variable/value-label attributes attached to every
matched LASA028 column, optional factor/numeric conversion and canonical
renaming, and \code{original_labels}/\code{original_values} attributes preserving
the original SPSS coding. A \code{"LASA_wave"} column is added when requested.
The generic name-matching audit is attached as \code{attr(data, "label_report")} and can be retrieved with \code{\link[=lasa_label_report]{lasa_label_report()}}.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to the 12 ALCOS perceived-self-efficacy items and
four missing-value counts documented in LASA028. All 12 waves use the same
item wording and five-point agreement scale; the -2 routing metadata is
expanded to the wave-specific reason-missing variable and LASA228 file.
}
\details{
The source prints the neutral response as
\code{"no disagreement/agreement"} for wave B and
\code{"no disagreement/ agreement"} for later waves. This function preserves
that spacing difference exactly.

By default, matched variables remain numeric with their SPSS-style labels.
With \code{to_factor = TRUE}, they become factors using the documented response
text; observed unlabelled codes remain numeric-text levels. The four
missing-value variables are counts: with \code{to_numeric = TRUE}, their -2
missing/routing code becomes \code{NA} and non-negative counts are retained.
ALCOS item responses remain categorical, and numeric conversion takes
precedence over factor conversion for the count variables.

The source PDF also documents LASA228 constructed scale scores and
reason-missing variables. Those variables are intentionally outside
LASA028 and are not changed by this function.

Column matching tries, in order: (1) an explicit override in
\code{name_corrections}, (2) an exact case-sensitive name match, and (3) a
case-insensitive exact match. Variables documented for the selected wave
but absent from \code{data} are left untouched and recorded as \code{"not found"} in
the generic matching audit.

\code{name_corrections}, \code{to_factor}, \code{to_numeric}, \code{standardize_names}, and
\code{split_wavecode} are part of the parameter contract shared by every
\verb{apply_*_labels()} function in this package. Regardless of conversion,
every matched column keeps its original SPSS value coding in
\code{attr(x, "original_labels")} and \code{attr(x, "original_values")}. The
\code{"respnr"} column is matched in any capitalization and, when
\code{standardize_names = TRUE}, renamed to \code{"respnr"}.
}
\examples{
dat <- data.frame(
  RespNr = 1:4,
  galcos01 = c(-2, 1, 3, 5),
  galcmv = c(-2, 0, 1, 4)
)
dat <- apply_lasa028_labels(dat, wave = "G")
attr(dat$galcos01, "labels")
}
\seealso{
\code{\link[=apply_lasa026_labels]{apply_lasa026_labels()}}, \code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
