% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA173_labels.R
\name{apply_lasa173_labels}
\alias{apply_lasa173_labels}
\title{Apply LASA173 (existential loneliness) SPSS labels}
\usage{
apply_lasa173_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASAJ173 \code{.sav} file.
Documented names are \code{jmexlo1}, \code{jmexlo3}, \code{jmexlo6}, and the remaining
listed \code{jmexlo} items through \code{jmexlo31}.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. Must be \code{"J"}.}

\item{name_corrections}{Optional named character vector mapping canonical
LASA173 suffixes without the wave prefix (for example, \code{mexlo1}) to
actual column names in \code{data}.}

\item{to_factor}{Logical. If \code{TRUE}, variables with documented value labels
are converted to factors. Observed unlabelled codes remain numeric-text
levels.}

\item{to_numeric}{Logical. Accepted for the shared interface. LASA173 has
no documented continuous numeric variables.}

\item{standardize_names}{Logical. If \code{TRUE}, matched columns are renamed to
canonical lowercase suffixes without the wave prefix, \code{respnr} is
standardized, and \code{split_wavecode} is treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{TRUE}, matched columns have the wave
prefix removed and a \code{LASA_wave} column is inserted after \code{respnr}.}
}
\value{
\code{data}, with LASA173 labels, optional factor conversion and
standardized naming, preserved original SPSS coding, the \code{LASA_wave}
provenance attribute, and a \code{label_report} matching audit.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to the 16 existential-loneliness items documented in
LASA173 for wave J. Each label contains the English item followed by its
Dutch wording. All items share the documented five-point response scale
from emphatic no to emphatic yes, plus missing/status codes.
}
\details{
All LASA173 variables are categorical and therefore none is converted by
\code{to_numeric = TRUE}. With \code{to_factor = TRUE}, documented codes become
factor levels while observed unlabelled codes are retained. Matching tries
\code{name_corrections}, an exact match, then a case-insensitive exact match.
Original labels and values are preserved. Optional standardized naming
removes the \code{j} prefix, standardizes \code{respnr}, and adds \code{LASA_wave}.
}
\examples{
dat <- data.frame(RespNr = 1:2, jmexlo1 = c(1, 5), jmexlo8 = c(2, 4))
apply_lasa173_labels(dat, wave = "J", to_factor = TRUE)
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
