% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA104_labels.R
\name{apply_lasa104_labels}
\alias{apply_lasa104_labels}
\title{Apply LASA104 (Perceived stress) SPSS labels}
\usage{
apply_lasa104_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA104 \code{.sav} file.
Documented names are \code{hqpss01} through \code{hqpss10} in H and \code{bqpss01}
through \code{bqpss10} in 3B.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"H"} or \code{"3B"}.}

\item{name_corrections}{Optional named character vector mapping canonical
suffixes without the wave prefix (for example \code{qpss01} or \code{qpss10}) to
actual column names in \code{data}.}

\item{to_factor}{Logical. If \code{TRUE}, matched variables are converted to
factors using the documented labels; observed unlabelled codes remain
numeric-text levels.}

\item{to_numeric}{Logical. Accepted for interface consistency; LASA104 has
no variables eligible for numeric conversion.}

\item{standardize_names}{Logical. If \code{TRUE}, matched columns are renamed to
canonical lowercase suffixes, \code{respnr} is standardized, and
\code{split_wavecode} is treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{TRUE}, matched names have their \code{h} or
\code{b} wave prefix removed and a \code{LASA_wave} column is inserted immediately
after the respondent-number column.}
}
\value{
\code{data}, with LASA104 labels, optional factor conversion and name
standardization, preserved original SPSS coding, the \code{LASA_wave}
provenance attribute, and a \code{label_report} matching audit.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to the ten perceived-stress questionnaire items
documented in LASA104 for waves H and 3B.
}
\details{
All ten items use the same response scale from never to very often, with
code -1 for no answer. The perceived-stress total score shown in the same
source PDF belongs to filecode LASA304 and is deliberately not included in
this LASA104 function.

LASA104 variables are categorical, so \code{to_numeric} is accepted for the
shared interface but does not convert them. With \code{to_factor = TRUE},
matched variables become factors using the documented response labels;
observed unlabelled codes remain numeric-text levels.

Column matching tries an explicit \code{name_corrections} override, an exact
case-sensitive name, then a case-insensitive exact name. Every matched
column preserves its original SPSS coding in \code{original_labels} and
\code{original_values}. Requested name standardization removes the wave prefix,
standardizes \code{respnr}, and inserts \code{LASA_wave}.
}
\examples{
dat <- data.frame(hqpss01 = c(-1, 0, 4), hqpss04 = c(1, 2, 3))
dat <- apply_lasa104_labels(dat, wave = "H")
attr(dat$hqpss01, "labels")
}
\seealso{
\code{\link[=apply_lasa304_labels]{apply_lasa304_labels()}}, \code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
