% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA073_labels.R
\name{apply_lasa073_labels}
\alias{apply_lasa073_labels}
\title{Apply LASA073 (Loneliness) SPSS labels}
\usage{
apply_lasa073_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA073 \code{.sav} file.}

\item{wave}{Character scalar identifying \code{"B"}, \code{"C"}, \code{"D"}, \code{"E"},
\code{"2B"}, \code{"F"}, \code{"G"}, \code{"H"}, \code{"3B"}, \code{"MB"}, \code{"I"}, or \code{"J"}.}

\item{name_corrections}{Optional named character vector mapping canonical
suffixes such as \code{lo1}, \code{lo11}, \code{losom}, \code{lorat}, or \code{lo_mode} to actual
names in \code{data}.}

\item{to_factor}{Logical. Convert matched categorical variables to factors
using the documented labels; undocumented observed codes remain levels.}

\item{to_numeric}{Logical. Accepted for interface consistency; LASA073 has
no eligible numeric variables.}

\item{standardize_names}{Logical. Rename matched columns to lowercase
canonical suffixes, standardize \code{respnr}, and imply wave splitting.}

\item{split_wavecode}{Logical. Remove the wave prefix from matched names and
insert \code{LASA_wave} immediately after the respondent-number column.}
}
\value{
The labelled data, with requested factor conversion and canonical
names, preserved original coding, a \code{LASA_wave} attribute, and
\code{label_report}.
}
\description{
Attaches the bilingual variable labels and SPSS-style value labels for the
De Jong Gierveld loneliness items documented in LASA073. Waves B, C, D, and
E contain the eleven scale items; B additionally documents interview mode.
Waves F, G, H, 3B, MB, I, and J add a direct loneliness item and loneliness
self-rating. Although LAS2B073 is named in the codebook header, the PDF does
not document any LAS2B073 raw variables; wave \code{"2B"} is therefore accepted
with an empty file-specific inventory rather than inferred metadata.
}
\details{
All documented LASA073 variables are categorical. \code{to_numeric} is accepted
for the shared interface but does not convert them. Matching tries explicit
corrections, exact names, and case-insensitive exact names. Factor
conversion, standardized names, respondent-number handling, wave splitting,
preserved original coding, and \code{label_report} use the shared engine.

The same PDF also documents derived scale filecode LASA273. Those variables
are intentionally excluded here and belong to \code{apply_lasa273_labels()}.
}
\examples{
dat <- data.frame(blo1 = c(-4, 1, 3), blo2 = c(1, 2, 3))
dat <- apply_lasa073_labels(dat, wave = "B")
attr(dat$blo1, "labels")
}
\seealso{
\code{\link[=apply_lasa272_labels]{apply_lasa272_labels()}}, \code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
