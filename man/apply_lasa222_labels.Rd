% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA222_labels.R
\name{apply_lasa222_labels}
\alias{apply_lasa222_labels}
\title{Apply LASA222 (Constructed Raven scores) SPSS labels}
\usage{
apply_lasa222_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA222 \code{.sav} file.}

\item{wave}{Character scalar identifying B, C, D, E, 2B, F, or G.}

\item{name_corrections}{Optional named character vector mapping canonical
suffixes for the selected wave to actual columns.}

\item{to_factor}{Logical. Convert labelled categorical variables to factors
unless numeric restoration takes precedence.}

\item{to_numeric}{Logical. Restore score variables to plain numeric and
replace negative missing codes with \code{NA}.}

\item{standardize_names}{Logical. Standardize names and \code{respnr}, remove
wave prefixes, and add \code{LASA_wave}.}

\item{split_wavecode}{Logical. Remove wave prefixes and add \code{LASA_wave};
implied by \code{standardize_names = TRUE}.}
}
\value{
\code{data} with LASA222 metadata, optional conversion and renaming, the
\code{LASA_wave} attribute, and a \code{label_report} audit.
}
\description{
Attaches SPSS-style labels to the constructed Raven Coloured Progressive
Matrices scores in LASA222: section A, section B, total score, and reason
fields. LASA222 is documented for waves B, C, D, E, 2B, F, and G.
}
\details{
The inventory contains four variables in B through 2B and five in F and G,
where separate reason fields are supplied for section B and the complete
test. The codebook documents wave-specific names and coding, including
\verb{na, see BRMCRAV} in B, physical/visual wording in B and C, a \code{-1} linked
reason in F and G, and an additional code 0 in G. Score variables are
numeric and \code{to_numeric = TRUE} replaces their negative missing codes with
\code{NA}; reason variables remain categorical. Matching, factor conversion,
original-value preservation, name standardization, \code{respnr}, \code{LASA_wave},
and the \code{label_report} follow the shared label engine.
}
\examples{
dat <- data.frame(RespNr = 1:2, dravatot = c(-2, 10), drmcrav = c(3, -2))
apply_lasa222_labels(dat, wave = "D", to_numeric = TRUE)
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
