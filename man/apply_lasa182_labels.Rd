% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA182_labels.R
\name{apply_lasa182_labels}
\alias{apply_lasa182_labels}
\title{Apply LASA182 (thyroid, back pain, bedridden) SPSS labels}
\usage{
apply_lasa182_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA182 \code{.sav} file,
for example via \code{\link[haven:read_spss]{haven::read_sav()}}.}

\item{wave}{Character scalar identifying the LASA wave. One of \code{"C"},
\code{"D"}, \code{"E"}, \code{"F"}, \code{"G"}, \code{"H"}, or \code{"3B"}; matching is
case-insensitive.}

\item{name_corrections}{Optional named character vector. Names are
canonical suffixes without a wave prefix (for example \code{mbackp}) and
values are actual column names in \code{data}.}

\item{to_factor}{Logical. Convert labelled categorical variables to factors
using their documented value labels.}

\item{to_numeric}{Logical. Accepted for the shared interface; LASA182 has
no numeric variables eligible for conversion.}

\item{standardize_names}{Logical. Rename matched columns to lowercase
suffixes without the wave prefix, standardize the respondent-number
column to \code{respnr}, and add \code{LASA_wave}.}

\item{split_wavecode}{Logical. Remove the wave prefix from matched column
names and add \code{LASA_wave}. Always enabled by \code{standardize_names = TRUE}.}
}
\value{
\code{data} with documented metadata applied, optional conversion and
renaming, a \code{LASA_wave} attribute, and a matching audit in the
\code{label_report} attribute.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to the thyroid, back-pain, and confinement-to-bed
variables documented for LASA182.
}
\details{
LASA182 is documented for waves C, D, E, F, G, H, and 3B. Thyroid status
occurs in C and D; back-pain occurrence and duration occur in C through E;
and confinement to bed occurs in C, D, F, G, H, and 3B. Wave C additionally
contains indicators for confinement before age 25, after age 25, and during
the past year. Coding and routing-missing values follow the wave-specific
tables in the codebook.

Column matching tries, in order: an explicit \code{name_corrections} override,
an exact case-sensitive match, and a case-insensitive exact match.
\code{to_factor = TRUE} converts matched categorical variables to factors while
retaining observed unlabelled codes as numeric-text levels. LASA182 has no
variables eligible for \code{to_numeric} conversion. Original values and value
labels are preserved in \code{original_values} and \code{original_labels}.
}
\examples{
dat <- data.frame(CMBACKP = c(-1, 1, 2), cmbackpd = c(-2, 1, 3))
dat <- apply_lasa182_labels(dat, wave = "C")
attr(dat$CMBACKP, "labels")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
