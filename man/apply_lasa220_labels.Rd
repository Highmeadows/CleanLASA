% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA220_labels.R
\name{apply_lasa220_labels}
\alias{apply_lasa220_labels}
\title{Apply LASA220 (Digit Span total scores) SPSS labels}
\usage{
apply_lasa220_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LAS3B220 \code{.sav} file.}

\item{wave}{Character scalar identifying wave \code{"3B"}.}

\item{name_corrections}{Optional named character vector mapping canonical
suffixes such as \code{dstot} or \code{rmds} to actual columns.}

\item{to_factor}{Logical. Convert variables carrying value labels to
factors, unless numeric restoration takes precedence.}

\item{to_numeric}{Logical. Restore the five score variables to plain
numeric and replace negative codes with \code{NA}.}

\item{standardize_names}{Logical. Standardize matched names and \code{respnr},
remove the wave prefix, and add \code{LASA_wave}.}

\item{split_wavecode}{Logical. Remove the wave prefix and add \code{LASA_wave};
implied by \code{standardize_names = TRUE}.}
}
\value{
\code{data} with LASA220 metadata, optional conversion and renaming, the
\code{LASA_wave} attribute, and a \code{label_report} matching audit.
}
\description{
Attaches SPSS-style variable and value labels to the six constructed Digit
Span variables documented in the LAS3B220 section of the LASA020 codebook.
The inventory contains the total correct score, forward and backward
correct scores, forward and backward spans, and the reason-missing field.
}
\details{
LASA220 is documented only for wave 3B. The five score variables use the
\code{-2} no-score and \code{-1} too-many-missings codes and are eligible for numeric
restoration: \code{to_numeric = TRUE} removes their value labels and converts
negative values to \code{NA}. The reason field remains categorical.
\code{to_factor = TRUE} applies the documented labels while preserving observed
undocumented values. Matching tries corrections, exact names, and then
case-insensitive exact names. Standardized naming removes the \code{b} prefix,
standardizes \code{respnr}, and adds \code{LASA_wave}.
}
\examples{
dat <- data.frame(RespNr = 1:2, BDSTOT = c(-1, 12), BRMDS = c(3, -2))
apply_lasa220_labels(dat, wave = "3B", to_numeric = TRUE)
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
