% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA121_labels.R
\name{apply_lasa121_labels}
\alias{apply_lasa121_labels}
\title{Apply LASA121 (Social participation: environmental influence) labels}
\usage{
apply_lasa121_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA121 \code{.sav} file.}

\item{wave}{Character scalar identifying \code{"B"}, \code{"C"}, or \code{"D"}; matching
is case-insensitive.}

\item{name_corrections}{Optional named character vector whose names are
canonical suffixes without the wave prefix, such as \code{qsocp01}, and whose
values are actual column names in \code{data}.}

\item{to_factor}{Logical. Convert matched variables to factors using the
documented value labels.}

\item{to_numeric}{Logical. Accepted for interface consistency; LASA121 has
no numeric variables eligible for conversion.}

\item{standardize_names}{Logical. Standardize matched names and respondent
number, remove wave prefixes, and add \code{LASA_wave}.}

\item{split_wavecode}{Logical. Remove wave prefixes from matched names and
add \code{LASA_wave}; implied by \code{standardize_names = TRUE}.}
}
\value{
\code{data} with LASA121 metadata, optional conversion and renaming,
original-coding attributes, \code{LASA_wave}, and a \code{label_report} audit.
}
\description{
Attaches SPSS-style variable and value labels to the three social-
participation variables documented for LASA121. The self-administered
items record how often respondents discuss magazines or newspapers,
discuss life, and make calculations.
}
\details{
LASA121 is documented for waves B, C, and D. Each wave contains the same
three categorical items, coded \code{-1} no answer, \code{1} rarely or never, \code{2}
monthly, and \code{3} weekly or more often. \code{to_factor = TRUE} converts matched
items with this map and retains observed unlabelled codes. No variable is
eligible for \code{to_numeric}.

Matching tries \code{name_corrections}, an exact name, then a case-insensitive
exact name. Original labels and values are retained in \code{original_labels}
and \code{original_values} attributes.
}
\examples{
dat <- data.frame(BQSOCP01 = c(-1, 1, 3), bqsocp03 = c(1, 2, 3))
dat <- apply_lasa121_labels(dat, wave = "B")
attr(dat$BQSOCP01, "labels")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
