% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA069_labels.R
\name{apply_lasa069_labels}
\alias{apply_lasa069_labels}
\title{Apply LASA069 (Ties with country of origin) SPSS labels}
\usage{
apply_lasa069_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASMB069 \code{.sav} file.
Names include \code{bcultid1}, \code{bNLlang1}, \code{bcultdis1}, and \code{bloss1}.}

\item{wave}{Character scalar identifying the LASA wave. Only \code{"MB"} is
documented and accepted; matching is case-insensitive.}

\item{name_corrections}{Optional named character vector mapping canonical
suffixes without the \code{b} prefix (for example \code{cultid1}, \code{nllang1}, or
\code{loss1}) to actual column names in \code{data}.}

\item{to_factor}{Logical. If \code{TRUE}, matched variables are converted to
factors using the documented labels; observed unlabelled codes remain
numeric-text levels.}

\item{to_numeric}{Logical. Accepted for interface consistency; LASA069 has
no numeric variables eligible for conversion.}

\item{standardize_names}{Logical. If \code{TRUE}, matched columns are renamed to
canonical suffixes without the \code{b} prefix, \code{respnr} is standardized, and
\code{split_wavecode} is treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{TRUE}, matched columns have their \code{b}
prefix removed and a \code{LASA_wave} column filled with \code{"MB"} is inserted
after the respondent-number column.}
}
\value{
\code{data}, with LASA069 labels, requested conversion/renaming,
preserved original coding, and a \code{label_report} matching audit.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to the 20 bilingual variables documented in the
LASMB069 ties-with-country-of-origin file.
}
\details{
The file contains seven cultural-identity activities rated from never to
often, three Dutch-language and three Dutch-acquaintance statements rated
from strongly disagree to strongly agree, and seven yes/no belonging,
cultural-loss, and homesickness statements. The English/Dutch variable
labels and all three shared value maps are retained.

All LASA069 variables are categorical. Consequently, \code{to_numeric} is
accepted for consistency with the shared apply-labels interface but does
not convert them. With \code{to_factor = TRUE}, matched variables become
factors and observed unlabelled codes remain numeric-text levels.

Column matching tries, in order: (1) an explicit override in
\code{name_corrections}, (2) an exact case-sensitive name match, and (3) a
case-insensitive exact match. Documented variables absent from \code{data} are
recorded as \code{"not found"} in the generic matching audit.

The five reshaping arguments follow the package's shared apply-labels
contract. Every matched column preserves the original SPSS coding in
\code{original_labels} and \code{original_values}; \code{respnr} is standardized when
requested.
}
\examples{
dat <- data.frame(bcultid1 = c(-1, 1, 4), bNLlang1 = c(1, 3, 4))
dat <- apply_lasa069_labels(dat, wave = "MB")
attr(dat$bcultid1, "labels")
}
\seealso{
\code{\link[=apply_lasa068_labels]{apply_lasa068_labels()}}, \code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
