% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA114_labels.R
\name{apply_lasa114_labels}
\alias{apply_lasa114_labels}
\title{Apply LASA114 (Perceived physical activity; LIVAS) SPSS labels}
\usage{
apply_lasa114_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA114 \code{.sav} file,
for example via \code{\link[haven:read_spss]{haven::read_sav()}}.}

\item{wave}{Character scalar identifying the LASA wave. One of \code{"B"},
\code{"C"}, \code{"D"}, \code{"E"}, \code{"2B"}, or \code{"F"}; matching is case-insensitive.}

\item{name_corrections}{Optional named character vector. Names are
canonical suffixes without a wave prefix (for example \code{qseff01}) and
values are actual column names in \code{data}.}

\item{to_factor}{Logical. Convert labelled categorical variables to
factors using their documented value labels.}

\item{to_numeric}{Logical. Accepted for the shared interface; LASA114 has
no numeric variables eligible for conversion.}

\item{standardize_names}{Logical. Rename matched columns to lowercase
suffixes without the wave prefix, standardize the respondent-number
column to \code{respnr}, and add \code{LASA_wave}.}

\item{split_wavecode}{Logical. Remove the wave prefix from matched column
names and add \code{LASA_wave}. Always enabled by \code{standardize_names = TRUE}.}
}
\value{
\code{data} with documented metadata applied, optional conversion and
renaming, a \code{LASA_wave} attribute, and a matching audit in the
\code{label_report} attribute.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to the ten perceived-physical-activity self-efficacy
items documented for LASA114. The items compare current flexibility,
reaction time, strength, condition, smooth movement, stair climbing, grip,
walking speed, balance, and activity with five years earlier.
}
\details{
LASA114 is documented for waves B, C, D, E, 2B, and F. Each wave contains
the same ten categorical items; only the wave prefix differs. Item-specific
response wording is reproduced from the codebook, including the common
\code{-1} no-answer code. The separately documented LIVAS scale score belongs
to filecode LASA314 and is deliberately not included here.

Column matching tries, in order: an explicit \code{name_corrections} override,
an exact case-sensitive match, and a case-insensitive exact match.
\code{to_factor = TRUE} converts matched categorical items to factors while
retaining observed unlabelled codes as numeric-text levels. LASA114 has no
numeric variables eligible for \code{to_numeric} conversion. Original values
and labels are preserved in \code{original_values} and \code{original_labels}.
}
\examples{
dat <- data.frame(
  RespNr = 1:3,
  BQSEFF01 = c(-1, 1, 5),
  bqseff10 = c(1, 3, 5)
)
dat <- apply_lasa114_labels(dat, wave = "B")
attr(dat$BQSEFF01, "labels")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
