% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA074_labels.R
\name{apply_lasa074_labels}
\alias{apply_lasa074_labels}
\title{Apply LASA074 (Vignettes about help) SPSS labels}
\usage{
apply_lasa074_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from LASAD074.}

\item{wave}{Character scalar identifying wave \code{"D"}; matching is
case-insensitive.}

\item{name_corrections}{Optional named character vector mapping canonical
suffixes without the \code{d} prefix (for example \code{vigseq}, \code{vigm1}, or
\code{vigeqc}) to actual column names in \code{data}.}

\item{to_factor}{Logical. Convert matched value-labelled variables to
factors; observed undocumented codes remain numeric-text levels.}

\item{to_numeric}{Logical. Accepted for interface consistency; LASA074 has
no eligible numeric variables.}

\item{standardize_names}{Logical. Rename matched columns to canonical
lowercase suffixes, standardize \code{respnr}, and imply wave splitting.}

\item{split_wavecode}{Logical. Remove the \code{d} prefix from matched names and
add \code{LASA_wave} after the respondent-number column.}
}
\value{
The labelled data, with requested factor conversion and canonical
names, preserved original coding, \code{LASA_wave}, and \code{label_report}.
}
\description{
Attaches the variable and value labels documented for the 52 LASAD074
vignettes-about-help variables. The D-wave file contains vignette sequence
variables; original and condensed responses about Mary, Sophia, Emily, and
John; and variables describing equality across the four vignette children.
}
\details{
The original response maps preserve vignette-specific structural-missing
codes, including the documented \code{corrected2yes}, equal-to-other-children,
and \code{no@dvig*1} labels. Sequence permutations and decimal-comma equality
codes are reproduced as numeric SPSS values. The routing variable \code{dvigeqr}
is documented as having no value-label map and is left unlabelled by value.

All LASA074 variables are treated as categorical; \code{to_numeric} is accepted
for interface consistency but does not convert them. The total-score
variable in LASAD274, documented in the same PDF, is intentionally excluded
and belongs to \code{apply_lasa274_labels()}.

Matching tries \code{name_corrections}, exact names, then case-insensitive exact
names. Factor conversion and standardized-name/wave splitting preserve the
original coding and use the shared \code{label_report} audit.
}
\examples{
dat <- data.frame(dvigseq1 = c(1, 4), dvigm1 = c(-2, 2))
dat <- apply_lasa074_labels(dat, wave = "D")
attr(dat$dvigm1, "labels")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
