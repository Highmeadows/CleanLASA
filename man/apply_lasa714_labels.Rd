% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA714_labels.R
\name{apply_lasa714_labels}
\alias{apply_lasa714_labels}
\title{Apply LASA714 (\link{RESP} Weight Change) SPSS Labels}
\usage{
apply_lasa714_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA714 \code{.sav} file.}

\item{wave}{Character scalar identifying wave \code{"I"} or \code{"J"}, matched
case-insensitively.}

\item{name_corrections}{Optional named character vector mapping canonical
suffixes without the wave prefix to actual column names in \code{data}.}

\item{to_factor}{Logical. Convert labelled categorical variables to factors.}

\item{to_numeric}{Logical. Restore eligible weight quantities to plain
numeric and replace negative missing codes with \code{NA}.}

\item{standardize_names}{Logical. Standardize matched names and \code{respnr},
remove wave prefixes, and add \code{LASA_wave}.}

\item{split_wavecode}{Logical. Remove wave prefixes and add \code{LASA_wave};
implied by \code{standardize_names = TRUE}.}
}
\value{
\code{data} with LASA714 metadata, optional conversions and renaming,
preserved original coding, a \code{LASA_wave} attribute, and a \code{label_report}.
}
\description{
Attaches the variable and value labels documented for LASA714 respondent
telephone-interview data in waves I and J. The twelve-variable inventory
covers recent weight change, kilograms gained or lost, reported and
constructed reasons for change, appetite, estimated body weight, and
self-reported weight. The appetite reference period is six months in wave I
and seven days in wave J.
}
\details{
The kilogram and self-reported-weight fields are codebook-numeric and are
eligible for \code{to_numeric}; their negative missing codes become \code{NA}.
Categorical fields can be converted with \code{to_factor}, while observed
undocumented codes are retained. Where the PDF prints a routing label
without a numeric code, no numeric value label is inferred.

Matching uses an explicit \code{name_corrections} entry, an exact match, then a
case-insensitive exact match. Original values and labels are preserved.
Standardized naming removes the wave prefix, standardizes \code{respnr}, and
adds \code{LASA_wave}; \code{split_wavecode} removes the prefix without otherwise
standardizing respondent-number spelling.
}
\examples{
dat <- data.frame(RespNr = 1:2, itrvar142 = c(1, 3),
                  itrvar143L = c(-1, 4.5))
apply_lasa714_labels(dat, wave = "I", to_numeric = TRUE)
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
