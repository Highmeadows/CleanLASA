% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA125_labels.R
\name{apply_lasa125_labels}
\alias{apply_lasa125_labels}
\title{Apply LASA125 (Social participation-2) SPSS labels}
\usage{
apply_lasa125_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA125 \code{.sav} file.
Names may include \code{bqsocp04}, \code{cqsocp07}, \code{fqsocp20}, or \code{gqsocp20b}.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"B"}, \code{"C"}, \code{"D"}, \code{"E"}, \code{"2B"},
\code{"F"}, or \code{"G"}.}

\item{name_corrections}{Optional named character vector mapping suffixes
without the wave prefix (for example \code{qsocp06} or \code{qsocp20b}) to actual
names in \code{data}.}

\item{to_factor}{Logical. If \code{TRUE}, categorical variables with documented
value labels are converted to factors.}

\item{to_numeric}{Logical. If \code{TRUE}, documented radio/television hour
fields are restored to plain numeric and negative values become \code{NA}.}

\item{standardize_names}{Logical. If \code{TRUE}, matched names and \code{respnr} are
standardized and \code{split_wavecode} is treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{TRUE}, matched names lose the wave prefix
and a \code{LASA_wave} column is inserted after \code{respnr}.}
}
\value{
\code{data}, with LASA125 metadata, requested conversion or renaming,
preserved original coding, \code{LASA_wave} provenance, and a \code{label_report}.
}
\description{
Attaches SPSS-style variable and value labels to the social-participation
variables documented in LASA125 for waves B, C, D, E, 2B, F, and G.
}
\details{
The wave-specific inventories cover possession and use of a senior card,
hours spent listening to radio or watching television, radio and television
content, newspaper reading, and involvement with geographic communities.
Wave B has the broadest community-involvement block; later waves retain
selected media items. Wave G adds reality programmes, and the film item is
labelled "films/tv series" in F and G rather than "films/soaps".

Radio- and television-hours variables are numeric and are eligible for
\code{to_numeric}; negative values become \code{NA}. Other variables are categorical
and can be converted with \code{to_factor}. \code{to_numeric} takes precedence for
eligible variables.

Matching tries \code{name_corrections}, an exact case-sensitive name, and a
case-insensitive exact name. Original SPSS coding is preserved, standardized
naming removes the wave prefix, and \code{split_wavecode} inserts \code{LASA_wave}.
}
\examples{
dat <- data.frame(bqsocp06 = c(-1, 2.5), bqsocp07 = c(1, 4))
dat <- apply_lasa125_labels(dat, wave = "B", to_numeric = TRUE)
attr(dat$bqsocp07, "labels")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
