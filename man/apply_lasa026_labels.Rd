% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA026_labels.R
\name{apply_lasa026_labels}
\alias{apply_lasa026_labels}
\title{Apply LASA026 (Anxiety/HADS-A) SPSS labels}
\usage{
apply_lasa026_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA026 \code{.sav} file,
for example via \code{\link[haven:read_spss]{haven::read_sav()}}. Expected names include \code{banxiet1},
\code{ganxiet7}, \code{bmanxiet1}, and wave-B-only \code{banxmv}.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"B"}, \code{"C"}, \code{"D"}, \code{"E"}, \code{"F"}, \code{"G"},
\code{"H"}, \code{"3B"}, \code{"MB"}, \code{"I"}, or \code{"J"}.}

\item{name_corrections}{Optional named character vector for explicit column
name overrides. Names are canonical LASA026 suffixes without the wave
prefix (for example \code{anxiet1}, \code{manxiet7}, or \code{anxmv}), and values are
actual column names in \code{data}, for example
\code{c(anxiet1 = "tense_last_week")}.}

\item{to_factor}{Logical. If \code{FALSE} (default), categorical variables
remain numeric with SPSS-style value-label attributes. If \code{TRUE}, they
are converted to factors using their documented labels. An observed
value without a label remains a numeric-text factor level. \code{banxmv} is
also factored unless \code{to_numeric = TRUE}.}

\item{to_numeric}{Logical. If \code{FALSE} (default), wave-B \code{banxmv} retains its
SPSS-style missing-code labels. If \code{TRUE}, it becomes ordinary numeric,
negative missing-reason codes become \code{NA}, and non-negative counts are
retained. Anxiety items remain categorical. This takes precedence over
\code{to_factor} for \code{banxmv}.}

\item{standardize_names}{Logical. If \code{FALSE} (default), source column names
are retained except for renaming requested through \code{split_wavecode}. If
\code{TRUE}, matched LASA026 columns are renamed to their canonical lowercase
suffixes with the wave code removed; \code{"respnr"} is standardized; and
\code{split_wavecode} is always treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{FALSE} (default) and
\code{standardize_names = FALSE}, no wave-code splitting occurs. If \code{TRUE},
matched columns are renamed with their wave prefix removed and a new
\code{"LASA_wave"} column filled with \code{wave} is inserted immediately after
the respondent-number column. It is always treated as \code{TRUE} when
\code{standardize_names = TRUE}.}
}
\value{
\code{data}, with variable/value-label attributes attached to every
matched LASA026 column, optional factor/numeric conversion and canonical
renaming, and \code{original_labels}/\code{original_values} attributes preserving
the original SPSS coding. A \code{"LASA_wave"} column is added when requested.
The generic name-matching audit is attached as \code{attr(data, "label_report")} and can be retrieved with \code{\link[=lasa_label_report]{lasa_label_report()}}.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to the seven anxiety items documented in LASA026.
Wave B additionally contains \code{banxmv}, the number of missing item values.
Wave MB uses medical-interview names (\code{bmanxiet1} through \code{bmanxiet7});
the other documented waves use the \code{anxiet} suffix.
}
\details{
All anxiety items use the same four response categories and a -2 routing
label referring to the wave-specific reason-missing variable in LASA226.
Item 4 is marked \code{"positive"} in its variable label exactly as printed.
The function attaches metadata only and does not reverse-score the item or
compute a total anxiety score.

By default, matched variables remain numeric with their SPSS-style labels.
With \code{to_factor = TRUE}, they become factors using the documented response
text; observed unlabelled codes remain numeric-text levels. \code{banxmv} is a
count: with \code{to_numeric = TRUE}, its negative missing-reason codes become
\code{NA} and non-negative counts are retained. The seven anxiety items remain
categorical, and numeric conversion takes precedence over factor
conversion for \code{banxmv}.

The source PDF documents waves B, C, D, E, F, G, H, 3B, MB, I, and J. It
does not contain variable tables for wave 2B or K. The PDF also documents
LASA226 scale scores and reason-missing variables; those are intentionally
outside LASA026 and are not changed by this function.

Column matching tries, in order: (1) an explicit override in
\code{name_corrections}, (2) an exact case-sensitive name match, and (3) a
case-insensitive exact match. Variables documented for the selected wave
but absent from \code{data} are left untouched and recorded as \code{"not found"} in
the generic matching audit.

\code{name_corrections}, \code{to_factor}, \code{to_numeric}, \code{standardize_names}, and
\code{split_wavecode} are part of the parameter contract shared by every
\verb{apply_*_labels()} function in this package. Regardless of conversion,
every matched column keeps its original SPSS value coding in
\code{attr(x, "original_labels")} and \code{attr(x, "original_values")}. The
\code{"respnr"} column is matched in any capitalization and, when
\code{standardize_names = TRUE}, renamed to \code{"respnr"}.
}
\examples{
dat <- data.frame(
  RespNr = 1:4,
  ganxiet1 = c(-2, 0, 1, 3),
  ganxiet4 = c(-1, 0, 2, 3)
)
dat <- apply_lasa026_labels(dat, wave = "G")
attr(dat$ganxiet1, "labels")
}
\seealso{
\code{\link[=apply_lasa025_labels]{apply_lasa025_labels()}}, \code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
