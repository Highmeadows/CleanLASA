% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lasa_labels_manual_update.R
\name{manual_update_lasa_labels}
\alias{manual_update_lasa_labels}
\title{Manually correct or add LASA variable/value labels}
\usage{
manual_update_lasa_labels(
  filecode = NULL,
  wave = NULL,
  variable,
  var_label = NULL,
  val_labels = NULL,
  replace_val_labels = FALSE
)
}
\arguments{
\item{filecode}{Optional LASA file code (e.g. \code{"046"}, \code{"zoa1"},
\code{"z004"}). Required when \code{variable}/\code{wave} match more than one file
code, or when defining a brand-new variable no existing row matches.}

\item{wave}{Optional LASA wave code (e.g. \code{"B"}, \code{"2B"}), or \code{"all"} to
apply the override to every wave the matched \code{filecode}+canonical-name
combination exists for. Required when \code{variable} is a canonical name
(which carries no wave information on its own), or when a
wave-specific name's leading prefix is shared by more than one wave
(e.g. \code{"b"} is shared by B/2B/3B/MB) and thus can't be inferred.}

\item{variable}{Required. Either a wave-specific variable name (e.g.
\code{"blphya01"}) or a canonical name (e.g. \code{"lphya01"}).}

\item{var_label}{Optional replacement variable label. \code{NA}/omitted
leaves the existing label untouched.}

\item{val_labels}{Optional named vector of value-label corrections,
keyed by numeric code, e.g. \verb{c(\\}-5\\verb{ = "NA, wrong, skip", \\}1\\verb{ = "man")} (quote or backtick negative/numeric-looking names).}

\item{replace_val_labels}{Logical, default \code{FALSE}. If \code{FALSE}, \code{val_labels}
is merged into the existing value-label set (upserted by code). If
\code{TRUE}, the result is \emph{only} \code{val_labels} -- every other coded value
for this variable is dropped when labels are applied.}
}
\value{
Invisibly, a list describing what was recorded: \code{filecode},
\code{wave} (the resolved wave(s)), \code{variable_name}, and which of
\code{variable_label}/\code{value_labels} were updated.
}
\description{
A hand-authored escape hatch for when the bundled \code{\link[=lasa_label_db]{lasa_label_db()}} is
itself incomplete or wrong, or a user wants a custom label. Writes
directly to the user-local label database used by \code{\link[=apply_lasa_labels]{apply_lasa_labels()}}
and \code{\link[=read_lasa_sav]{read_lasa_sav()}}. Overrides are stored separately from the bundled
data and are composed back on top of it at lookup time, so a later
package update never silently discards a correction recorded here.
}
\examples{
\dontrun{
# Merge a correction into the existing value-label set:
manual_update_lasa_labels(
  filecode = "046", wave = "B", variable = "lphya01",
  val_labels = c(`-5` = "NA, wrong, skip")
)

# Fully replace a variable's value labels:
manual_update_lasa_labels(
  variable = "lphya01", wave = "all", filecode = "046",
  val_labels = c(`-5` = "NA, wrong, skip"), replace_val_labels = TRUE
)
}
}
\seealso{
\code{\link[=lasa_label_db]{lasa_label_db()}}, \code{\link[=apply_lasa_labels]{apply_lasa_labels()}}
}
