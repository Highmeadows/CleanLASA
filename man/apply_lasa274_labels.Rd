% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA274_labels.R
\name{apply_lasa274_labels}
\alias{apply_lasa274_labels}
\title{Apply LASA274 (Vignette Total Score) SPSS labels}
\usage{
apply_lasa274_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASAD274 \code{.sav} file.}

\item{wave}{Character scalar identifying wave \code{"D"}, matched
case-insensitively.}

\item{name_corrections}{Optional named character vector mapping canonical
suffix \code{vignet} to the actual column name in \code{data}.}

\item{to_factor}{Logical. If \code{TRUE}, the score is converted to a factor from
its documented missing and endpoint labels unless \code{to_numeric = TRUE}
takes precedence. Unlabelled observed scores are retained.}

\item{to_numeric}{Logical. If \code{TRUE}, convert the score to plain numeric and
replace its negative missing code with \code{NA}.}

\item{standardize_names}{Logical. If \code{TRUE}, standardize matched names and
\code{respnr}, remove the wave prefix, and add \code{LASA_wave}.}

\item{split_wavecode}{Logical. If \code{TRUE}, remove the wave prefix from the
matched column and add \code{LASA_wave}. Implied by
\code{standardize_names = TRUE}.}
}
\value{
\code{data} with LASA274 metadata, requested conversion and renaming,
the \code{LASA_wave} provenance attribute, and a \code{label_report} matching audit.
}
\description{
Attaches the SPSS-style variable label (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to the LASA274 total Frequency of Requested Effort
(FRE) score derived from the LASA074 vignettes about help.
}
\details{
LASA274 is documented only for wave D and contains one numeric score,
\code{dvignet}. The documented range is 0 (low FRE) through 19 (high FRE), with
\code{-1} indicating more than three missing vignette items. With
\code{to_numeric = TRUE}, the score becomes plain numeric and \code{-1} becomes
\code{NA}. Original values and labels remain available in \code{original_values} and
\code{original_labels}.

Column matching uses an explicit \code{name_corrections} override, an exact
case-sensitive match, and then a case-insensitive exact match. Standardized
naming removes the D-wave prefix, standardizes \code{respnr}, and inserts a
\code{LASA_wave} column.
}
\examples{
dat <- data.frame(RespNr = 1:3, dvignet = c(-1, 0, 19))
dat <- apply_lasa274_labels(dat, wave = "D", to_numeric = TRUE)
attr(dat$dvignet, "label")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}},
\code{\link[=apply_lasa074_labels]{apply_lasa074_labels()}}
}
