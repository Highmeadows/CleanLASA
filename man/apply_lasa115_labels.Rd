% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA115_labels.R
\name{apply_lasa115_labels}
\alias{apply_lasa115_labels}
\title{Apply LASA115 (Subjective pain) SPSS labels}
\usage{
apply_lasa115_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA115 \code{.sav} file.}

\item{wave}{Character scalar identifying wave \code{"B"}, \code{"C"}, \code{"D"}, \code{"E"},
\code{"2B"}, \code{"F"}, \code{"G"}, \code{"H"}, \code{"3B"}, \code{"MB"}, \code{"I"}, \code{"J"}, or \code{"K"}.
Matching is case-insensitive.}

\item{name_corrections}{Optional named character vector whose names are
canonical suffixes without the wave prefix, such as \code{qpain1} or
\code{mpain1}, and whose values are actual column names in \code{data}.}

\item{to_factor}{Logical. Convert labelled categorical variables to
factors using the documented coding.}

\item{to_numeric}{Logical. Accepted for interface consistency; LASA115 has
no numeric variables eligible for conversion.}

\item{standardize_names}{Logical. Standardize matched names and respondent
number, remove the wave prefix, and add \code{LASA_wave}.}

\item{split_wavecode}{Logical. Remove wave prefixes from matched names and
add \code{LASA_wave}; always enabled by \code{standardize_names = TRUE}.}
}
\value{
\code{data} with LASA115 metadata, optional conversion and renaming,
original-coding attributes, \code{LASA_wave}, and a \code{label_report} audit.
}
\description{
Attaches SPSS-style variable and value labels to the six subjective-pain
items documented for LASA115. The items concern pain while standing,
changing position, sitting, and walking, and whether pain is unbearable or
constant.
}
\details{
The self-administered questionnaire variables are documented for waves B,
C, D, E, 2B, F, G, H, 3B, I, J, and K with suffixes \code{qpain1} through
\code{qpain6}. Wave MB contains the same six items in the medical interview,
using suffixes \code{mpain1} through \code{mpain6}. Every item uses \code{-1} for no
answer, \code{1} for yes, and \code{2} for no. The pain scale score documented in
the same PDF belongs to LASA315 and is not included here.

Matching tries an explicit \code{name_corrections} override, an exact match,
and a case-insensitive exact match, in that order. \code{to_factor = TRUE}
converts matched items using the documented value map and retains observed
unlabelled codes. No LASA115 variable is eligible for \code{to_numeric}.
Original labels and values remain available through \code{original_labels} and
\code{original_values} attributes.
}
\examples{
dat <- data.frame(RespNr = 1:3, BQPAIN1 = c(-1, 1, 2))
dat <- apply_lasa115_labels(dat, wave = "B")
attr(dat$BQPAIN1, "labels")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
