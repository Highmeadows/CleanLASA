% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA070_labels.R
\name{apply_lasa070_labels}
\alias{apply_lasa070_labels}
\title{Apply LASA070 (Social participation) SPSS labels}
\usage{
apply_lasa070_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA070 \code{.sav} file.}

\item{wave}{Character scalar identifying wave \code{"B"}, \code{"C"}, \code{"D"}, \code{"E"},
\code{"2B"}, \code{"F"}, \code{"G"}, \code{"H"}, \code{"3B"}, \code{"MB"}, \code{"I"}, or \code{"J"}.
Matching is case-insensitive.}

\item{name_corrections}{Optional named character vector. Names are canonical
suffixes without a wave prefix (for example \code{socpyn}, \code{socp27}, or
\code{sp95f1}); values are actual names in \code{data}.}

\item{to_factor}{Logical. Convert matched categorical variables to factors
with documented labels; observed undocumented codes remain text levels.}

\item{to_numeric}{Logical. Restore matched minutes and age variables to
plain numeric, replacing negative missing-reason codes with \code{NA}.}

\item{standardize_names}{Logical. Rename matched variables to lowercase
canonical suffixes, standardize \code{respnr}, and imply wave splitting.}

\item{split_wavecode}{Logical. Remove matched variables' wave prefix and add
a \code{LASA_wave} column immediately after the respondent-number column.}
}
\value{
The labelled data, with optional conversions and standardized names,
preserved original coding, a \code{LASA_wave} attribute, and \code{label_report}.
}
\description{
Attaches the variable labels and SPSS-style value-label maps documented for
LASA070 social-participation files. The codebook covers waves B, C, D, E,
2B, F, G, H, 3B, MB, I, and J. Inventories differ substantially by wave:
earlier files emphasize organizational membership, visits, volunteering,
cultural activities, study, and age norms; later files add computer,
internet, mobile-phone, IT-support, and altruism items. The reduced MB file
contains migrant-cohort membership-composition and technology items.
}
\details{
Labels, codes, routing-specific missing labels, variable order, and the
wave-specific omissions are reproduced from \code{LASA070_varinfo.pdf}. The
frequency codes used where the document refers to its Visit frequency table
are 1 (almost never) through 7 (every day). Minutes and age fields are the
numeric variables eligible for \code{to_numeric}; other documented variables are
categorical.

Matching tries \code{name_corrections}, exact names, then case-insensitive exact
names. Missing documented columns are recorded in \code{label_report}. Conversion
preserves \code{original_labels} and \code{original_values}; standardized names drop
the wave prefix, standardize \code{respnr}, and optionally add \code{LASA_wave}.
}
\examples{
dat <- data.frame(RespNr = 1:2, bsocpyn = c(1, 2), bsocp30 = c(-1, 60))
dat <- apply_lasa070_labels(dat, wave = "B")
attr(dat$bsocpyn, "labels")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
