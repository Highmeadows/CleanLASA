% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA864_labels.R
\name{apply_lasa864_labels}
\alias{apply_lasa864_labels}
\title{Apply LASA864 (Serum Cortisol) SPSS labels}
\usage{
apply_lasa864_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASAC864 \code{.sav} file.}

\item{wave}{Character scalar: \code{"C"}; matching is case-insensitive.}

\item{name_corrections}{Optional named character vector mapping canonical
suffixes without the wave prefix to actual column names in \code{data}.}

\item{to_factor}{Logical. Convert value-labelled variables to factors.}

\item{to_numeric}{Logical. Restore laboratory measures to plain numeric and
replace negative missing codes with \code{NA}.}

\item{standardize_names}{Logical. Standardize matched names and \code{respnr},
remove the wave prefix, and add \code{LASA_wave}.}

\item{split_wavecode}{Logical. Remove wave prefixes from matched names and
add \code{LASA_wave}; implied by \code{standardize_names = TRUE}.}
}
\value{
\code{data} with LASA864 metadata, optional conversions and renaming, a
\code{LASA_wave} attribute, and a \code{label_report} matching audit.
}
\description{
Attaches the variable and value labels documented for the three serum
cortisol measures in the LASAC864 medical-interview file. The wave-C
inventory contains total cortisol, corticosteroid binding globuline, and
calculated free cortisol. Codes \code{-3} and \code{-1} identify an incorrect value
and no determination where documented.
}
\details{
Matching uses an explicit \code{name_corrections} entry, an exact match, then a
case-insensitive exact match. \code{to_numeric = TRUE} restores the laboratory
measures to plain numeric and replaces negative missing codes with \code{NA}.
\code{to_factor = TRUE} converts value-labelled columns to factors when numeric
restoration is not requested. Original values and labels remain available
in \code{original_values} and \code{original_labels} attributes.
}
\examples{
dat <- data.frame(RespNr = 1:2, cmco = c(-1, 250))
apply_lasa864_labels(dat, wave = "C", to_numeric = TRUE)
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
