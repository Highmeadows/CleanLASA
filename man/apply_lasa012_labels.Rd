% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA012_labels.R
\name{apply_lasa012_labels}
\alias{apply_lasa012_labels}
\title{Apply LASA012 (Pets in Household) SPSS labels}
\usage{
apply_lasa012_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA012 \code{.sav} file,
for example via \code{\link[haven:read_spss]{haven::read_sav()}}. Expected wave-prefixed names include
\code{bpets}, \code{cpetothe}, \code{fcat}, and \code{knspecie}.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"B"}, \code{"C"}, \code{"D"}, \code{"E"}, \code{"2B"}, \code{"F"},
\code{"G"}, \code{"H"}, \code{"3B"}, \code{"I"}, \code{"J"}, or \code{"K"}.}

\item{name_corrections}{Optional named character vector for explicit column
name overrides. Names are canonical LASA012 suffixes without the wave
prefix (for example \code{pets}, \code{petothe}, or \code{nspecie}), and values are the
actual column names in \code{data}, for example
\code{c(petothe = "F_OTHER_PET")}.}

\item{to_factor}{Logical. If \code{FALSE} (default), categorical variables
remain numeric with SPSS-style value-label attributes. If \code{TRUE}, the
main pets question and the cat, dog, bird, and other-pets indicators are
converted to factors using their value labels as levels. Observed values
without a codebook label are retained as numeric-text levels rather than
converted to \code{NA}.}

\item{to_numeric}{Logical. If \code{FALSE} (default), the number-of-species
variable retains its SPSS-style missing-value label. If \code{TRUE},
\code{nspecie} is restored to an ordinary numeric vector, negative
missing-reason codes are replaced with \code{NA}, and non-negative species
counts are retained. This takes precedence over \code{to_factor} for
\code{nspecie}; the categorical pet variables remain labelled or become
factors according to \code{to_factor}.}

\item{standardize_names}{Logical. If \code{FALSE} (default), source column names
are retained except for renaming requested through \code{split_wavecode}. If
\code{TRUE}, matched LASA012 columns are renamed to their canonical lowercase
suffixes with the wave code removed, such as \code{pets}, \code{petothe}, or
\code{nspecie}; \code{"respnr"} in any capitalization is renamed to \code{"respnr"};
and \code{split_wavecode} is always treated as \code{TRUE}, regardless of the value
supplied to \code{split_wavecode}.}

\item{split_wavecode}{Logical. If \code{FALSE} (default) and
\code{standardize_names = FALSE}, no wave-code splitting occurs. If \code{TRUE},
matched columns are renamed with the wave prefix removed (for example,
\code{fpetothe} becomes \code{petothe}) and a new \code{"LASA_wave"} column filled with
\code{wave} is inserted immediately after the respondent-number column. It is
always treated as \code{TRUE} when \code{standardize_names = TRUE}.}
}
\value{
\code{data}, with variable- and value-label attributes attached to every
matched LASA012 column, optional factor/numeric conversion and canonical
renaming, and \code{original_labels}/\code{original_values} attributes preserving
the original SPSS coding. A \code{"LASA_wave"} column is added after
\code{"respnr"} when \code{standardize_names = TRUE} or \code{split_wavecode = TRUE}.
The generic variable-name audit is attached as \code{attr(data, "label_report")} and can be retrieved with \code{\link[=lasa_label_report]{lasa_label_report()}}.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to a LASA012 pets-in-household data frame, using the
variable names, wording, and missing-value codes documented for the
requested wave. By default, matched variables remain numeric. Optional
arguments allow converting categorical variables to factors, restoring
the number-of-species variable to plain numeric, correcting mismatched
column names, and standardizing names to the canonical LASA documentation
spelling.
}
\details{
LASA012 changes across waves. Wave B calls the other-pets indicator
\code{petsoth}, whereas later waves use \code{petothe}; the routing and
missing-reason codes for the main \code{pets} question also differ between B,
C, D/E, 2B, and the later waves. \code{wave} is therefore required, especially
because B, 2B, and 3B all use variable names beginning with \code{"b"}.

Column matching tries, in order: (1) an explicit override in
\code{name_corrections}, (2) an exact case-sensitive name match, and (3) a
case-insensitive exact match. Variables documented for the selected wave
but absent from \code{data} are left untouched and recorded as \code{"not found"} in
the generic matching audit.

The LASAB212 variables shown in the LASA012 codebook describe specific
types of other pets and are not processed by this function because
LASAB212 is a separate LASA file code.

\code{name_corrections}, \code{to_factor}, \code{to_numeric}, \code{standardize_names}, and
\code{split_wavecode} are part of the parameter contract shared by every
\verb{apply_*_labels()} function in this package. Regardless of
\code{to_factor}/\code{to_numeric}, every matched column keeps its original SPSS
value coding in \code{attr(x, "original_labels")} and
\code{attr(x, "original_values")}. The \code{"respnr"} column is matched in any
capitalization and, when \code{standardize_names = TRUE}, renamed to
\code{"respnr"}.
}
\examples{
dat_b <- data.frame(
  BPETS = c(-4, 0, 1),
  BCAT = c(-2, 0, 1),
  BNSPECIE = c(-2, 0, 2)
)
dat_b <- apply_lasa012_labels(dat_b, wave = "B")
attr(dat_b$BPETS, "label")
attr(dat_b$BPETS, "labels")

dat_f <- data.frame(
  RespNr = 1:3,
  FPETS = c(1, 0, -1),
  FPETOTHE = c(1, 0, -2),
  FNSPECIE = c(2, 0, -2)
)
dat_f <- apply_lasa012_labels(
  dat_f,
  wave = "F",
  to_factor = TRUE,
  to_numeric = TRUE,
  standardize_names = TRUE
)
lasa_label_report(dat_f, problems_only = TRUE)
}
\seealso{
\code{\link[=apply_lasa046_labels]{apply_lasa046_labels()}}, \code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
