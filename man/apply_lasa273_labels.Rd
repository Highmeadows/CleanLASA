% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA273_labels.R
\name{apply_lasa273_labels}
\alias{apply_lasa273_labels}
\title{Apply LASA273 (Loneliness Scales) SPSS labels}
\usage{
apply_lasa273_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA273 \code{.sav} file.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"B"}, \code{"C"}, \code{"D"}, \code{"E"}, \code{"2B"}, \code{"F"},
\code{"G"}, \code{"H"}, \code{"3B"}, \code{"MB"}, \code{"I"}, or \code{"J"}.}

\item{name_corrections}{Optional named character vector mapping canonical
suffixes \code{lo}, \code{loemo}, or \code{losoc} to actual column names in \code{data}.}

\item{to_factor}{Logical. If \code{TRUE}, scale variables are converted to
factors using documented value labels, unless \code{to_numeric = TRUE} takes
precedence.}

\item{to_numeric}{Logical. If \code{TRUE}, scale variables are converted to plain
numeric and negative missing/routing values become \code{NA}.}

\item{standardize_names}{Logical. If \code{TRUE}, matched names and \code{respnr} are
standardized, wave prefixes are removed, and \code{LASA_wave} is added.}

\item{split_wavecode}{Logical. If \code{TRUE}, matched columns have the wave
prefix removed and \code{LASA_wave} is added. Implied by
\code{standardize_names = TRUE}.}
}
\value{
\code{data} with LASA273 metadata, requested conversions and renaming,
the \code{LASA_wave} provenance attribute, and a \code{label_report} matching audit.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to the three scaled loneliness variables documented
as LASA273 in the LASA073 codebook: total, emotional, and social
loneliness.
}
\details{
LASA273 is documented for waves B, C, D, E, 2B, F, G, H, 3B, MB, I, and J.
Every wave contains three scale variables, but missing-value and routing
codes differ across B--E, 2B, F, G, and H onward. The total score ranges
from no loneliness (0) to severe loneliness (11); emotional and social
subscales have documented endpoints 0--6 and 0--5, respectively.

All three variables are numeric scale scores. With \code{to_numeric = TRUE},
they become plain numeric and documented negative missing/routing values
become \code{NA}. With \code{to_factor = TRUE} alone, the documented endpoint and
missing labels can be used as factor levels. Original SPSS values and
labels remain available in \code{original_values} and \code{original_labels}.

Column matching uses an explicit \code{name_corrections} override, an exact
case-sensitive match, and then a case-insensitive exact match. Standardized
naming removes the wave prefix, standardizes \code{respnr}, and inserts a
\code{LASA_wave} column.
}
\examples{
dat <- data.frame(RespNr = 1:3, hlo = c(-1, 0, 7), hloemo = c(0, 3, 6))
dat <- apply_lasa273_labels(dat, wave = "H", to_numeric = TRUE)
attr(dat$hlo, "label")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}},
\code{\link[=apply_lasa073_labels]{apply_lasa073_labels()}}
}
