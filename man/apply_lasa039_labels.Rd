% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA039_labels.R
\name{apply_lasa039_labels}
\alias{apply_lasa039_labels}
\title{Apply LASA039 (Lifetime Psychopathology/LIDAS) SPSS labels}
\usage{
apply_lasa039_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASAK039 \code{.sav} file,
for example via \code{\link[haven:read_spss]{haven::read_sav()}}. Names include \code{klidascont},
\code{klidasdepd}, \code{klidasdepa}, and \code{klidastreatpsy}.}

\item{wave}{Character scalar identifying the LASA wave. Only \code{"K"} is
documented and accepted; matching is case-insensitive.}

\item{name_corrections}{Optional named character vector for explicit column
name overrides. Names are canonical LASA039 suffixes without the \code{k}
prefix (for example \code{lidascont}, \code{lidasdepa}, or \code{lidastreatpsy}), and
values are actual column names in \code{data}.}

\item{to_factor}{Logical. If \code{FALSE} (default), variables retain their
numeric representation with SPSS-style value-label attributes. If
\code{TRUE}, categorical variables are converted to factors using the
documented labels. Observed unlabelled codes remain numeric-text levels.}

\item{to_numeric}{Logical. If \code{TRUE}, the 14 age-at-first-diagnosis
variables are restored to plain numeric and negative values are replaced
by \code{NA}. Other LASA039 variables remain categorical. This conversion
takes precedence over \code{to_factor} for eligible variables.}

\item{standardize_names}{Logical. If \code{FALSE} (default), source column names
are retained except for renaming requested through \code{split_wavecode}. If
\code{TRUE}, matched LASA039 columns are renamed to their canonical lowercase
suffixes with the \code{k} wave prefix removed; \code{"respnr"} is standardized;
and \code{split_wavecode} is always treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{FALSE} (default) and
\code{standardize_names = FALSE}, no wave-code splitting occurs. If \code{TRUE},
matched columns are renamed with their \code{k} prefix removed and a new
\code{"LASA_wave"} column filled with \code{"K"} is inserted immediately after
the respondent-number column. It is always treated as \code{TRUE} when
\code{standardize_names = TRUE}.}
}
\value{
\code{data}, with variable/value-label attributes attached to every
matched LASA039 column, optional factor/numeric conversion and canonical
renaming, and \code{original_labels}/\code{original_values} attributes preserving
the original SPSS coding. A \code{"LASA_wave"} column is added when requested.
The generic name-matching audit is attached as \code{attr(data, "label_report")} and can be retrieved with \code{\link[=lasa_label_report]{lasa_label_report()}}.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to the 53 variables documented in the wave-K
LASA039 Lifetime Psychopathology (LIDAS) file.
}
\details{
The file records contact with psychological care, lifetime mental-disorder
status, and diagnosis/age-at-first-diagnosis/treatment triplets for 14
disorder groups. It also contains a general mental-treatment item and
eight treatment or medication types. Shared value cells in the source PDF
are applied to every variable covered by those cells: \code{lidascont} and
\code{lidasdiag} share one yes/no map, while all eight \verb{lidastreat*} type
variables share one mentioned/not-mentioned map.

The 14 age-at-first-diagnosis variables are continuous measures whose only
documented value labels are negative missing-reason codes. With
\code{to_numeric = TRUE}, those fields are restored to plain numeric and all
observed negative values become \code{NA}. The remaining variables are
categorical and are unaffected by \code{to_numeric}. With \code{to_factor = TRUE},
categorical variables become factors; observed unlabelled codes remain
numeric-text levels. For eligible age variables, \code{to_numeric} takes
precedence when both conversion options are requested.

Column matching tries, in order: (1) an explicit override in
\code{name_corrections}, (2) an exact case-sensitive name match, and (3) a
case-insensitive exact match. Variables documented for wave K but absent
from \code{data} are left untouched and recorded as \code{"not found"} in the
generic matching audit.

\code{name_corrections}, \code{to_factor}, \code{to_numeric}, \code{standardize_names}, and
\code{split_wavecode} follow the parameter contract shared by the package's
other \verb{apply_*_labels()} functions. Regardless of conversion, every
matched column keeps its original SPSS coding in
\code{attr(x, "original_labels")} and \code{attr(x, "original_values")}. The
\code{"respnr"} column is matched in any capitalization and, when
\code{standardize_names = TRUE}, renamed to \code{"respnr"}.
}
\examples{
dat <- data.frame(
  RespNr = 1:3,
  klidasdepd = c(-2, 0, 1),
  klidasdepa = c(-3, 24, 51),
  klidasdept = c(-1, 1, 2)
)
dat <- apply_lasa039_labels(dat, wave = "K", to_numeric = TRUE)
attr(dat$klidasdepd, "labels")
}
\seealso{
\code{\link[=apply_lasa038_labels]{apply_lasa038_labels()}}, \code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
