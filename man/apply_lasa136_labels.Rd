% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA136_labels.R
\name{apply_lasa136_labels}
\alias{apply_lasa136_labels}
\title{Apply LASA136 (Worrying) SPSS labels}
\usage{
apply_lasa136_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASAE136 \code{.sav} file,
for example via \code{\link[haven:read_spss]{haven::read_sav()}}, containing any of \code{eqworr01} through
\code{eqworr16}.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. LASA136 is documented only for \code{"E"}.}

\item{name_corrections}{Optional named character vector mapping canonical
suffixes without the wave prefix (\code{qworr01} through \code{qworr16}) to actual
column names in \code{data}.}

\item{to_factor}{Logical. If \code{TRUE}, the categorical worry items are
converted to factors using the documented response labels.}

\item{to_numeric}{Logical. Retained for the shared parameter contract.
LASA136 contains no numeric count or continuous variables, so it does not
change the documented items.}

\item{standardize_names}{Logical. If \code{TRUE}, matched columns are renamed to
canonical lowercase suffixes without the wave prefix, \code{respnr} is
standardized, and \code{split_wavecode} is treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{TRUE}, matched columns have the wave
prefix removed and a \code{LASA_wave} column containing \code{"E"} is inserted
immediately after the respondent-number column.}
}
\value{
\code{data}, with LASA136 variable/value labels, optional factor
conversion and standardized naming, preserved original SPSS coding, the
\code{LASA_wave} provenance attribute, and a \code{label_report} matching audit.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to the 16 Penn State Worry Questionnaire items
documented in LASAE136.
}
\details{
All items use the same five-level response scale from "not at all
characteristic" to "very characteristic", plus -1 for no answer. The
worry-scale score shown in the source PDF belongs to the separate LASA336
scale-score file and is intentionally outside this function.

LASA136 is documented only for wave E. Its 16 variables are categorical;
consequently \code{to_factor = TRUE} converts them to factors, whereas
\code{to_numeric} is retained for the common parameter contract but does not
change them. Observed unlabelled codes remain numeric-text factor levels.

Column matching tries an explicit \code{name_corrections} override, an exact
case-sensitive match, then a case-insensitive exact match. Absent variables
are reported as \code{"not found"}. Original labels and values are preserved,
and optional standardized naming removes the \code{e} prefix, standardizes
\code{respnr}, and adds \code{LASA_wave}.
}
\examples{
dat <- data.frame(RespNr = 1:2, eqworr01 = c(1, 5), eqworr16 = c(-1, 3))
dat <- apply_lasa136_labels(dat, wave = "E", to_factor = TRUE)
attr(dat$eqworr01, "label")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
