% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA148_labels.R
\name{apply_lasa148_labels}
\alias{apply_lasa148_labels}
\title{Apply LASA148 (Medical decisions about life) SPSS labels}
\usage{
apply_lasa148_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA148 \code{.sav} file,
for example via \code{\link[haven:read_spss]{haven::read_sav()}}.}

\item{wave}{Character scalar identifying LASA wave \code{"G"}; matching is
case-insensitive.}

\item{name_corrections}{Optional named character vector. Names are
canonical suffixes without the wave prefix (for example \code{qeoldt1}) and
values are actual column names in \code{data}.}

\item{to_factor}{Logical. Convert labelled categorical variables to factors
using their documented value labels.}

\item{to_numeric}{Logical. Accepted for the shared interface; LASA148 has
no numeric variables eligible for conversion.}

\item{standardize_names}{Logical. Rename matched columns to lowercase
suffixes without the wave prefix, standardize the respondent-number
column to \code{respnr}, and add \code{LASA_wave}.}

\item{split_wavecode}{Logical. Remove the wave prefix from matched column
names and add \code{LASA_wave}. Always enabled by \code{standardize_names = TRUE}.}
}
\value{
\code{data} with documented metadata applied, optional conversion and
renaming, a \code{LASA_wave} attribute, and a matching audit in the
\code{label_report} attribute.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to the fifteen medical-decision variables documented
for LASA148. Five items identify who should decide about treatment at the
end of life, and ten items record treatment preferences in cancer and
dementia vignettes.
}
\details{
LASA148 is documented only for wave G. The five decision-maker indicators
use the codebook's \code{-1}/0/\code{-1} map for no answer, not mentioned, and
mentioned, respectively. That duplicated \code{-1} is reproduced literally and
retained in \code{original_labels}; it is not silently corrected. The vignette
items use codes 1 through 4 from yes to no, plus \code{-1} for no answer.

Column matching tries, in order: an explicit \code{name_corrections} override,
an exact case-sensitive match, and a case-insensitive exact match.
\code{to_factor = TRUE} converts matched categorical items to factors while
retaining observed unlabelled codes as numeric-text levels. Because the
decision-maker map assigns two texts to \code{-1}, factor conversion uses the
first documented text for that code; the full map remains available in
\code{original_labels}. LASA148 has no numeric variables eligible for
\code{to_numeric} conversion. Original values are preserved in
\code{original_values}.
}
\examples{
dat <- data.frame(
  RespNr = 1:3,
  GQEOLDT1 = c(-1, 0, 1),
  gqvgcan1 = c(-1, 1, 4)
)
dat <- apply_lasa148_labels(dat, wave = "G")
attr(dat$GQEOLDT1, "labels")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
