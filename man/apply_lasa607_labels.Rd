% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA607_labels.R
\name{apply_lasa607_labels}
\alias{apply_lasa607_labels}
\title{Apply LASA607 (\link{PROXY} cognition, IQCODE) SPSS labels}
\usage{
apply_lasa607_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA607 \code{.sav} file.}

\item{wave}{Character scalar from \code{"C"} through \code{"K"}; matching is
case-insensitive.}

\item{name_corrections}{Optional named character vector mapping canonical
suffixes without the wave prefix to actual column names in \code{data}.}

\item{to_factor}{Logical. Convert labelled categorical variables to factors.}

\item{to_numeric}{Logical. Convert the eligible IQCODE total score to plain
numeric and replace its negative missing code with \code{NA}.}

\item{standardize_names}{Logical. Standardize matched names and \code{respnr},
remove the wave prefix, and add \code{LASA_wave}.}

\item{split_wavecode}{Logical. Remove wave prefixes from matched names and
add \code{LASA_wave}; implied by \code{standardize_names = TRUE}.}
}
\value{
\code{data} with LASA607 metadata, optional conversions and renaming, a
\code{LASA_wave} attribute, and a \code{label_report} matching audit.
}
\description{
Attaches the variable and value labels documented for the six LASA607
Informant Questionnaire on Cognitive Decline (IQCODE) items and their total
score in waves C through K. Items range from much better (1) to much worse
(5). The score is documented as low at its minimum (nominally 6) and much
decline at its maximum (nominally 30), with endpoints depending on the
specific sample and wave; only its fixed missing code is attached as a
value label.
}
\details{
Matching uses an explicit \code{name_corrections} entry, an exact match, then a
case-insensitive exact match. \code{to_factor = TRUE} converts the six
categorical items to factors while retaining observed undocumented codes.
\code{to_numeric = TRUE} restores the total score to plain numeric and replaces
its documented negative missing code with \code{NA}. Original values and labels
remain available in \code{original_values} and \code{original_labels} attributes.
}
\examples{
dat <- data.frame(RespNr = 1:2, ctpcogd1 = c(3, 4), ctpcogdc = c(-1, 18))
apply_lasa607_labels(dat, wave = "C")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
