% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA019_labels.R
\name{apply_lasa019_labels}
\alias{apply_lasa019_labels}
\title{Apply LASA019 (Income Evaluation) SPSS labels}
\usage{
apply_lasa019_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA019 \code{.sav} file,
for example via \code{\link[haven:read_spss]{haven::read_sav()}}. Expected wave-prefixed names include
\code{bincdecl}, \code{cincdclm}, \code{gincdcly}, and \code{kincsat1}.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"B"}, \code{"C"}, \code{"D"}, \code{"E"}, \code{"2B"}, \code{"F"},
\code{"G"}, \code{"H"}, \code{"3B"}, \code{"MB"}, \code{"I"}, \code{"J"}, or \code{"K"}.}

\item{name_corrections}{Optional named character vector for explicit column
name overrides. Names are canonical LASA019 suffixes without the wave
prefix (for example \code{incdecl}, \code{incdcly}, or \code{incsat1}), and values are
actual column names in \code{data}, for example
\code{c(incdecl = "B_INCOME_DECLINE")}.}

\item{to_factor}{Logical. If \code{FALSE} (default), categorical variables
remain numeric with SPSS-style value-label attributes. If \code{TRUE}, they
are converted to factors using their documented value labels. Observed
values without a label remain numeric-text factor levels.}

\item{to_numeric}{Logical. If \code{FALSE} (default), \code{incdcly} retains its
SPSS-style missing-reason labels. If \code{TRUE}, it is restored to ordinary
numeric, negative missing-reason codes become \code{NA}, and non-negative
years are retained. This takes precedence over \code{to_factor} for
\code{incdcly}; the month and evaluation variables remain categorical.}

\item{standardize_names}{Logical. If \code{FALSE} (default), source column names
are retained except for renaming requested through \code{split_wavecode}. If
\code{TRUE}, matched LASA019 columns are renamed to their canonical lowercase
suffixes with the wave code removed; \code{"respnr"} is standardized; and
\code{split_wavecode} is always treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{FALSE} (default) and
\code{standardize_names = FALSE}, no wave-code splitting occurs. If \code{TRUE},
matched columns are renamed with the wave prefix removed and a new
\code{"LASA_wave"} column filled with \code{wave} is inserted immediately after
the respondent-number column. It is always treated as \code{TRUE} when
\code{standardize_names = TRUE}.}
}
\value{
\code{data}, with variable/value-label attributes attached to every
matched LASA019 column, optional factor/numeric conversion and canonical
renaming, and \code{original_labels}/\code{original_values} attributes preserving
the original SPSS coding. A \code{"LASA_wave"} column is added when requested.
The generic name-matching audit is attached as \code{attr(data, "label_report")} and can be retrieved with \code{\link[=lasa_label_report]{lasa_label_report()}}.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to a LASA019 income-evaluation data frame, using the
wording, currency bands, routing references, and missing-value codes
documented for the requested wave. By default, matched variables remain
numeric. Optional arguments allow converting categorical variables to
factors, restoring the year of a major income decline to plain numeric,
correcting mismatched column names, and standardizing names.
}
\details{
LASA019 changes across waves. Wave B asks about the last five years and
uses guilder bands; C/D ask about the last three years and add month/year
follow-ups; E/F contain only satisfaction items; 2B uses detailed euro
bands; G through K use broader euro bands; 3B documents month/year
follow-ups but does not print month value labels; and MB again contains a
smaller item set. \code{wave} is required because B, 2B, 3B, and MB all use
variable names beginning with \code{"b"} while their schemas differ.

In the shared G/H/I/J/K table, J and K replace the general routing label
for \code{incdecl} with \code{"na, see J/KINCCAT"}; the function applies that
wave-specific override. For 3B, only the printed negative labels are
attached to \code{incdclm}; unprinted month names are not inferred.

Column matching tries, in order: (1) an explicit override in
\code{name_corrections}, (2) an exact case-sensitive name match, and (3) a
case-insensitive exact match. Variables documented for the selected wave
but absent from \code{data} are left untouched and recorded as \code{"not found"} in
the generic matching audit.

\code{name_corrections}, \code{to_factor}, \code{to_numeric}, \code{standardize_names}, and
\code{split_wavecode} are part of the parameter contract shared by every
\verb{apply_*_labels()} function in this package. Regardless of conversion,
every matched column keeps its original SPSS value coding in
\code{attr(x, "original_labels")} and \code{attr(x, "original_values")}. The
\code{"respnr"} column is matched in any capitalization and, when
\code{standardize_names = TRUE}, renamed to \code{"respnr"}.
}
\examples{
dat <- data.frame(
  RespNr = 1:3,
  GINCDECL = c(-5, 1, 7),
  GINCDCLM = c(-2, 1, 12),
  GINCDCLY = c(-2, 2018, 2022),
  GINCSAT1 = c(-5, 1, 5)
)
dat <- apply_lasa019_labels(dat, wave = "G")
attr(dat$GINCDECL, "labels")
}
\seealso{
\code{\link[=apply_lasa018_labels]{apply_lasa018_labels()}}, \code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
