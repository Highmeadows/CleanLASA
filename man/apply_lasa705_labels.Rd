% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA705_labels.R
\name{apply_lasa705_labels}
\alias{apply_lasa705_labels}
\title{Apply LASA705 (\link{RESP} falling) SPSS labels}
\usage{
apply_lasa705_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA705 \code{.sav} file.}

\item{wave}{Character scalar: \code{"C"}, \code{"D"}, \code{"E"}, \code{"F"}, \code{"G"}, \code{"H"},
\code{"I"}, \code{"J"}, or \code{"K"}; matching is case-insensitive.}

\item{name_corrections}{Optional named character vector mapping canonical
suffixes without the wave prefix to actual column names in \code{data}.}

\item{to_factor}{Logical. Convert labelled categorical variables to factors.}

\item{to_numeric}{Logical. Convert documented fall and fracture counts to
plain numeric and replace negative values with \code{NA}.}

\item{standardize_names}{Logical. Standardize matched names and \code{respnr},
remove wave prefixes, and add \code{LASA_wave}.}

\item{split_wavecode}{Logical. Remove wave prefixes from matched names and
add \code{LASA_wave}; implied by \code{standardize_names = TRUE}.}
}
\value{
\code{data} with LASA705 metadata, requested conversions and reshaping,
an \code{LASA_wave} attribute, and a \code{label_report} matching audit.
}
\description{
Attaches the variable and value labels documented for LASA705 respondent
telephone-interview data in waves C through K. The file covers falls during
the previous year, the number of fractures since the previous interview,
and up to three fractures with their type, cause, and doctor contact.
}
\details{
Wave C uses an earlier fracture inventory. Wave D retains two fall
questions that are absent from E through K. Waves D through K include a
fracture count and three separate indicators of whether each fracture
occurred. Doctor-contact code \code{0} means no in waves C through G, whereas
code \code{1} means no from wave H onward.

Fall and fracture counts are codebook-numeric and are eligible for
\code{to_numeric}; negative values become \code{NA}. Other documented variables are
categorical and can be converted with \code{to_factor}. Original values and
labels are preserved in reference attributes.

Matching tries \code{name_corrections}, an exact match, and a case-insensitive
exact match. Standardized naming removes the wave prefix;
\code{standardize_names} and \code{split_wavecode} add \code{LASA_wave}.
}
\examples{
dat <- data.frame(RespNr = 1:2, ctrfall = c(1, 2), ctrfalln = c(-2, 3))
apply_lasa705_labels(dat, wave = "C", to_numeric = TRUE)
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
