% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA030_labels.R
\name{apply_lasa030_labels}
\alias{apply_lasa030_labels}
\title{Apply LASA030 (Functional Limitations and ADL) SPSS labels}
\usage{
apply_lasa030_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA030 \code{.sav} file,
containing wave-prefixed variables such as \code{badl1a}, \code{cadl1a}, or
\code{kadl7d}.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"B"}, \code{"C"}, \code{"D"}, \code{"E"}, \code{"2B"}, \code{"F"},
\code{"G"}, \code{"H"}, \code{"3B"}, \code{"MB"}, \code{"I"}, \code{"J"}, or \code{"K"}.}

\item{name_corrections}{Optional named character vector for explicit name
overrides. Names are canonical LASA030 suffixes without the wave prefix,
and values are the actual column names in \code{data}, for example
\code{c(adl4a = "B_ADL4A")}.}

\item{to_factor}{Logical. If \code{FALSE} (default), categorical variables stay
numeric with SPSS-style value-label attributes. If \code{TRUE}, value labels
are used as factor levels, while observed values without a codebook label
are retained under their numeric code.}

\item{to_numeric}{Logical. If \code{FALSE} (default), variables retain their
value-label attributes. If \code{TRUE}, variables whose codebook labels contain
only negative missing-reason codes are restored to plain numeric, with
negative values converted to \code{NA}. LASA030 contains categorical variables,
so this option normally makes no changes, but it is retained as part of
the package-wide \verb{apply_*_labels()} argument contract.}

\item{standardize_names}{Logical. If \code{FALSE} (default), source column names
are preserved (except for any renaming triggered by \code{split_wavecode}, see
below). If \code{TRUE}, matched columns are renamed to their canonical
lowercase LASA030 names with the wave code removed, such as \code{adl1a} or
\code{adl7d}, \code{"respnr"} (in any capitalization) is renamed to \code{"respnr"}, and
\code{split_wavecode} is always treated as \code{TRUE} as well, regardless of what
was passed for \code{split_wavecode}.}

\item{split_wavecode}{Logical. If \code{FALSE} (default) and
\code{standardize_names = FALSE}, no wave-code splitting occurs. If \code{TRUE},
matched columns are renamed with the wave-letter prefix removed (e.g.
\code{badl1a} becomes \code{adl1a}), and a new \code{"LASA_wave"} column, filled with
\code{wave}, is inserted right after \code{"respnr"}. Always treated as \code{TRUE} when
\code{standardize_names = TRUE}.}
}
\value{
\code{data}, with variable- and value-label attributes attached to every
matched LASA030 column, optionally converted to factors and/or renamed,
plus reference \code{original_labels}/\code{original_values} attributes preserving
the original SPSS value coding regardless of that reshaping. A new
\code{"LASA_wave"} column is added after \code{"respnr"} when \code{standardize_names = TRUE} or \code{split_wavecode = TRUE}. A matching audit is attached as the
generic \code{"label_report"} attribute and can be retrieved with
\code{\link[=lasa_label_report]{lasa_label_report()}}.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to a LASA030 functional-limitations data frame, using
the coding and questionnaire layout documented for the requested wave.
}
\details{
LASA030 changed across waves. Wave B used four response categories for the
activity questions and included three activities. Later waves used five
response categories, while the available activities and follow-up items
also varied. \code{wave} must therefore be supplied explicitly, especially
because B, 2B, 3B, and MB all use variable names beginning with \code{"b"}.

Column matching tries, in order: (1) an explicit override in
\code{name_corrections}, (2) an exact case-sensitive match, and (3) a
case-insensitive match. Variables documented for a wave but absent from
\code{data} are left untouched and recorded as \code{"not found"} in the matching
audit.

\code{name_corrections}, \code{to_factor}, \code{to_numeric}, \code{standardize_names}, and
\code{split_wavecode} are part of a parameter contract shared by every
\verb{apply_*_labels()} function in this package (see the header comment in
\code{lasa_io.R} and \code{\link[=apply_lasa046_labels]{apply_lasa046_labels()}} for the canonical
implementation). Regardless of \code{to_factor}/\code{to_numeric}, every matched
column also keeps its original SPSS value coding available as reference
attributes -- \code{attr(x, "original_labels")} and \code{attr(x, "original_values")}
-- and the \code{"respnr"} column (in any capitalization) is matched and, when
\code{standardize_names = TRUE}, renamed to \code{"respnr"}.
}
\examples{
dat_b <- data.frame(BADL1A = c(1, 4), BADL1B = c(1, 2))
dat_b <- apply_lasa030_labels(dat_b, wave = "B")
attr(dat_b$BADL1A, "label")
attr(dat_b$BADL1A, "labels")

dat_k <- data.frame(KADL1A = c(1, 5), KADL7D = c(1, 4))
dat_k <- apply_lasa030_labels(
  dat_k,
  wave = "K",
  to_factor = TRUE,
  standardize_names = TRUE
)
lasa_label_report(dat_k, problems_only = TRUE)
}
\seealso{
\code{\link[=apply_lasa046_labels]{apply_lasa046_labels()}}, \code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
