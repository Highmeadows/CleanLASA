% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA131_labels.R
\name{apply_lasa131_labels}
\alias{apply_lasa131_labels}
\title{Apply LASA131 (Social identity) SPSS labels}
\usage{
apply_lasa131_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA131 \code{.sav} file.
Expected names are \code{dqsoid01}--\code{dqsoid10} or \code{eqsoid01}--\code{eqsoid10}.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"D"} or \code{"E"}.}

\item{name_corrections}{Optional named character vector mapping suffixes
without the wave prefix (for example \code{qsoid01}) to actual names in
\code{data}.}

\item{to_factor}{Logical. If \code{TRUE}, matched variables are converted to
factors using the documented labels.}

\item{to_numeric}{Logical. Accepted for interface consistency; LASA131 has
no numeric variables eligible for conversion.}

\item{standardize_names}{Logical. If \code{TRUE}, matched names and \code{respnr} are
standardized and \code{split_wavecode} is treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{TRUE}, matched names lose the wave prefix
and a \code{LASA_wave} column is inserted after \code{respnr}.}
}
\value{
\code{data}, with LASA131 labels, requested conversion or renaming,
preserved original coding, \code{LASA_wave} provenance, and a \code{label_report}.
}
\description{
Attaches SPSS-style variable and value labels to the ten social-identity
items documented in LASA131 for waves D and E.
}
\details{
The items assess involvement, emotional connection, pride, pleasure,
identification, loyalty, honour, joining, being average, and sense of self.
All use the same strongly-disagree--strongly-agree response scale. The
solidarity score in the separate LASA331 file is outside this function's
scope.

All LASA131 variables are categorical. \code{to_numeric} is accepted for
interface consistency but does not convert them. \code{to_factor = TRUE}
converts matched variables using the documented labels.

Matching tries \code{name_corrections}, an exact case-sensitive match, and then
a case-insensitive exact match. Original SPSS coding is preserved;
standardized naming removes the wave prefix and can add \code{LASA_wave}.
}
\examples{
dat <- data.frame(dqsoid01 = c(1, 5), dqsoid03 = c(2, 4))
dat <- apply_lasa131_labels(dat, wave = "D", to_factor = TRUE)
attr(dat$dqsoid01, "labels")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
