% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA018_labels.R
\name{apply_lasa018_labels}
\alias{apply_lasa018_labels}
\title{Apply LASA018 (Health Insurance) SPSS labels}
\usage{
apply_lasa018_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASAB018 \code{.sav} file,
for example via \code{\link[haven:read_spss]{haven::read_sav()}}, containing \code{bhinsura}.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. LASA018 is documented only for \code{"B"}.}

\item{name_corrections}{Optional named character vector for explicit column
name overrides. The name is the canonical suffix \code{hinsura}, and its value
is the actual column name in \code{data}, for example
\code{c(hinsura = "B_HEALTH_INSURANCE")}.}

\item{to_factor}{Logical. If \code{FALSE} (default), \code{hinsura} remains numeric
with SPSS-style value-label attributes. If \code{TRUE}, it is converted to a
factor using the documented labels. An observed unlabelled code is
retained as a numeric-text level rather than becoming \code{NA}.}

\item{to_numeric}{Logical. Retained for the common apply-label parameter
contract. \code{hinsura} is categorical, so this argument does not change it.}

\item{standardize_names}{Logical. If \code{FALSE} (default), the source column
name is retained except for renaming requested through \code{split_wavecode}.
If \code{TRUE}, the matched column is renamed to \code{hinsura}, \code{"respnr"} is
standardized, and \code{split_wavecode} is always treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{FALSE} (default) and
\code{standardize_names = FALSE}, no wave-code splitting occurs. If \code{TRUE},
\code{bhinsura} is renamed to \code{hinsura} and a new \code{"LASA_wave"} column filled
with \code{"B"} is inserted immediately after the respondent-number column.
It is always treated as \code{TRUE} when \code{standardize_names = TRUE}.}
}
\value{
\code{data}, with variable/value-label attributes attached to the
matched LASA018 column, optional factor conversion and canonical
renaming, and \code{original_labels}/\code{original_values} attributes preserving
the original SPSS coding. A \code{"LASA_wave"} column is added when requested.
The generic name-matching audit is attached as \code{attr(data, "label_report")} and can be retrieved with \code{\link[=lasa_label_report]{lasa_label_report()}}.
}
\description{
Attaches the SPSS-style variable label (\code{attr(x, "label")}) and value
labels (\code{attr(x, "labels")}) documented for the LASAB018 health-insurance
variable. By default, the matched variable remains numeric. Optional
arguments allow converting it to a factor, correcting a mismatched column
name, and standardizing its name to the canonical LASA spelling.
}
\details{
LASA018 is documented only for wave B. Column matching tries, in order:
(1) an explicit override in \code{name_corrections}, (2) an exact
case-sensitive name match, and (3) a case-insensitive exact match. A
missing \code{bhinsura} column is recorded as \code{"not found"} in the matching
audit rather than raising an error.

\code{name_corrections}, \code{to_factor}, \code{to_numeric}, \code{standardize_names}, and
\code{split_wavecode} are part of the parameter contract shared by every
\verb{apply_*_labels()} function in this package. Regardless of conversion, the
matched column keeps its original SPSS value coding in
\code{attr(x, "original_labels")} and \code{attr(x, "original_values")}. The
\code{"respnr"} column is matched in any capitalization and, when
\code{standardize_names = TRUE}, renamed to \code{"respnr"}.
}
\examples{
dat <- data.frame(
  RespNr = 1:3,
  BHINSURA = c(-5, 1, 3)
)
dat <- apply_lasa018_labels(dat, wave = "B")
attr(dat$BHINSURA, "label")
attr(dat$BHINSURA, "labels")
}
\seealso{
\code{\link[=apply_lasa017_labels]{apply_lasa017_labels()}}, \code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
