% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA109_labels.R
\name{apply_lasa109_labels}
\alias{apply_lasa109_labels}
\title{Apply LASA109 (Care and contacts with caregivers) SPSS labels}
\usage{
apply_lasa109_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA109 \code{.sav} file.
Names use the wave prefix followed by \code{qpchc05} through \code{qpchc15},
\code{qpchc17}, or \code{qpchc18}.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"I"}, \code{"J"}, or \code{"K"}.}

\item{name_corrections}{Optional named character vector mapping canonical
suffixes without the wave prefix (for example \code{qpchc05} or \code{qpchc18}) to
actual column names in \code{data}.}

\item{to_factor}{Logical. If \code{TRUE}, matched variables are converted to
factors using the documented labels; observed unlabelled codes remain
numeric-text levels.}

\item{to_numeric}{Logical. Accepted for interface consistency; LASA109 has
no variables eligible for numeric conversion.}

\item{standardize_names}{Logical. If \code{TRUE}, matched columns are renamed to
canonical lowercase suffixes, \code{respnr} is standardized, and
\code{split_wavecode} is treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{TRUE}, matched names have their wave
prefix removed and a \code{LASA_wave} column is inserted immediately after
the respondent-number column.}
}
\value{
\code{data}, with LASA109 labels, optional factor conversion and name
standardization, preserved original SPSS coding, the \code{LASA_wave}
provenance attribute, and a \code{label_report} matching audit.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to the 13 perceived-control-in-health-care items
documented in LASA109 for waves I, J, and K.
}
\details{
Items 05--15 cover recognizing care needs, finding and arranging care,
navigating organizations and appointments, and communicating with health
professionals. Items 17 and 18 cover managing medication and following
recommendations. All items use a five-point agreement scale; items 17 and
18 additionally include code 6 for not applicable.

All LASA109 variables are categorical. \code{to_numeric} is accepted for the
shared interface but does not convert them. With \code{to_factor = TRUE},
matched variables become factors and observed unlabelled codes remain
numeric-text levels.

Column matching tries an explicit \code{name_corrections} override, an exact
case-sensitive name, then a case-insensitive exact name. Matched fields
preserve their original SPSS coding in \code{original_labels} and
\code{original_values}. Requested standardization removes the wave prefix,
standardizes \code{respnr}, and inserts \code{LASA_wave}.
}
\examples{
dat <- data.frame(iqpchc05 = c(1, 3, 5), iqpchc17 = c(1, 5, 6))
dat <- apply_lasa109_labels(dat, wave = "I")
attr(dat$iqpchc17, "labels")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
