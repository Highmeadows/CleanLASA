% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA078_labels.R
\name{apply_lasa078_labels}
\alias{apply_lasa078_labels}
\title{Apply LASA078 (Need for affiliation) SPSS labels}
\usage{
apply_lasa078_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from LASAB078 or LASAE078.}

\item{wave}{Character scalar identifying wave \code{"B"} or \code{"E"}; matching is
case-insensitive.}

\item{name_corrections}{Optional named character vector mapping canonical
suffixes (\code{an1}, \code{an4}, \code{an6}, \code{an8}, \code{an9}, or \code{an10}) to actual names.}

\item{to_factor}{Logical. Convert matched items to factors with documented
value labels; observed undocumented codes remain numeric-text levels.}

\item{to_numeric}{Logical. Accepted for interface consistency; LASA078 has
no eligible numeric variables.}

\item{standardize_names}{Logical. Rename matched variables to lowercase
canonical suffixes, standardize \code{respnr}, and imply wave splitting.}

\item{split_wavecode}{Logical. Remove the wave prefix from matched names and
insert \code{LASA_wave} after the respondent-number column.}
}
\value{
The labelled data, with requested factor conversion and canonical
names, preserved original coding, \code{LASA_wave}, and \code{label_report}.
}
\description{
Attaches the variable and value labels documented for the six need-for-
affiliation questionnaire items in LASAB078 and LASAE078. The items cover
talking about feelings, seeking support, coping alone, going to others,
keeping feelings private, and leaning on someone during difficulties.
}
\details{
Both waves use no/more-or-less/yes responses but have different documented
missing-reason labels. All six variables are categorical; \code{to_numeric} is
accepted for the shared interface but does not convert them. Constructed
scale filecode LASA278, documented in the same PDF, is intentionally
excluded and belongs to \code{apply_lasa278_labels()}.

Matching tries \code{name_corrections}, exact names, then case-insensitive exact
names. Factor conversion and canonical renaming/wave splitting preserve the
original SPSS coding and use the shared \code{label_report} audit.
}
\examples{
dat <- data.frame(ban1 = c(-4, 1, 3), ban4 = c(1, 2, 3))
dat <- apply_lasa078_labels(dat, wave = "B")
attr(dat$ban1, "labels")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
