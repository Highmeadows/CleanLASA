% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA120_labels.R
\name{apply_lasa120_labels}
\alias{apply_lasa120_labels}
\title{Apply LASA120 (Sexuality) SPSS labels}
\usage{
apply_lasa120_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA120 \code{.sav} file.}

\item{wave}{Character scalar identifying \code{"B"}, \code{"E"}, \code{"2B"}, \code{"F"}, or
\code{"3B"}; matching is case-insensitive.}

\item{name_corrections}{Optional named character vector whose names are
canonical suffixes without the wave prefix, such as \code{qsex01}, and whose
values are actual column names in \code{data}.}

\item{to_factor}{Logical. Convert matched variables to factors using the
documented value labels.}

\item{to_numeric}{Logical. Accepted for interface consistency; LASA120 has
no numeric variables eligible for conversion.}

\item{standardize_names}{Logical. Standardize matched names and respondent
number, remove wave prefixes, and add \code{LASA_wave}.}

\item{split_wavecode}{Logical. Remove wave prefixes from matched names and
add \code{LASA_wave}; implied by \code{standardize_names = TRUE}.}
}
\value{
\code{data} with LASA120 metadata, optional conversion and renaming,
original-coding attributes, \code{LASA_wave}, and a \code{label_report} audit.
}
\description{
Attaches SPSS-style variable and value labels to the sexuality variables
documented for LASA120. Waves B, 2B, and 3B contain three evaluations of
sexual life. Wave E contains those three items plus two attitudes about
sexuality and intimacy in later life. Wave F contains items 2 through 5.
}
\details{
All LASA120 variables are categorical and use \code{-1} for no answer.
\code{to_factor = TRUE} converts matched variables with their documented
unpleasant/pleasant, unimportant/important, or disagreement/agreement
scales while retaining observed unlabelled codes. No variable is eligible
for \code{to_numeric}.

Matching tries \code{name_corrections}, an exact name, then a case-insensitive
exact name. Original labels and values are retained in \code{original_labels}
and \code{original_values} attributes.
}
\examples{
dat <- data.frame(RespNr = 1:3, BQSEX01 = c(-1, 1, 5))
dat <- apply_lasa120_labels(dat, wave = "B")
attr(dat$BQSEX01, "labels")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
