% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA349_labels.R
\name{apply_lasa349_labels}
\alias{apply_lasa349_labels}
\title{Apply LASA349 (General Self-Regulation Scale Scores) SPSS labels}
\usage{
apply_lasa349_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA349 \code{.sav} file.
Documented names use a \code{g} or \code{h} wave prefix followed by \code{qgsr},
\code{qgsrsu}, \code{qgsrmk}, \code{qgsracp}, or \code{qmisgsr}.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"G"} or \code{"H"}.}

\item{name_corrections}{Optional named character vector mapping canonical
LASA349 suffixes without the wave prefix to actual column names in
\code{data}.}

\item{to_factor}{Logical. If \code{TRUE}, the four variables carrying documented
value labels are converted to factors. Observed unlabelled scores remain
numeric-text levels.}

\item{to_numeric}{Logical. If \code{TRUE}, all five variables are restored to
plain numeric and negative values are replaced by \code{NA}.}

\item{standardize_names}{Logical. If \code{TRUE}, matched columns are renamed to
canonical lowercase suffixes without the wave prefix, \code{respnr} is
standardized, and \code{split_wavecode} is treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{TRUE}, matched columns have the wave
prefix removed and a \code{LASA_wave} column containing the selected wave is
inserted immediately after the respondent-number column.}
}
\value{
\code{data}, with LASA349 variable/value labels, optional conversion and
standardized naming, preserved original SPSS coding, the \code{LASA_wave}
provenance attribute, and a \code{label_report} matching audit.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to the five constructed General Self-Regulation
variables documented in LASA349 for waves G and H.
}
\details{
The inventory comprises the total General Self-Regulation score, strategy
use and metastrategy-knowledge subscales, an action-and-coping-planning
subscale, and a count of missing General Self-Regulation values. The four
scores use \code{-2} for no valid data and have documented low/high endpoints.
The missing-value count has no documented value-label map. The 14 source
questionnaire items belong to LASA149 and are intentionally outside this
function's inventory.

All five variables are numeric. With \code{to_numeric = TRUE}, negative missing
codes become \code{NA}; \code{to_numeric} therefore takes precedence over
\code{to_factor}. With \code{to_factor = TRUE} alone, the four value-labelled scores
become factors and observed unlabelled interior scores remain numeric-text
levels. The unlabelled missing-value count remains numeric.

Column matching tries, in order: (1) an explicit override in
\code{name_corrections}, (2) an exact case-sensitive match, and (3) a
case-insensitive exact match. Variables absent from \code{data} are recorded as
\code{"not found"} in the matching audit. Original labels and values are
preserved, and optional standardized naming removes the wave prefix,
standardizes \code{respnr}, and adds \code{LASA_wave}.
}
\examples{
dat <- data.frame(RespNr = 1:2, gqgsr = c(-2, 70), gqmisgsr = c(2, 0))
dat <- apply_lasa349_labels(dat, wave = "G", to_numeric = TRUE)
attr(dat$gqgsr, "label")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
