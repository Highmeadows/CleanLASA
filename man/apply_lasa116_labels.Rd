% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA116_labels.R
\name{apply_lasa116_labels}
\alias{apply_lasa116_labels}
\title{Apply LASA116 (Decubitus) SPSS labels}
\usage{
apply_lasa116_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from LASAB116.}

\item{wave}{Character scalar. Only \code{"B"} is documented and accepted;
matching is case-insensitive.}

\item{name_corrections}{Optional named character vector whose names are
canonical suffixes (\code{qdecu01} or \code{qdecu02}) and whose values are actual
column names in \code{data}.}

\item{to_factor}{Logical. Convert matched variables to factors using the
documented value labels.}

\item{to_numeric}{Logical. Accepted for interface consistency; LASA116 has
no numeric variables eligible for conversion.}

\item{standardize_names}{Logical. Standardize matched names and respondent
number, remove the \code{b} wave prefix, and add \code{LASA_wave}.}

\item{split_wavecode}{Logical. Remove the wave prefix from matched names
and add \code{LASA_wave}; implied by \code{standardize_names = TRUE}.}
}
\value{
\code{data} with LASA116 metadata, optional conversion and renaming,
original-coding attributes, \code{LASA_wave}, and a \code{label_report} audit.
}
\description{
Attaches SPSS-style variable and value labels to the two decubitus items
documented in LASAB116. The self-administered questionnaire records
whether the respondent suffered bed sores and, separately, whether the
respondent received treatment.
}
\details{
LASA116 is documented only for wave B. Both variables are categorical.
\code{qdecu01} distinguishes no bed sores from three documented duration
categories; \code{qdecu02} records treatment. Both use \code{-1} for no answer.

Matching tries \code{name_corrections}, an exact name, then a case-insensitive
exact name. \code{to_factor = TRUE} converts the items using their documented
value maps and preserves any observed unlabelled codes as numeric-text
levels. No variable is eligible for \code{to_numeric}. Original labels and
values are retained in \code{original_labels} and \code{original_values}.
}
\examples{
dat <- data.frame(BQDECU01 = c(-1, 1, 4), bqdecu02 = c(-1, 1, 2))
dat <- apply_lasa116_labels(dat, wave = "B")
attr(dat$BQDECU01, "labels")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
