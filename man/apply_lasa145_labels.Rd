% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA145_labels.R
\name{apply_lasa145_labels}
\alias{apply_lasa145_labels}
\title{Apply LASA145 (Body shape) SPSS labels}
\usage{
apply_lasa145_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA145 \code{.sav} file,
for example via \code{\link[haven:read_spss]{haven::read_sav()}}.}

\item{wave}{Character scalar identifying the LASA wave. One of \code{"F"},
\code{"G"}, \code{"3B"}, or \code{"MB"}; matching is case-insensitive.}

\item{name_corrections}{Optional named character vector. Names are
canonical suffixes without a wave prefix (for example \code{qcshape}) and
values are actual column names in \code{data}.}

\item{to_factor}{Logical. Convert labelled categorical variables to factors
using their documented value labels.}

\item{to_numeric}{Logical. Accepted for the shared interface; LASA145 has
no numeric variables eligible for conversion.}

\item{standardize_names}{Logical. Rename matched columns to lowercase
suffixes without the wave prefix, standardize the respondent-number
column to \code{respnr}, and add \code{LASA_wave}.}

\item{split_wavecode}{Logical. Remove the wave prefix from matched column
names and add \code{LASA_wave}. Always enabled by \code{standardize_names = TRUE}.}
}
\value{
\code{data} with documented metadata applied, optional conversion and
renaming, a \code{LASA_wave} attribute, and a matching audit in the
\code{label_report} attribute.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to the perceived-body-shape variables documented for
LASA145. These cover current and ideal silhouettes, plus recalled body shape
at age 40 where that item was collected.
}
\details{
LASA145 is documented for waves F, G, 3B, and MB. Waves F and G contain
current and ideal body shape; wave 3B additionally contains body shape at
age 40. The MB medical-interview file contains current body shape and body
shape at age 40, using the documented \code{bm} prefix. Every item uses silhouette
codes 1 through 9 and the \code{-1} no-valid-data code. The discrepancy score
shown in the same codebook belongs to filecode LASA345 and is not included.

Column matching tries, in order: an explicit \code{name_corrections} override,
an exact case-sensitive match, and a case-insensitive exact match.
\code{to_factor = TRUE} converts matched silhouette variables to factors while
retaining observed unlabelled codes as numeric-text levels. LASA145 has no
numeric variables eligible for \code{to_numeric} conversion. Original values and
value labels are preserved in \code{original_values} and \code{original_labels}.
}
\examples{
dat <- data.frame(
  RespNr = 1:3,
  FQCSHAPE = c(-1, 1, 9),
  fqishape = c(1, 5, 9)
)
dat <- apply_lasa145_labels(dat, wave = "F")
attr(dat$FQCSHAPE, "labels")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
