% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_LASA110_labels.R
\name{apply_lasa110_labels}
\alias{apply_lasa110_labels}
\title{Apply LASA110 (Satisfaction and important life aspects) SPSS labels}
\usage{
apply_lasa110_labels(
  data,
  wave,
  name_corrections = NULL,
  to_factor = FALSE,
  to_numeric = FALSE,
  standardize_names = FALSE,
  split_wavecode = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble imported from a LASA110 \code{.sav} file.
Names include a wave prefix followed by \code{qsatis1}, \code{qsatis2},
\code{qimpor1}--\code{qimpor9}, and \code{qnansw}; H, I, and J also use \code{qsatis3} and
\code{qsatis4}.}

\item{wave}{Character scalar identifying the LASA wave, matched
case-insensitively. One of \code{"B"}, \code{"C"}, \code{"D"}, \code{"E"}, \code{"2B"}, \code{"F"},
\code{"G"}, \code{"H"}, \code{"3B"}, \code{"I"}, \code{"J"}, or \code{"K"}.}

\item{name_corrections}{Optional named character vector mapping canonical
suffixes without the wave prefix (for example \code{qsatis1}, \code{qimpor4}, or
\code{qnansw}) to actual column names in \code{data}.}

\item{to_factor}{Logical. If \code{TRUE}, categorical variables are converted to
factors using the documented labels; observed unlabelled codes remain
numeric-text levels.}

\item{to_numeric}{Logical. If \code{TRUE}, the aspect-count variable and, where
present, the 0--10 satisfaction score are restored to plain numeric and
negative values are replaced by \code{NA}.}

\item{standardize_names}{Logical. If \code{TRUE}, matched columns are renamed to
canonical lowercase suffixes without the wave prefix, \code{respnr} is
standardized, and \code{split_wavecode} is treated as \code{TRUE}.}

\item{split_wavecode}{Logical. If \code{TRUE}, matched names have their wave
prefix removed and \code{LASA_wave} is inserted immediately after the
respondent-number column.}
}
\value{
\code{data}, with LASA110 labels, optional conversion and standardized
naming, preserved original SPSS coding, the \code{LASA_wave} provenance
attribute, and a \code{label_report} matching audit.
}
\description{
Attaches SPSS-style variable labels (\code{attr(x, "label")}) and value labels
(\code{attr(x, "labels")}) to the life-satisfaction and important-life-aspect
variables documented in LASA110 for waves B through K, including
replenishment waves 2B and 3B.
}
\details{
Every documented wave contains two five-point satisfaction items, nine
indicators for important life aspects, and a count of mentioned aspects.
The fourth aspect is labelled \verb{sensible spending of time} in waves B--H
except 3B, and \verb{meaningful pastime} in 3B, I, J, and K. Waves H, I, and J
additionally contain a 0--10 current-satisfaction score and a comparison
with the previous year.

The aspect-count variable and the 0--10 current-satisfaction score are
eligible for \code{to_numeric}; observed negative missing codes become \code{NA}.
Other LASA110 fields are categorical. With \code{to_factor = TRUE}, categorical
variables become factors and observed unlabelled codes remain numeric-text
levels. \code{to_numeric} takes precedence for eligible fields.

Column matching tries an explicit \code{name_corrections} override, an exact
case-sensitive name, then a case-insensitive exact name. Every matched
field preserves its original SPSS coding in \code{original_labels} and
\code{original_values}. Requested standardization removes the wave prefix,
standardizes \code{respnr}, and inserts \code{LASA_wave}.
}
\examples{
dat <- data.frame(hqsatis1 = c(1, 5), hqnansw = c(0, 9))
dat <- apply_lasa110_labels(dat, wave = "H", to_numeric = TRUE)
attr(dat$hqsatis1, "labels")
}
\seealso{
\code{\link[=lasa_label_report]{lasa_label_report()}}, \code{\link[=read_lasa_sav]{read_lasa_sav()}}
}
